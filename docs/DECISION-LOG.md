# QuietKey Decision Log

EXPERIMENTAL — NO REAL FUNDS — NOT A WALLET

Design entries below were recorded as `DRAFT — PENDING OWNER APPROVAL` at creation. The explicit owner approval covering QK-DEC-001…014 as part of baseline H0 is recorded under Owner approval records below (QK-APR-2026-08-18-001). No implementation evidence or audit results exist. Only an explicit owner-approval entry added here may change any decision's status.

| Date | ID | Title | Status | Rationale | Architecture link |
|---|---|---|---|---|---|
| 2026-08-18 | QK-DEC-001 | Product philosophy | DRAFT — PENDING OWNER APPROVAL | Stealth and robustness principles anchor every later choice; camouflage explicitly subordinated to cryptography. | [ARCHITECTURE.md § QK-DEC-001](../ARCHITECTURE.md#qk-dec-001--product-philosophy) |
| 2026-08-18 | QK-DEC-002 | Custody topology | DRAFT — PENDING OWNER APPROVAL | Standard P2WSH sortedmulti 2-of-3 gives single-loss tolerance with well-understood, widely reviewable script semantics. | [ARCHITECTURE.md § QK-DEC-002](../ARCHITECTURE.md#qk-dec-002--custody-topology) |
| 2026-08-18 | QK-DEC-003 | Fixed v1 Bitcoin profile | DRAFT — PENDING OWNER APPROVAL | One fixed mainnet/BIP48 profile with no passphrase or 12-word variants minimizes recovery ambiguity and mistake surface. | [ARCHITECTURE.md § QK-DEC-003](../ARCHITECTURE.md#qk-dec-003--fixed-v1-bitcoin-profile) |
| 2026-08-18 | QK-DEC-004 | Physical elements and cloak | DRAFT — PENDING OWNER APPROVAL | Everyday-looking objects (document, cards, calculator) hide custody in plain sight while remaining safe after recognition. | [ARCHITECTURE.md § QK-DEC-004](../ARCHITECTURE.md#qk-dec-004--physical-elements-and-cloak) |
| 2026-08-18 | QK-DEC-005 | Use and recovery hierarchy | DRAFT — PENDING OWNER APPROVAL | Every single-object loss leaves a defined pair path; mandatory rotate-and-sweep prevents reuse of a degraded wallet. | [ARCHITECTURE.md § QK-DEC-005](../ARCHITECTURE.md#qk-dec-005--use-and-recovery-hierarchy) |
| 2026-08-18 | QK-DEC-006 | Bearer-card and terminal model | DRAFT — PENDING OWNER APPROVAL | Possession-is-authorization avoids PIN/vendor dependencies; least-authority card payload limits blast radius, pending exact-card feasibility. | [ARCHITECTURE.md § QK-DEC-006](../ARCHITECTURE.md#qk-dec-006--bearer-card-and-terminal-model) |
| 2026-08-18 | QK-DEC-007 | Entropy product profile | DRAFT — PENDING OWNER APPROVAL | Four independent 256-bit values prevent common-master failure; fail-closed dice mode gives an auditable physical alternative. | [ARCHITECTURE.md § QK-DEC-007](../ARCHITECTURE.md#qk-dec-007--entropy-product-profile) |
| 2026-08-18 | QK-DEC-008 | A1 digital-cryptography baseline | DRAFT — PENDING OWNER APPROVAL | Standard AEAD (ChaCha20-Poly1305) with HKDF domain separation and AAD context binding; no plaintext before authentication. | [ARCHITECTURE.md § QK-DEC-008](../ARCHITECTURE.md#qk-dec-008--a1-digital-cryptography-baseline) |
| 2026-08-18 | QK-DEC-009 | PSBT and transport profile | DRAFT — PENDING OWNER APPROVAL | Single strict input formats (PSBT v0, binary .psbt, BBQr P) shrink the hostile parsing surface; trusted core revalidates everything. | [ARCHITECTURE.md § QK-DEC-009](../ARCHITECTURE.md#qk-dec-009--psbt-and-transport-profile) |
| 2026-08-18 | QK-DEC-010 | Software trust boundary | DRAFT — PENDING OWNER APPROVAL | Separating secret-owning core from unprivileged transport and decoy bounds compromise, without overclaiming against a hostile kernel. | [ARCHITECTURE.md § QK-DEC-010](../ARCHITECTURE.md#qk-dec-010--software-trust-boundary) |
| 2026-08-18 | QK-DEC-011 | Kit tiers | DRAFT — PENDING OWNER APPROVAL | Packaging and instructions may vary by audience while the cryptographic core stays identical across all kits. | [ARCHITECTURE.md § QK-DEC-011](../ARCHITECTURE.md#qk-dec-011--kit-tiers) |
| 2026-08-18 | QK-DEC-012 | Quantum posture | DRAFT — PENDING OWNER APPROVAL | Honest posture: exposure reduction and a reviewed migration plan; no post-quantum claims before Bitcoin consensus support. | [ARCHITECTURE.md § QK-DEC-012](../ARCHITECTURE.md#qk-dec-012--quantum-posture) |
| 2026-08-18 | QK-DEC-013 | Source and licensing boundary | DRAFT — PENDING OWNER APPROVAL | Clean-room discipline: no third-party or legacy code without recorded commit, license, provenance, purpose, and review status. | [ARCHITECTURE.md § QK-DEC-013](../ARCHITECTURE.md#qk-dec-013--source-and-licensing-boundary) |
| 2026-08-18 | QK-DEC-014 | Maturity and release claims | DRAFT — PENDING OWNER APPROVAL | Host simulation cannot prove physical properties; STOP-SHIP holds until exact-target evidence, reproducible builds, review, and rehearsals exist. | [ARCHITECTURE.md § QK-DEC-014](../ARCHITECTURE.md#qk-dec-014--maturity-and-release-claims) |

## Administrative entries

| Date | Entry | Status | Notes |
|---|---|---|---|
| 2026-08-18 | Root MIT LICENSE removed by explicit owner authorization; repository license is now an OPEN owner decision (see docs/OPEN-DECISIONS.md OD-08). No replacement selected. | RECORDED | Sole authorized deletion during F0. |


## Owner approval records

Entries in this section are immutable once checkpointed; corrections require a superseding approval record. Never edit an existing record.

### QK-APR-2026-08-18-001 — Corrected F0/F1 baseline approval

- **ID:** QK-APR-2026-08-18-001
- **Date:** 2026-08-18
- **Approver:** Project owner
- **Approved substantive baseline H0:** `c618407a3900657d8ce4c479c4056f859f86bec6`
- **Scope:** the corrected F0/F1 documents at H0, including QK-DEC-001…014 and the corrected requirements, threat, traceability, lifecycle, resource-budget, and planned-test baselines.
- **Authorized next scope:** F2 feasibility only.
- **Not approved:** implementation or use with funds; Bitcoin/wallet/cryptographic code; production language/toolchain/dependency selection; card/hardware selection; the entropy conditioner; A1 codec parameters; PSBT authorization choices; numeric limits; boot/update design; human-factors thresholds; kit wording; license or governance appointments; closure of any gate; F3 and beyond.
- **Effect:** the architecture-owner-approval release blocker is satisfied; Gates A–E remain OPEN; implementation evidence remains none; OD-01…08 remain unresolved.

### QK-AUTH-F3F4-001 — Host-only F3/F4 bootstrap authorization

- **ID:** QK-AUTH-F3F4-001
- **Date:** 2026-08-18
- **Approver:** Project owner
- **Owner words:** “Yes, go ahead.” (approving a software-first, bounded F3/F4 host bootstrap before hardware purchase, in the immediately preceding explicit scope; see docs/HOST-WORK-AUTHORIZATION.md)
- **Scope:** host-only F3 profile and public test-vector preparation; host-only F4 interface and state-machine scaffold; the reversible rust-stable Replit module as a HOST bootstrap toolchain only.
- **Not approved:** any production toolchain selection (OD-01 remains open); any license/SPDX choice (OD-08 remains open); F5–F12; gate closure; any QK-LIM/QK-TST/evidence/specimen/hardware transition; physical or exact-target work.
- **Effect:** F2 remains AUTHORIZED — PREPARATION COMPLETE — PHYSICAL/EXACT-TARGET WORK BLOCKED — OVERALL INCOMPLETE; F3/F4 become AUTHORIZED host-only as recorded in docs/HOST-WORK-AUTHORIZATION.md; Gates A–E remain OPEN; OD-01…08 remain unresolved; host results are HOST evidence only, never TARGET evidence.

### QK-AUTH-F3.1A-001 — F3.1a correction-only remediation authorization

- **ID:** QK-AUTH-F3.1A-001
- **Date:** 2026-08-18
- **Approver:** Project owner
- **Owner words exactly:** “Please continue, full authorization sustained.”
- **Context:** this immediately approved continuing the explicitly proposed F3.1a correction-only PSBT draft remediation, independent audit, and owner-decision-packet preparation.
- **Published parent:** `145f960e659334be55afc11a1e0427c23c0f3b5e`.
- **Authorized next commits:** Commit B changing exactly `docs/SOURCE-REGISTER.md`, `docs/f3/PSBT-V0-REVIEW-PROFILE-DRAFT.md`, `docs/f3/README.md`, `replit.md`, and `tools/verify-host-boundary.sh`; then Commit C changing exactly the current-stage consistency-checker path `tools/verify-current-stage.sh`.
- **Explicit exclusions:** no profile acceptance; no D item selected; no parser/code/vectors/fixtures/crypto/signing; no gate/OD/QK-LIM/QK-TST/evidence change; no hardware/Flux/procurement/fabrication; no license/governance-role selection; no claim that a repository script authenticates itself.
- **Effect:** F3 remains authorized host-only drafting, incomplete, no profile accepted, no target evidence; all other existing statuses unchanged.

### Effective-status clarification (append-only; alters no prior row or record)

QK-DEC-001…014 are owner-approved as part of baseline H0 by the existing record QK-APR-2026-08-18-001; implementation remains unvalidated and all Gates remain OPEN. The original table cells above remain the historical status-at-creation and are intentionally unedited. No in-repo digest is a trust anchor.

### QK-AUTH-F3.1B-001 — Status-accuracy erratum authorization

- **ID:** QK-AUTH-F3.1B-001
- **Date:** 2026-08-18
- **Approver:** Project owner
- **Owner words exactly:** “Agreed”
- **Context:** the owner approved the immediately preceding explicit two-stage proposal to (1) correct stale present-state wording and the CARD-006 Evidence-family omission, then (2) prepare a separate non-binding PSBT owner-decision evidence packet. This record authorizes ONLY stage (1), the status-accuracy erratum chain below; the later owner-decision evidence packet remains a separate checkpoint and is not begun by this authorization.
- **Published parent:** `9ffea22a75946a53b7aa4d358cf25844bb3e323a`.
- **Authorized next commits:** Commit B changing exactly `README.md`, `SECURITY.md`, `docs/BUILD-ROADMAP.md`, `docs/HOST-WORK-AUTHORIZATION.md`, `docs/REQUIREMENTS.md` (append-only), and `tools/verify-host-boundary.sh`; then Commit C changing exactly the current-stage consistency-checker path `tools/verify-current-stage.sh`.
- **Explicit exclusions:** no architecture/profile/D-item acceptance; no vectors, fixtures, corpora, parser, crypto, seed, signing, QR/SD/card/target code; no gate, OD, QK-LIM, QK-TST status, evidence, toolchain, dependency, license, hardware, Flux, procurement, or fabrication change.
- **Effect:** wording accuracy and one append-only requirements erratum only; every substantive status remains exactly as previously recorded.

### QK-AUTH-F3.1B-R2-001 — F3.1b Revision 2 policy-direction authorization

- **ID:** QK-AUTH-F3.1B-R2-001
- **Date:** 2026-08-18
- **Approver:** Project owner
- **Owner words exactly:** “Agreed, I approve this direction.”
- **Context:** given immediately after the consolidated Revision 2 direction. It approves RECORDING owner-selected policy directions for D-01 (prevout), D-03 (ceremony-pair signatures), D-08 (review/fee-rate), and D-12 (transaction version/temporal policy), a future-only D-05 recipient-P2TR direction, and bounded citation-only source additions. It is direction approval only — NOT retroactive approval, NOT profile acceptance, NOT implementation authorization, NOT evidence, and NOT a trust anchor.
- **Published parent:** `d9277cd2fcd8d699448a1667624123a3347118d6`.
- **Authorized next commits:** Commit B changing exactly `docs/SOURCE-REGISTER.md`, `docs/f3/PSBT-V0-REVIEW-PROFILE-DRAFT.md`, `docs/f3/README.md`, and `tools/verify-host-boundary.sh`; then Commit C changing exactly the current-stage consistency-checker path `tools/verify-current-stage.sh`.
- **Explicit limits and exclusions:** does NOT accept the PSBT profile or any clause as normative; does NOT amend canonical architecture; resolves NO OD; chooses NO numeric QK-LIM value; generates/runs NO vector; changes NO QK-TST or evidence status; closes NO gate; authorizes NO parser/crypto/signing/QR/SD/card/target code; approves NO procurement/fabrication; makes NO production/security/funds claim. All clauses remain PROPOSED/NON-NORMATIVE until separate profile acceptance. Gates A–E remain OPEN; OD-01..08 and every QK-LIM remain OPEN; QK-TST remains PLANNED — NOT RUN; F2 remains physically blocked/incomplete; F3/F4 remain host-only/incomplete; F5–F12 remain unauthorized.

### QK-AUTH-F3.2A-001 — F3.2a Wallet Trust Spine drafting authorization

- **ID:** QK-AUTH-F3.2A-001
- **Date:** 2026-08-19
- **Approver:** Project owner
- **Owner words exactly:** “Agreed, please continue”
- **Context:** given immediately after the explicit recommendation for a docs-only Wallet Trust Spine candidate and exercised under QK-AUTH-F3F4-001. These words authorize preparation of a proposed/non-normative F3.2a draft only; they do not accept a clause/profile, resolve an OD, select a limit, authorize implementation, or change canonical architecture/requirements.
- **Published parent:** `a9d1f205cfa879a6f54b8838256d36e469cfed97`.
- **Authorizes only:** proposed canonical receive/change descriptor and wallet_id byte candidates already bounded by QK-DEC-003; A/B/C role-to-key mapping; provisioning/recovery trust-establishment invariants; A1+B, A1+C and B+C card-independent ceremony requirements; analysis of sequential-card/single-interface prerequisites and conflicts; explicit card-independent versus OD-02/APDU-blocked boundaries.
- **Authorized next commits:** Commit B changing exactly `docs/SOURCE-REGISTER.md`, `docs/f3/WALLET-TRUST-SPINE-DRAFT.md` (new), `docs/f3/README.md`, and `tools/verify-host-boundary.sh`; then Commit C changing exactly the current-stage consistency-checker path `tools/verify-current-stage.sh`.
- **Explicit exclusions:** no architecture/requirements/open-decision amendment; no owner-decision resolution; no QK-LIM/QK-TST/evidence/gate transition; no vectors, fixtures, corpus or payload; no parser, crypto, signing, seed, QR, SD, card or target code; no real keys/descriptors/addresses/funds; no APDU, nonce, anti-exfil, attestation, terminal-pairing or exact-target mechanism; no license/governance/hardware/Flux/procurement/fabrication transition; F5–F12 remain unauthorized.
- **Effect:** F3 remains host-only drafting, incomplete, with no profile accepted and no target evidence. OD-08 and Flux intake are separate future checkpoints outside this chain.

### QK-AUTH-OD08-PKT-001 — OD-08 non-binding decision-packet preparation authorization

- **ID:** QK-AUTH-OD08-PKT-001
- **Date:** 2026-08-19
- **Approver:** Project owner
- **Owner words exactly:** “Perfect, go ahead”
- **Context:** given immediately after the recommendation to prepare the separate OD-08 licensing-and-governance decision packet, followed later by separately authorized F3.2b decisions. The owner authorized decision-packet preparation only; no packet option or policy was selected; this record does not claim that the prior recommendation explicitly mentioned independent audit or publication.
- **Published parent:** `e81ab9c61dda9be60ac45facbe5db57115578540`.
- **Authorizes only:** local preparation of a factual current-state and rights-chain inventory; an informational comparison of unselected standard license families for software, hardware designs, documentation, and ancillary material; separately answerable owner questions covering licensing, contributions, maintainership, review, vulnerability reporting, release signing, supported versions, audit ownership, and trademark/official-build boundaries; independent read-only audit of the prepared packet; and the exact verifier rebind below. Publication is not authorized by this record and requires a later explicit owner instruction after a passing independent audit.
- **Authorized next commits:** Commit B adding exactly `docs/OD-08-DECISION-PACKET.md` and changing exactly `tools/verify-host-boundary.sh` solely to advance its byte-identity Decision-Log anchor to Commit A while preserving all other host-boundary predicates; then Commit C changing exactly `tools/verify-current-stage.sh`.
- **Explicit exclusions:** no license selection, grant, application, modification, or legal conclusion; no `LICENSE`, `LICENCE`, `COPYING`, `COPYRIGHT`, `NOTICE`, `AUTHORS`, `CONTRIBUTING`, `CODEOWNERS`, `REUSE.toml`, SPDX header, manifest license field, DCO/CLA activation, or third-party text/code import; no assertion of copyright ownership or contributor consent; no OD resolution; no architecture, requirement, limit, test, evidence, maturity-gate, milestone, release, or support-status change; no person, organization, handle, email, channel, maintainer, reviewer, key custodian, auditor, or service appointment; no repository/hosting setting, branch rule, private-reporting channel, release-signing mechanism, or supported-version policy activation; no code, vector, fixture, dependency, hardware, Flux, procurement, fabrication, or use-with-funds authorization; no host-boundary scope, acceptance predicate, test expectation, status rule, file allowlist, platform/dependency check, or semantic claim may change except the exact necessary Decision-Log anchor advance.
- **Effect:** OD-08 remains OPEN in full; this chain grants no new rights and does not determine or revoke any rights that may have arisen from historical repository versions; the repository still has no selected project license; no governance role or channel exists by virtue of this record; Gates A–E remain OPEN, STOP-SHIP remains in force, every other status remains unchanged; and this chain remains local and unpublished pending a separate explicit owner publication approval.

### QK-AUTH-OD08-RSP-001 — OD-08 non-enacting owner-response record preparation authorization

- **ID:** QK-AUTH-OD08-RSP-001
- **Date:** 2026-08-19
- **Approver:** Project owner
- **Owner words exactly:** “Authorize preparation and independent audit of the OD‑08 owner-response record from 8c18266. No license application or settings changes.”
- **Context:** given after publication of the audited OD-08 decision-packet chain ending at the published parent below. The owner authorized preparation and independent audit of a non-enacting owner-response record only; no license application, settings change, or policy enactment was authorized; the owner responses recorded under this authorization are direction input only and are not independently verified legal facts.
- **Published parent:** `8c18266c6fc6c00831f444f51a6a1f762250a12d`.
- **Authorizes only:** transcription of the owner-provided responses into a non-enacting owner-response record; local verification of the prepared chain; independent read-only audit of the prepared record; export of the exact unpushed tree for that audit; Commit B; and Commit C. Publication is not authorized by this record and requires a later explicit owner instruction after passing independent audits.
- **Authorized next commits:** Commit B adding exactly `docs/OD-08-OWNER-RESPONSE-RECORD.md` and changing exactly `tools/verify-host-boundary.sh` solely to advance its byte-identity Decision-Log anchor to Commit A while preserving all other host-boundary predicates; then Commit C changing exactly `tools/verify-current-stage.sh`.
- **Explicit exclusions:** no publication; no license or SPDX selection, application, or grant; no legal conclusion; no OD-08 closure; no policy, role, channel, repository or hosting setting, support promise, release, or maturity-gate activation; no protected canonical-document change; no code, vector, fixture, dependency, hardware, Flux, procurement, or fabrication work; no assertion of copyright ownership, contributor consent, or chain-of-title proof; no person, organization, handle, email, channel, maintainer, reviewer, key custodian, auditor, or service appointment takes operational effect by virtue of this record.
- **Effect:** OD-08 remains OPEN in full; this chain enacts nothing, grants no rights, and does not determine or revoke any rights that may have arisen from historical repository versions; the repository still has no selected project license; Gates A–E remain OPEN, STOP-SHIP remains in force, every other status remains unchanged; and this chain remains local and unpublished pending a later explicit owner publication instruction after passing independent audits.

### QK-AUTH-F3.2B-PKT-001 — F3.2b non-binding PSBT decision-packet preparation authorization

- **ID:** QK-AUTH-F3.2B-PKT-001
- **Date:** 2026-08-19
- **Approver:** Project owner
- **Owner words exactly:** “Authorize preparation and independent audit of the docs-only F3.2b PSBT decision packet from a9840819. No profile acceptance, implementation, vectors, license application, hardware work, or settings changes.”
- **Context:** given after publication of the audited OD-08 owner-response chain ending at the published parent below. The owner authorized preparation and independent audit of a non-binding F3.2b PSBT decision packet only: decision input, not an answer, selection, adoption, or acceptance of any profile or clause, and not publication or any later implementation or application authority. The prepared chain includes the single mechanically necessary host-verifier file-set addition described below because the packet lives inside the host verifier’s exact tracked host file set scope; this implementation detail does not broaden the owner’s quoted authorization or alter any other host-boundary predicate.
- **Published parent:** `a9840819eba4d1b98b8346fc0ae0b44f611a45c5`.
- **Authorizes only:** local preparation of the non-binding F3.2b PSBT decision packet; local verification of the prepared chain; independent read-only audit of the prepared packet; export of the exact unpushed tree for that audit; Commit B adding exactly `docs/f3/F3.2B-PSBT-DECISION-PACKET.md` and changing exactly `tools/verify-host-boundary.sh` solely to reanchor its Decision-Log byte-identity anchor to this Commit A and to add exactly `docs/f3/F3.2B-PSBT-DECISION-PACKET.md` as one line to its exact authorized tracked host file set, while preserving every other host-boundary predicate; then Commit C changing exactly `tools/verify-current-stage.sh`. Publication is not authorized by this record and requires a later explicit owner instruction after passing independent audits.
- **Packet scope:** exactly four owner questions: D-02, present-profile D-05, D-06, and D-10. D-00 remains blocked; D-04 remains owner-open and is not posed in this four-question packet; representative coordinator-corpus review remains required before any later selection; D-07, D-09, and D-11 remain evidence- or construction-blocked with no response requested; the recorded D-01, D-03, D-08, and D-12 directions remain non-normative and are preserved without reopening; the future-only recipient-P2TR direction remains future-only.
- **Explicit exclusions:** no publication; no owner response or new D-item selection; no profile, bundle, clause, serialization, hash, policy, limit, test, vector, or evidence acceptance; no normative change; no edit to the PSBT draft, F3 README, Source Register, Architecture, Requirements, Traceability, Open Decisions, gates, evidence, OD, QK-LIM, or QK-TST documents; no code, parser, serializer, wallet, crypto, signing, key, address, PSBT specimen, QR, SD, card, or target work; no vector, fixture, corpus, or payload generation or execution; no license, SPDX, or manifest license change; no hardware, Flux, procurement, or fabrication work; no role, channel, repository setting, workflow, deployment, or `.replit` change; no third-party text, import, or source change; no host-boundary predicate change besides the exact Decision-Log anchor advance and the single authorized file-set line addition above.
- **Effect:** local packet preparation only; the PSBT draft remains byte-identical, proposed, non-normative, and not accepted; every status remains unchanged; Gates A–E remain OPEN; F5–F12 remain unauthorized; and this chain remains local and unpublished pending a later explicit owner publication instruction after passing independent audits.

### QK-AUTH-F3.2B-RSP-001 — F3.2b PSBT owner-response record preparation authorization

- **ID:** QK-AUTH-F3.2B-RSP-001
- **Date:** 2026-08-19
- **Approver:** Project owner
- **Owner words exactly:** “Approve all four F3.2b directions.”
- **Owner preparation authority exactly:** “Authorize preparation and independent audit of the docs-only F3.2b owner-response record from bb6601f3. No profile acceptance, implementation, vectors, license application, hardware work, settings changes, or publication.”
- **Published parent:** `bb6601f3b97528a72c55622251a4b475680ec21b`.
- **Source packet:** `docs/f3/F3.2B-PSBT-DECISION-PACKET.md` at the published parent.
- **Scope:** exactly four non-enacting policy directions only: `F32B-PSBT-Q-001`/D-02, `F32B-PSBT-Q-002`/D-05, `F32B-PSBT-Q-003`/D-06, and `F32B-PSBT-Q-004`/D-10. Preparation and independent audit are authorized; publication and any profile or clause acceptance are not authorized.
- **Authorized next commits:** Commit B adding exactly `docs/f3/F3.2B-PSBT-OWNER-RESPONSE-RECORD.md` and changing exactly `tools/verify-host-boundary.sh` to reanchor this Decision Log state, add the record to its exact host file set, and add strict record checks; then Commit C changing exactly `tools/verify-current-stage.sh`.
- **Explicit exclusions:** D-00 remains blocked; D-04 remains OWNER-OPEN, with representative coordinator-corpus review required before selection; D-07 remains EVIDENCE/CONSTRUCTION-BLOCKED pending target/human QK-LIM evidence; D-09 remains EVIDENCE/CONSTRUCTION-BLOCKED pending the exact commitment construction; D-11 remains EVIDENCE/CONSTRUCTION-BLOCKED pending the media/write lifecycle; no edit to the packet or PSBT profile draft; no architecture/requirements/OD/QK-LIM/QK-TST/evidence/gate status change; no implementation, dependencies, parser/signing/crypto code, fixtures/vectors, conformance or real-funds use; no finalization/extraction/broadcast authorization; no license application; no hardware/firmware/Flux/procurement/fabrication; no other-branch, remote-branch, branch-setting, tag, release, push, publication, settings, or credential change.
- **Effect:** this record records four non-enacting owner-direction dispositions, but enacts no normative profile or clause and changes no OD, QK-LIM, QK-TST, evidence, gate, implementation, release, remote, or publication status; it authorizes no publication.

### QK-AUTH-F3.2C-D11-PKT-001 — F3.2c D-11 non-binding media/write lifecycle construction-packet preparation authorization

- **ID:** QK-AUTH-F3.2C-D11-PKT-001
- **Date:** 2026-08-19
- **Approver:** Project owner
- **Owner words exactly:** “Authorize preparation and independent audit of the non-binding docs-only F3.2c D-11 media/write lifecycle construction packet from 26e075704cdd172fce62b9b7cd38b4035db384d8, with only the mechanically necessary host-boundary and current-stage verifier bindings. No D-11 selection, profile or clause acceptance, implementation, dependencies, vectors, fixtures, specimens, media I/O or testing, QK-LIM/QK-TST/evidence/gate change, license application, hardware or firmware work, settings or credential changes, or publication.”
- **Published parent:** `26e075704cdd172fce62b9b7cd38b4035db384d8`.
- **Exact commit/path map:** Commit A changes only `docs/DECISION-LOG.md`; Commit B adds only `docs/f3/F3.2C-D11-MEDIA-WRITE-LIFECYCLE-CONSTRUCTION-PACKET.md` and changes only `tools/verify-host-boundary.sh`; Commit C changes only `tools/verify-current-stage.sh`.
- **Least-authority scope:** only local packet preparation and verification, one credential-safe audit-bundle export, and independent read-only audit are authorized. The mechanically necessary verifier bindings do not broaden this authority.
- **Boundaries:** D-11 remains unanswered and EVIDENCE/CONSTRUCTION-BLOCKED; D-09 remains unresolved; the PSBT profile draft remains not accepted; no normative clause, OD, limit, test, evidence, gate, implementation, product, target, release, remote, publication, license, hardware, firmware, setting, credential, tag, or other-branch transition is authorized.
- **Later publication:** publication requires a separate explicit instruction naming the exact audited 40-hex commit.
- **Effect:** this authorization records no D-11 selection, enacts no profile or clause, changes no protected status, and authorizes no publication.

### QK-AUTH-F3.2D-D11-Q001-CLR-001 — F3.2d D-11 Q-001 owner-clarification record preparation authorization

- **ID:** QK-AUTH-F3.2D-D11-Q001-CLR-001
- **Date:** 2026-08-19
- **Approver:** Project owner
- **Owner words exactly (literal two-paragraph block follows):**

Approve the recommended F32C-D11-Q-001 clarification: “Failure releases no artifact or partial output” does not claim that already observed QR frames or SD residue can be withdrawn or proven absent. Before route output begins, failure releases nothing; after output begins, a later failure stops further output, permits no complete-artifact, delivery, receipt, finalization, broadcast, atomicity, or durability claim, and permits no automatic fallback, retry, or re-signing.

Authorize preparation and independent audit of a non-enacting docs-only F3.2d Q-001 owner-clarification record from e4cdf7771e189fc0f729358334aafd35177048c6, with only the mechanically necessary verifier bindings. No D-11 selection; no answer to Q-002 through Q-012; no profile acceptance, D-09 or QK-LIM selection, implementation, testing, media I/O, hardware work, settings changes, or publication.

- **Published parent:** `e4cdf7771e189fc0f729358334aafd35177048c6`.
- **Source packet:** `docs/f3/F3.2C-D11-MEDIA-WRITE-LIFECYCLE-CONSTRUCTION-PACKET.md` at the published parent.
- **Exact commit/path map:** Commit A changes only `docs/DECISION-LOG.md`; Commit B adds only `docs/f3/F3.2D-D11-Q001-OWNER-CLARIFICATION-RECORD.md` and changes only `tools/verify-host-boundary.sh`; Commit C changes only `tools/verify-current-stage.sh`.
- **Least-authority scope:** only local preparation and verification of the non-enacting docs-only F3.2d Q-001 owner-clarification record, the mechanically necessary verifier bindings, and independent read-only audit are authorized.
- **Explicit exclusions:** no D-11 selection, closure, construction, state, route, route-set, filesystem, filename, attachment, retry, orphan, QR-preflight, or signature-order selection; no response to F32C-D11-Q-002 through F32C-D11-Q-012; no profile or clause acceptance; no D-09 field, serialization, hash, or domain selection; no QK-LIM selection; no OD, QK-TST, evidence, gate, STOP-SHIP, implementation, dependency, vector, fixture, specimen, test, media-I/O, license, hardware, firmware, setting, credential, remote, tag, release, publication, or other-branch change.
- **Effect:** this authorization records preparation authority for one non-enacting clarification record only; it does not close Q-001 or D-11, select any construction or policy, enact any profile or clause, alter any protected status, authenticate or audit its own output, or authorize publication.
- **Later publication:** publication requires a separate explicit owner instruction naming the exact independently audited 40-hex commit.

### QK-AUTH-F3.2E-PSBT-TST-ALIGN-PKT-001 — F3.2e PSBT-output TEST-ARCHITECTURE alignment-packet preparation authorization

- **ID:** QK-AUTH-F3.2E-PSBT-TST-ALIGN-PKT-001
- **Date:** 2026-08-20
- **Approver:** Project owner
- **Owner words exactly:**

Authorize preparation and independent audit of a non-binding docs-only F3.2e PSBT-output TEST-ARCHITECTURE alignment packet from be4d019e349e15ca575ac64b64f900957283e5e0, with only the mechanically necessary verifier bindings. Limit scope to QK-TST-DIFF-002 and QK-TST-DIFF-004, with QK-TST-REH-004 retained unchanged as the external-finalization control. No canonical TEST-ARCHITECTURE edit, test-status change, Q-002 through Q-012 answer, D-11 or D-09 selection, profile acceptance, implementation, testing, vectors, media I/O, hardware work, settings changes, or publication.

- **Published parent:** `be4d019e349e15ca575ac64b64f900957283e5e0`.
- **Scope:** local preparation and independent read-only audit of one non-binding docs-only F3.2e alignment packet limited to proposed, unselected plan/oracle correction text for `QK-TST-DIFF-002` and `QK-TST-DIFF-004`, with `QK-TST-REH-004` reproduced byte-for-byte as the unchanged external-finalization control, plus only the mechanically necessary verifier bindings and one clean complete sole-main audit bundle in `/tmp`.
- **Exact commit/path map:** Commit A changes only `docs/DECISION-LOG.md`; Commit B adds only `docs/f3/F3.2E-PSBT-OUTPUT-TEST-ALIGNMENT-PACKET.md` and changes only `tools/verify-host-boundary.sh`; Commit C changes only `tools/verify-current-stage.sh`. The chain is exactly three linear one-parent commits with no merge and cumulative changes to only those four paths.
- **Explicit exclusions:** no canonical `docs/TEST-ARCHITECTURE.md`, `docs/REQUIREMENTS.md`, `docs/TRACEABILITY.md`, `docs/RESOURCE-BUDGETS.md`, prior F3.2b/c/d record or packet, PSBT profile, `docs/OPEN-DECISIONS.md`, `ARCHITECTURE.md`, `docs/MATURITY-GATES.md`, `docs/SOURCE-REGISTER.md`, `docs/f3/README.md`, `README.md`, `.replit`, code, manifest, dependency, license, hardware, firmware, remote, branch, tag, release, setting, credential, or publication change; no Q-002 through Q-012 answer; no D-09 or D-11 selection; no QK-LIM value or link change; no profile or clause acceptance; no implementation, testing, vector, fixture, specimen, corpus, payload, media I/O, evidence, gate, or STOP-SHIP change.
- **Non-effects:** the packet is alignment input only and selects or enacts no correction. `QK-TST-DIFF-002` and `QK-TST-DIFF-004` remain `PLANNED — NOT RUN`; `QK-TST-REH-004` remains unchanged; every QK-TST status remains unchanged; D-09, D-11, Q-002 through Q-012, every QK-LIM, profile acceptance, tests, evidence, gates, and STOP-SHIP remain open or unchanged exactly as before. Verifier results and independent review are supporting evidence only, never authentication, audit proof, correction selection, canonical amendment, or publication authority.
- **Later publication:** publication requires a separate explicit owner instruction naming the exact independently audited 40-hex commit; this authorization alone does not authorize publication.

### QK-AUTH-F3.2F-PSBT-TST-DIR-REC-001 — F3.2f PSBT-output test owner-direction record preparation authorization

- **ID:** QK-AUTH-F3.2F-PSBT-TST-DIR-REC-001
- **Date:** 2026-08-20
- **Approver:** Project owner
- **Owner approval words exactly:**

Approve, as non-enacting owner directions, the complete proposed Plan / oracle text for QK-TST-DIFF-002 and QK-TST-DIFF-004 exactly as printed in the F3.2e packet at published commit 574e790193d45d3f392c64951d319bb5fde11d20. Approve no change to any other cell. QK-TST-REH-004 remains unchanged.

This approval does not amend docs/TEST-ARCHITECTURE.md, change any QK-TST status, authorize testing or evidence, answer F32C-D11-Q-002 through Q-012, or select D-11, D-09, any QK-LIM, the PSBT profile, or any clause.

- **Owner preparation authority words exactly:**

Authorize preparation and independent audit of a non-enacting docs-only F3.2f PSBT-output test owner-direction record from 574e790193d45d3f392c64951d319bb5fde11d20, recording only my approved directions for QK-TST-DIFF-002 and QK-TST-DIFF-004, with QK-TST-REH-004 retained unchanged and only the mechanically necessary verifier bindings.

No canonical TEST-ARCHITECTURE edit, test-status change, F32C-D11-Q-002 through Q-012 answer, D-11 or D-09 selection, QK-LIM selection, profile or clause acceptance, implementation, testing, vectors, evidence, media I/O, hardware work, settings changes, or publication.

- **Published parent and source locators:** `574e790193d45d3f392c64951d319bb5fde11d20`; `docs/f3/F3.2E-PSBT-OUTPUT-TEST-ALIGNMENT-PACKET.md` blob `4ffa20afbedeb0b6cfbbe57298f941bc0537683e`; `docs/TEST-ARCHITECTURE.md` blob `f4e127a92fc68243274b7d384e335fa4632e5dd2`. These are supporting byte-identity audit locators only, not authentication or proof.
- **Exact commit/path map:** Commit A changes only `docs/DECISION-LOG.md`; Commit B adds only `docs/f3/F3.2F-PSBT-OUTPUT-TEST-OWNER-DIRECTION-RECORD.md` and changes only `tools/verify-host-boundary.sh`; Commit C changes only `tools/verify-current-stage.sh`. The chain is exactly three linear one-parent commits with no merge and cumulative changes to only those four paths.
- **Scope:** records only the two non-enacting owner directions attached to the complete proposed Plan / oracle cells for `QK-TST-DIFF-002` and `QK-TST-DIFF-004` as printed in the F3.2e source packet. `QK-TST-REH-004` remains the unchanged external-finalization control and receives no direction.
- **Explicit exclusions and non-effects:** no canonical document or other cell change; no QK-TST status, test, vector, evidence, gate, STOP-SHIP, implementation, media-I/O, hardware, firmware, setting, credential, remote, branch, tag, release, or publication change; no answer to F32C-D11-Q-002 through F32C-D11-Q-012; no D-11, D-09, QK-LIM, profile, or clause selection or acceptance. Every dependency, open question, QK-LIM title/link, and existing `PLANNED — NOT RUN` status remains unchanged.
- **Later canonical amendment:** this record is non-enacting. Canonical amendment requires a separate explicit owner authorization after this direction record; this authorization does not amend `docs/TEST-ARCHITECTURE.md`.
- **Later publication:** publication requires a separate explicit owner instruction naming the exact independently audited 40-hex commit; this authorization alone does not authorize publication.

### QK-AUTH-F3.2G-PSBT-TST-ARCH-AMD-001 — F3.2g canonical TEST-ARCHITECTURE amendment authorization

- **ID:** QK-AUTH-F3.2G-PSBT-TST-ARCH-AMD-001
- **Date:** 2026-08-20
- **Approver:** Project owner
- **Owner words exactly:**

Authorize preparation and independent audit of a docs-only F3.2g canonical TEST-ARCHITECTURE amendment from 45f2b362994b785d303e91b8e530efc724dc2d81, replacing only the complete Plan / oracle cells of QK-TST-DIFF-002 and QK-TST-DIFF-004 with the owner-approved text recorded in the published F3.2f owner-direction record, byte-for-byte. Preserve every other cell and row unchanged, retain QK-TST-REH-004 byte-for-byte, and include only the mechanically necessary Decision Log and verifier bindings.

No QK-TST status change or testing/evidence; no F32C-D11-Q-002 through Q-012 answer; no D-11, D-09, QK-LIM, profile, clause, OD-05/06, dependency, corpus, implementation, vector, fixture, media-I/O, hardware, license, settings, credential, release, or publication change.

- **Published parent and source locators:** `45f2b362994b785d303e91b8e530efc724dc2d81`; published F3.2f owner-direction record blob `a066fc9710371f414d158a3deb9c345f2b00821b`; F3.2e alignment-packet blob `4ffa20afbedeb0b6cfbbe57298f941bc0537683e`; canonical `docs/TEST-ARCHITECTURE.md` base blob `f4e127a92fc68243274b7d384e335fa4632e5dd2`. These hashes are supporting byte-identity audit locators only, not authentication or proof.
- **Exact commit/path map:** Commit A changes only `docs/DECISION-LOG.md`; Commit B changes only `docs/TEST-ARCHITECTURE.md` and `tools/verify-host-boundary.sh`; Commit C changes only `tools/verify-current-stage.sh`. The chain is exactly three linear one-parent commits with no merge and cumulative changes to only those four paths; documentation modes remain `100644` and verifier modes remain `100755`.
- **Least-authority scope:** locally enact exactly the two owner-approved complete Plan / oracle cells for `QK-TST-DIFF-002` and `QK-TST-DIFF-004`, byte-for-byte from the published F3.2f record, plus only the mechanically necessary Decision Log and verifier bindings and independent read-only audit.
- **Preserved cells, rows, and sources:** each target row's ID, Method, Links, Milestone, Gate, Evidence artifact, and Status cells remain byte-for-byte unchanged; every other `docs/TEST-ARCHITECTURE.md` byte and row remains unchanged; `QK-TST-REH-004` remains byte-for-byte unchanged; the published F3.2e packet and F3.2f record remain unchanged.
- **Explicit exclusions and non-effects:** no QK-TST status, testing, evidence, gate, STOP-SHIP, F32C-D11-Q-002 through F32C-D11-Q-012 answer, D-11 or D-09 selection, QK-LIM value/title/link, profile or clause acceptance, OD-05/06 resolution, dependency, corpus, implementation, vector, fixture, media-I/O, hardware, license, setting, credential, remote, other branch, tag, release, or publication change. The known `QK-LIM-PSBT-027` title/link inconsistency remains untouched. All QK-TST statuses remain `PLANNED — NOT RUN`.
- **Effect:** this authorization canonically enacts exactly the two approved complete Plan / oracle cells and nothing else. It does not authenticate or audit its own output, establish test evidence, or authorize publication.
- **Later publication:** publication requires a separate explicit owner instruction naming the exact independently audited 40-hex commit; this authorization alone does not authorize publication.

### QK-AUTH-F3.2H-QK-LIM-PSBT-027-ALIGN-PKT-001 — F3.2h QK-LIM-PSBT-027 cross-document alignment-packet preparation authorization

- **ID:** QK-AUTH-F3.2H-QK-LIM-PSBT-027-ALIGN-PKT-001
- **Date:** 2026-08-20
- **Approver:** Project owner
- **Owner words exactly (literal three-paragraph block follows):**

Authorize preparation and independent audit of a non-binding docs-only F3.2h QK-LIM-PSBT-027 cross-document alignment packet from a64399dd35aef8d6daa05171f1da30c8026f6d1f, with only the mechanically necessary Decision Log and verifier bindings. Limit scope to the stale “Raw final-transaction output bytes” dimension and its live links in docs/RESOURCE-BUDGETS.md, docs/REQUIREMENTS.md, and the QK-TST-DIFF-004 Links cell of docs/TEST-ARCHITECTURE.md.

Present exactly three unselected dispositions: recommended permanent tombstoning, unlinking, and non-reuse of ID 027; preservation as an inactive future contingency with no current links or value; or deferral with the known inconsistency retained. Reject deletion, renumbering, reuse, or repurposing of ID 027. Keep QK-LIM-PSBT-026 and all historical records unchanged.

No owner selection or canonical edit; no QK-LIM value, title, status, link, or row change; no QK-TST Plan / oracle or status change; no answer to F32C-D11-Q-002 through Q-012; no D-11, D-09, OD-05/06, profile, clause, implementation, testing, corpus, vector, fixture, evidence, media-I/O, hardware, license, gate, STOP-SHIP, setting, credential, release, publication, or remote change.

- **Published parent and source locators:** `a64399dd35aef8d6daa05171f1da30c8026f6d1f`; canonical `docs/RESOURCE-BUDGETS.md` blob `1ae6494fc24a8e3572464cf45e6d43ea4875e95d`; canonical `docs/REQUIREMENTS.md` blob `981b1b497102187da66fb4e82ef0725b32c088f7`; canonical `docs/TEST-ARCHITECTURE.md` blob `065e20eed4e916c907a244aa3e5ca2e66cbc5d4e`. These hashes are supporting byte-identity audit locators only, not authentication or proof.
- **Exact commit/path map:** Commit A appends only `docs/DECISION-LOG.md`; Commit B adds only `docs/f3/F3.2H-QK-LIM-PSBT-027-CROSS-DOCUMENT-ALIGNMENT-PACKET.md` and changes only `tools/verify-host-boundary.sh`; Commit C changes only `tools/verify-current-stage.sh`. The chain is exactly three linear one-parent commits with no merge and cumulative changes to only those four paths; documentation modes remain `100644` and verifier modes remain `100755`.
- **Least-authority scope:** local preparation and independent read-only audit of one non-binding, non-normative packet that inventories the current `QK-LIM-PSBT-027` cross-document inconsistency and presents exactly three ordered, unselected dispositions. A recommendation is analysis only and is not an owner selection.
- **Preserved sources and state:** `QK-LIM-PSBT-026`, `QK-LIM-PSBT-027`, `QK-REQ-PSBT-005`, `QK-REQ-TRN-007`, `QK-TST-DIFF-004`, all historical records, and every other canonical byte remain unchanged. The current registry vocabulary is not expanded by this packet.
- **Explicit exclusions and non-effects:** no owner response or disposition selection; no deletion, renumbering, reuse, or repurposing of ID 027; no numeric QK-LIM value, including zero; no QK-LIM value, title, status, link, or row change; no QK-TST Plan / oracle, Links, or status change; no answer to F32C-D11-Q-002 through F32C-D11-Q-012; no D-11, D-09, OD-05/06, profile, clause, implementation, test, corpus, vector, fixture, evidence, media-I/O, hardware, license, gate, STOP-SHIP, setting, credential, remote, other-branch, tag, release, or publication change.
- **Effect:** packet-preparation authority only. `QK-LIM-PSBT-026` and `QK-LIM-PSBT-027` remain byte-for-byte unchanged and `OPEN — VALUE NOT AUTHORIZED IN F1`; all QK-TST statuses remain `PLANNED — NOT RUN`; the known inconsistency remains live unless and until a later owner-selected disposition receives separate canonical-amendment authority.
- **Later canonical amendment and publication:** any disposition selection, canonical edit, or publication requires a separate explicit owner instruction after independent audit; this authorization alone authorizes none of them.

### QK-AUTH-F3.2I-QK-LIM-PSBT-027-DIR-REC-001 — F3.2i QK-LIM-PSBT-027 owner-direction record preparation authorization

- **ID:** QK-AUTH-F3.2I-QK-LIM-PSBT-027-DIR-REC-001
- **Date:** 2026-08-20
- **Approver:** Project owner
- **Owner words exactly (literal four-paragraph block follows):**

Approve, as a non-enacting owner direction, F32H-LIM-OPT-001 exactly as printed in the published F3.2h packet at commit e36b41e5cd55264b4b745550c96c74968b06eae7: “Under separately authorized later canonical work, preserve ID 027 as a permanent non-reusable tombstone and remove its live links. Never delete, renumber, reuse, or repurpose the ID.” Approve no other disposition.

This approval selects only the future disposition direction. It does not declare the current QK-LIM-PSBT-027 row tombstoned or inactive; select any replacement title, Status vocabulary, row syntax, link syntax, or canonical bytes; authorize any value, including zero; or amend any current live link. QK-LIM-PSBT-026 remains unchanged.

Authorize preparation and independent audit of a non-enacting docs-only F3.2i QK-LIM-PSBT-027 owner-direction record from e36b41e5cd55264b4b745550c96c74968b06eae7, recording only that approved direction, with only the mechanically necessary Decision Log and verifier bindings.

No canonical RESOURCE-BUDGETS, REQUIREMENTS, or TEST-ARCHITECTURE edit; no QK-LIM value, title, status, link, or row change; no QK-TST Plan / oracle, Links, or status change; no answer to F32C-D11-Q-002 through Q-012; no D-11, D-09, OD-05/06, profile, clause, implementation, testing, corpus, vector, fixture, evidence, media-I/O, hardware, license, gate, STOP-SHIP, setting, credential, other-branch, tag, release, publication, or remote change.

- **Published parent and source locators:** `e36b41e5cd55264b4b745550c96c74968b06eae7`; F3.2h packet `docs/f3/F3.2H-QK-LIM-PSBT-027-CROSS-DOCUMENT-ALIGNMENT-PACKET.md` blob `e18ba6b4a5c2ee5fc5b7d1a464b656c631b526d5`; canonical `docs/RESOURCE-BUDGETS.md` blob `1ae6494fc24a8e3572464cf45e6d43ea4875e95d`; canonical `docs/REQUIREMENTS.md` blob `981b1b497102187da66fb4e82ef0725b32c088f7`; canonical `docs/TEST-ARCHITECTURE.md` blob `065e20eed4e916c907a244aa3e5ca2e66cbc5d4e`. These hashes are supporting byte-identity audit locators only, not authentication or proof.
- **Exact commit/path map:** Commit A appends only `docs/DECISION-LOG.md`; Commit B adds only `docs/f3/F3.2I-QK-LIM-PSBT-027-OWNER-DIRECTION-RECORD.md` and changes only `tools/verify-host-boundary.sh`; Commit C changes only `tools/verify-current-stage.sh`. The chain is exactly three linear one-parent commits with no merge and cumulative changes to only those four paths; documentation modes remain `100644` and verifier modes remain `100755`.
- **Least-authority scope:** local preparation and independent read-only audit of one non-enacting owner-direction record. It records only the selected future direction for `F32H-LIM-OPT-001`; it does not enact a canonical amendment or change the packet’s historical all-UNSELECTED state.
- **Preserved sources and state:** `QK-LIM-PSBT-026`, `QK-LIM-PSBT-027`, `QK-REQ-PSBT-005`, `QK-REQ-TRN-007`, `QK-TST-DIFF-004`, the F3.2h packet, all historical records, and every other canonical byte remain unchanged. No current `TOMBSTONED` or `INACTIVE` status is recorded or introduced.
- **Explicit exclusions and non-effects:** no present tombstone or inactive declaration; no replacement title, Status vocabulary, row syntax, link syntax, canonical bytes, value including zero, or live-link change; no deletion, renumbering, reuse, or repurposing of ID 027; no QK-TST Plan / oracle, Links, or status change; no answer to F32C-D11-Q-002 through F32C-D11-Q-012; no D-11, D-09, OD-05/06, profile, clause, implementation, test, corpus, vector, fixture, evidence, media-I/O, hardware, license, gate, STOP-SHIP, setting, credential, remote, other-branch, tag, release, or publication change.
- **Effect:** records F32H-LIM-OPT-001 as the sole owner-approved non-enacting future direction and authorizes preparation and independent audit of its record only. F32H-LIM-OPT-002 and F32H-LIM-OPT-003 remain not approved. No canonical change is enacted; later exact construction, canonical work, and publication each require a separate explicit owner instruction after independent audit.

### QK-AUTH-F3.2J-QK-LIM-PSBT-027-CONSTRUCT-001 — F3.2j QK-LIM-PSBT-027 exact tombstone-construction packet preparation authorization

- **ID:** QK-AUTH-F3.2J-QK-LIM-PSBT-027-CONSTRUCT-001
- **Date:** 2026-08-20
- **Approver:** Project owner
- **Owner words exactly (literal three-paragraph block follows):**

Authorize preparation and independent audit of a non-binding, non-enacting docs-only F3.2j QK-LIM-PSBT-027 exact tombstone-construction packet from published commit 878b23be4690bb778a615bfe1a6ef108e7a94008, with only the mechanically necessary Decision Log and verifier bindings.

Limit the packet to exactly one recommended, byte-complete candidate, F32J-LIM-CON-001, explicitly PROPOSED — UNSELECTED. Print the exact current and proposed canonical bytes for: the minimum QK-LIM-PSBT-027-specific RESOURCE-BUDGETS preamble, invariant, and append-only-ID-note corrections needed for a permanent no-value tombstone; every cell of row 027; removal of 027 from the QK-REQ-PSBT-005 and QK-REQ-TRN-007 Lim cells; and removal of 027 from the QK-TST-DIFF-004 Links cell. Preserve the historical dimension name and ID, make every former row relationship non-operative, define any proposed Value-cell text as a non-value sentinel rather than zero or an authorized QK-LIM value, and never delete, renumber, reuse, repurpose, or reopen ID 027. Introduce no general tombstone transition for another row. Keep QK-LIM-PSBT-026 and every other canonical and historical byte unchanged.

No owner selection of F32J-LIM-CON-001 and no canonical edit; no current tombstone or inactive declaration; no actual QK-LIM value, title, status, link, row, vocabulary, or syntax change; no QK-TST Plan / oracle, Links, or status change; no answer to F32C-D11-Q-002 through Q-012; no D-11, D-09, OD-05/06, profile, clause, dependency, implementation, testing, corpus, vector, fixture, evidence, media-I/O, hardware, license, gate, STOP-SHIP, setting, credential, other-branch, tag, release, publication, or remote change.

- **Published parent and source locators:** `878b23be4690bb778a615bfe1a6ef108e7a94008`; F3.2i record `docs/f3/F3.2I-QK-LIM-PSBT-027-OWNER-DIRECTION-RECORD.md` blob `2f4bc0f5cb8e861b45f515fb7c4c5dbc764784c5`; canonical `docs/RESOURCE-BUDGETS.md` blob `1ae6494fc24a8e3572464cf45e6d43ea4875e95d`; canonical `docs/REQUIREMENTS.md` blob `981b1b497102187da66fb4e82ef0725b32c088f7`; canonical `docs/TEST-ARCHITECTURE.md` blob `065e20eed4e916c907a244aa3e5ca2e66cbc5d4e`. These hashes are supporting byte-identity audit locators only, not authentication or proof.
- **Exact commit/path map:** Commit A appends only `docs/DECISION-LOG.md`; Commit B adds only `docs/f3/F3.2J-QK-LIM-PSBT-027-EXACT-TOMBSTONE-CONSTRUCTION-PACKET.md` and changes only `tools/verify-host-boundary.sh`; Commit C changes only `tools/verify-current-stage.sh`. The chain is exactly three linear one-parent bodyless commits with no merge and cumulative changes to only those four paths; documentation modes remain `100644` and verifier modes remain `100755`.
- **Least-authority scope:** local preparation and independent audit of one non-binding, non-enacting packet containing exactly one recommended, byte-complete, proposed-but-unselected future construction. Recommendation is not selection; the packet neither enacts nor publishes any canonical change.
- **Preserved current and historical state:** `QK-LIM-PSBT-026`, `QK-LIM-PSBT-027`, `QK-REQ-PSBT-005`, `QK-REQ-TRN-007`, `QK-TST-DIFF-004`, every canonical source, the F3.2i record, and every historical byte remain unchanged. No current tombstone, inactive state, value, title, status, link, row, vocabulary, or syntax is introduced.
- **Candidate boundary:** `F32J-LIM-CON-001` is the sole candidate and remains `PROPOSED — UNSELECTED`. It may print only the exact future bytes needed for the permanent, non-reusable, no-value tombstone construction and the three specified consumer-link removals. Any proposed Value text is a non-value sentinel, never zero or an authorized QK-LIM value; no general tombstone transition is created for another row.
- **Explicit exclusions and non-effects:** no owner candidate selection or canonical edit; no deletion, renumbering, reuse, repurposing, or reopening of ID 027; no actual QK-LIM value, title, status, link, row, vocabulary, or syntax change; no QK-TST Plan / oracle, Links, or status change; no answer to F32C-D11-Q-002 through F32C-D11-Q-012; no D-11, D-09, OD-05/06, profile, clause, dependency, implementation, test, corpus, vector, fixture, evidence, media-I/O, hardware, license, gate, STOP-SHIP, setting, credential, remote, other-branch, tag, release, or publication change.
- **Effect:** packet-preparation and independent-audit authority only. Canonical enactment, candidate selection, and publication each require separate later explicit owner authority after independent audit.

### QK-AUTH-F3.2K-QK-LIM-PSBT-027-EXACT-CON-DIR-REC-001 — F3.2k QK-LIM-PSBT-027 exact-construction owner-direction record preparation authorization

- **ID:** QK-AUTH-F3.2K-QK-LIM-PSBT-027-EXACT-CON-DIR-REC-001
- **Date:** 2026-08-20
- **Approver:** Project owner
- **Owner words exactly (literal four-paragraph block follows):**

Approve, as a non-enacting owner direction for separately authorized later canonical work, the complete, indivisible, byte-for-byte candidate F32J-LIM-CON-001 exactly as printed in the published F3.2j packet at commit 0d2115de9c322bf221f5a5008d91e7b7b48363e6, packet blob b1c2153f5f89a0e1d44d62a09921251e225c0d87. Approve all and only its proposed future canonical bytes. Approve no alternative, subset, paraphrase, normalization, substitution, or additional change.

This approval selects only the exact future construction. It does not enact those bytes, amend any canonical document, or declare QK-LIM-PSBT-027 currently tombstoned or inactive. The current row remains OPEN with its current live links until separately authorized canonical work. The proposed Value-cell text remains a non-value sentinel, never zero or an authorized QK-LIM value. Preserve the historical ID and dimension name, QK-LIM-PSBT-026, ALL GATES OPEN, and every other canonical and historical byte. OD-05 and QK-THR-016 remain unresolved or unchanged globally; only their future row-027 relationships become non-operative. Introduce no general tombstone vocabulary or transition for another row.

Authorize preparation and independent audit of a non-enacting docs-only F3.2k QK-LIM-PSBT-027 exact-construction owner-direction record from published commit 0d2115de9c322bf221f5a5008d91e7b7b48363e6, recording only this exact approval and reproducing the selected future bytes byte-for-byte, with only the mechanically necessary Decision Log and verifier bindings.

No canonical RESOURCE-BUDGETS, REQUIREMENTS, or TEST-ARCHITECTURE edit; no current QK-LIM value, title, Status, link, row, vocabulary, or syntax change; no current QK-TST Plan / oracle, Links, or status change; no answer to F32C-D11-Q-002 through Q-012; no D-11, D-09, OD-05/06, profile, clause, dependency, implementation, testing, corpus, vector, fixture, evidence, media-I/O, hardware, license, gate, STOP-SHIP, setting, credential, other-branch, tag, release, publication, or remote change.

- **Published parent and source locators:** `0d2115de9c322bf221f5a5008d91e7b7b48363e6`; immutable F3.2j packet `docs/f3/F3.2J-QK-LIM-PSBT-027-EXACT-TOMBSTONE-CONSTRUCTION-PACKET.md` blob `b1c2153f5f89a0e1d44d62a09921251e225c0d87`; F3.2i record blob `2f4bc0f5cb8e861b45f515fb7c4c5dbc764784c5`; canonical RESOURCE-BUDGETS blob `1ae6494fc24a8e3572464cf45e6d43ea4875e95d`; canonical REQUIREMENTS blob `981b1b497102187da66fb4e82ef0725b32c088f7`; canonical TEST-ARCHITECTURE blob `065e20eed4e916c907a244aa3e5ca2e66cbc5d4e`. These hashes are supporting byte-identity audit locators only, not authentication, audit proof, enactment, or publication authority.
- **Exact commit/path map:** Commit A appends only `docs/DECISION-LOG.md`; Commit B adds only `docs/f3/F3.2K-QK-LIM-PSBT-027-EXACT-CONSTRUCTION-OWNER-DIRECTION-RECORD.md` and changes only `tools/verify-host-boundary.sh`; Commit C changes only `tools/verify-current-stage.sh`. The chain is exactly three linear single-parent bodyless commits with no merge or fourth commit and cumulative changes to only those four paths; documentation modes remain `100644` and verifier modes remain `100755`.
- **Least-authority scope:** local preparation, verification, and independent read-only audit of one non-enacting owner-direction record selecting all and only the complete, indivisible, byte-for-byte `F32J-LIM-CON-001` future construction. The mechanically necessary verifier bindings do not broaden this authority.
- **Historical and current disposition:** the immutable F3.2j packet remains `PROPOSED — UNSELECTED` as its historical source state. This later record marks only the complete exact future construction `OWNER DIRECTION APPROVED — NON-ENACTING`, with present canonical effect `NONE`.
- **Exact payload boundary:** approval attaches only to the seven proposed future payloads printed in the packet: the RESOURCE-BUDGETS preamble, invariant, append-only note, complete nine-cell row 027, two Requirement Lim cells, and QK-TST-DIFF-004 Links cell. They are one indivisible construction; no alternative, subset, paraphrase, normalization, substitution, explanatory packet prose, or additional change is approved.
- **Preserved current and historical state:** the packet, F3.2i record, `QK-LIM-PSBT-026`, current `QK-LIM-PSBT-027` OPEN row and live links, canonical RESOURCE-BUDGETS, REQUIREMENTS, and TEST-ARCHITECTURE sources, `ALL GATES OPEN`, and every other canonical and historical byte remain unchanged. The packet’s `LOCAL AND UNPUBLISHED` wording remains historical and unchanged.
- **Mandatory semantic boundary:** the proposed Value text is exactly `NOT A VALUE — PERMANENT TOMBSTONE SENTINEL`, never zero, a value, default, candidate, or approved limit. Future `TOMBSTONED` syntax is row-027-specific and creates no current or general vocabulary or transition. ID and historical dimension are preserved; ID 027 is never deleted, renumbered, reused, repurposed, or reopened. OD-05 and QK-THR-016 remain globally unresolved or unchanged; only their future row-027 relationships become non-operative. The only future consumer changes are the two named Requirement Lim cells and the DIFF-004 Links cell.
- **Explicit exclusions and non-effects:** no canonical edit; no current tombstone or inactive declaration; no current QK-LIM value, title, Status, link, row, vocabulary, or syntax change; no current QK-TST Plan / oracle, Links, or status change; no answer to F32C-D11-Q-002 through Q-012; no D-11, D-09, OD-05/06, profile, clause, dependency, implementation, testing, corpus, vector, fixture, evidence, media-I/O, hardware, license, gate, STOP-SHIP, setting, credential, remote, other-branch, tag, release, or publication change.
- **Effect:** records one exact owner-approved non-enacting future construction only and authorizes local record preparation and independent audit. It enacts no canonical byte and grants no publication or remote authority.
- **Later canonical amendment and publication:** canonical work and publication each require a separate explicit owner instruction naming the exact independently audited 40-hex commit.

### QK-AUTH-F3.2L-QK-LIM-PSBT-027-CANONICAL-AMEND-001 — F3.2l QK-LIM-PSBT-027 canonical permanent-tombstone amendment authorization

- **ID:** QK-AUTH-F3.2L-QK-LIM-PSBT-027-CANONICAL-AMEND-001
- **Date:** 2026-08-21
- **Approver:** Project owner
- **Owner words exactly (literal three-paragraph block follows):**

Authorize preparation and independent audit of a docs-only F3.2l canonical QK-LIM-PSBT-027 permanent-tombstone amendment from published commit 49ca0aa4a19ca10ebe8f2866b2d9426720b8ebdf. Enact, as one indivisible byte-for-byte construction, all and only the seven owner-approved future canonical payloads for F32J-LIM-CON-001 reproduced in the published F3.2k owner-direction record at blob 5b05d62776c65bfaf5ec41ab39108bc0531b7944 and sourced from the immutable F3.2j packet at blob b1c2153f5f89a0e1d44d62a09921251e225c0d87. Stop on any missing, duplicated, or mismatched source span; permit no alternative, subset, paraphrase, normalization, substitution, reformatting, or additional byte.

Replace exactly once: in docs/RESOURCE-BUDGETS.md, only the approved preamble, invariant, append-only-ID note, and complete QK-LIM-PSBT-027 row; in docs/REQUIREMENTS.md, only the Lim cells of QK-REQ-PSBT-005 and QK-REQ-TRN-007; and in docs/TEST-ARCHITECTURE.md, only the Links cell of QK-TST-DIFF-004. This enactment makes QK-LIM-PSBT-027 the sole permanent, non-reusable canonical tombstone. Preserve its historical ID and dimension name, QK-LIM-PSBT-026, ALL GATES OPEN, and every other canonical and historical byte. Its Value text is a non-value sentinel, never zero or an authorized QK-LIM value. Never delete, renumber, reuse, repurpose, or reopen ID 027. Introduce no general tombstone vocabulary or transition for another row. OD-05 remains unresolved globally and QK-THR-016 remains unchanged globally; only their row-027 relationships become non-operative.

Use exactly three linear single-parent commits: Commit A appends only docs/DECISION-LOG.md; Commit B changes only docs/RESOURCE-BUDGETS.md, docs/REQUIREMENTS.md, docs/TEST-ARCHITECTURE.md, and tools/verify-host-boundary.sh; Commit C changes only tools/verify-current-stage.sh. Include only the mechanically necessary Decision Log and verifier bindings. No other RESOURCE-BUDGETS row or byte, Requirement text or cell, or QK-TST Plan / oracle, Milestone, Gate, Evidence artifact, Status, or other cell change. No answer to F32C-D11-Q-002 through Q-012; no D-11, D-09, OD-05/06, other QK-LIM value, profile, clause, or dependency selection; no implementation, testing, corpus, vector, fixture, evidence, media-I/O, hardware, license, gate, STOP-SHIP, setting, credential, other-branch, tag, release, publication, remote, or other change. Stop locally after independent audit; publication requires separate exact-commit authority.

- **Published parent and source locators:** `49ca0aa4a19ca10ebe8f2866b2d9426720b8ebdf`; immutable F3.2k owner-direction record `docs/f3/F3.2K-QK-LIM-PSBT-027-EXACT-CONSTRUCTION-OWNER-DIRECTION-RECORD.md` blob `5b05d62776c65bfaf5ec41ab39108bc0531b7944`; immutable F3.2j packet `docs/f3/F3.2J-QK-LIM-PSBT-027-EXACT-TOMBSTONE-CONSTRUCTION-PACKET.md` blob `b1c2153f5f89a0e1d44d62a09921251e225c0d87`; canonical RESOURCE-BUDGETS source blob `1ae6494fc24a8e3572464cf45e6d43ea4875e95d`; canonical REQUIREMENTS source blob `981b1b497102187da66fb4e82ef0725b32c088f7`; canonical TEST-ARCHITECTURE source blob `065e20eed4e916c907a244aa3e5ca2e66cbc5d4e`. These hashes are supporting byte-identity audit locators only, not authentication, audit proof, or publication authority.
- **Expected post-enactment byte identities:** canonical RESOURCE-BUDGETS blob `09815b0a75c26a118f80c5cec008e695d319900b`; canonical REQUIREMENTS blob `2cf8a34aa5621dcea114e2d5ec3dabc2b79aef84`; canonical TEST-ARCHITECTURE blob `ecb3867ca2842f6d011288d3dcd94d2b1997e516`. Independent deterministic reconstruction from the seven approved payloads must produce all three identities before enactment.
- **Exact commit/path map:** Commit A appends only `docs/DECISION-LOG.md`; Commit B changes only `docs/RESOURCE-BUDGETS.md`, `docs/REQUIREMENTS.md`, `docs/TEST-ARCHITECTURE.md`, and `tools/verify-host-boundary.sh`; Commit C changes only `tools/verify-current-stage.sh`. The chain is exactly three linear single-parent bodyless commits with no merge or fourth commit and cumulative changes to only those six paths; documentation modes remain `100644` and verifier modes remain `100755`.
- **Least-authority scope:** local canonical enactment, verification, and independent read-only audit of all and only the seven complete, indivisible, byte-for-byte `F32J-LIM-CON-001` payloads. The mechanically necessary Decision Log and verifier bindings do not broaden this authority; no remote or publication authority is granted.
- **Historical source disposition:** the immutable F3.2j packet remains `PROPOSED — UNSELECTED` as its historical source state, and the immutable F3.2k record remains `OWNER DIRECTION APPROVED — NON-ENACTING` as its historical selection state. Their former `LOCAL AND UNPUBLISHED` text remains byte-identical historical wording; both historical stages are now published.
- **Exact payload boundary:** enactment attaches only to the RESOURCE-BUDGETS preamble, invariant, append-only-ID note, and complete nine-cell row 027; the QK-REQ-PSBT-005 and QK-REQ-TRN-007 Lim cells; and the QK-TST-DIFF-004 Links cell. All seven are one construction; no alternative, subset, paraphrase, normalization, substitution, reformatting, explanatory source prose, or additional byte is enacted.
- **Canonical effect and preserved state:** `QK-LIM-PSBT-027` becomes the sole permanent, non-reusable canonical tombstone while preserving its ID and historical dimension name. Its exact Value text is `NOT A VALUE — PERMANENT TOMBSTONE SENTINEL`, never zero or an authorized QK-LIM value. Every former row-027 relationship becomes non-operative, and only the three named consumer cells unlink 027. `QK-LIM-PSBT-026`, `ALL GATES OPEN`, all other canonical and historical bytes, global OD-05 unresolved state, and global QK-THR-016 state remain unchanged. No general tombstone vocabulary or transition is created for another row.
- **Explicit exclusions and non-effects:** no other RESOURCE-BUDGETS row or byte, Requirement text or cell, or QK-TST Plan / oracle, Milestone, Gate, Evidence artifact, Status, or other cell change; no answer to F32C-D11-Q-002 through F32C-D11-Q-012; no D-11, D-09, OD-05/06, other QK-LIM value, profile, clause, dependency, implementation, test, corpus, vector, fixture, evidence, media-I/O, hardware, license, gate, STOP-SHIP, setting, credential, remote, other-branch, tag, release, publication, or other change.
- **Effect:** enacts the exact owner-approved permanent tombstone construction locally after verification and independent audit only. F3.2l remains local and unpublished; publication requires separate exact-commit owner authority.

### QK-AUTH-F3.2M-D11-Q002-PKT-001 — F3.2m D-11 Q-002 SD attachment-epoch decision-packet preparation authorization

- **ID:** QK-AUTH-F3.2M-D11-Q002-PKT-001
- **Date:** 2026-08-21
- **Approver:** Project owner
- **Owner words exactly (literal three-paragraph block follows):**

Authorize preparation and independent audit of a non-binding, non-enacting docs-only F3.2m D-11 Q-002 SD attachment-epoch decision packet from published commit 1408fa98961e62ba6316515b1e277b6952a96e18, with only the mechanically necessary Decision Log and verifier bindings.

Limit the packet to F32C-D11-Q-002 only. Preserve and inventory the exact current QK-REQ-BND-003 requirement, the F3.2c Q-002, analysis-only sd_attachment_epoch, bounded read-only preflight, state-transition, and F32C-D11-F-017 material, the published F3.2d Q-001 clarification, and the published F3.2l canonical state. Present exactly three finite, mutually exclusive dispositions, all explicitly PROPOSED — UNSELECTED: F32M-D11-Q002-OPT-001, recommended conditional physical attachment-epoch binding whenever a later-selected export plan includes SD, with bounded read-only preflight before review and approval, the session-scoped non-authenticating epoch and exact reviewed preflight facts approval-bound, removal or replacement ending the epoch and the active attempt, no continuation or substitute-media output under that approval, and a new medium requiring a new plan and full review and approval while unchanged-epoch retry remains Q-005-open; F32M-D11-Q002-OPT-002, non-recommended logical SD-route binding with deferred physical insertion only after signing, subject to an explicit reconciliation with QK-REQ-BND-003, with any pre-signing media change still invalidating approval, no replacement continuation or silent approval carry-over, and retry remaining Q-005-open; or F32M-D11-Q002-OPT-003, deferral with Q-002 unresolved. State that recommendation is analysis only, not an owner answer or selection. Select no identifier, hash, serialization, authenticator, filename, collision policy, media-identity mechanism, filesystem, commit primitive, or target behavior.

No owner selection or canonical or historical edit; no answer to F32C-D11-Q-003 through Q-012 and no change to the recorded Q-001 clarification; no route-set or completion rule, retry rule, same-versus-distinct input/output media rule, filename/collision rule, visibility/commit/orphan rule, QR self-check rule, signature-insertion rule, or D-09 semantic-field rule; no D-11, D-09, OD-05/06, profile, clause, dependency, QK-LIM, QK-TST, evidence, gate, or STOP-SHIP change. Preserve QK-LIM-PSBT-027 as the sole permanent non-reusable tombstone, QK-LIM-PSBT-026, ALL GATES OPEN, and every other canonical and historical byte. No implementation, testing, corpus, vector, fixture, evidence, media I/O, hardware, firmware, license, setting, credential, other-branch, tag, release, publication, remote, or other change.

- **Published parent and source locators:** published base commit `1408fa98961e62ba6316515b1e277b6952a96e18`, tree `75c5e69cf5154b1936b86a6f0795f615a4d6ab3d`, and Decision Log source blob `7d03656d245786f3a17eb4dbae689ef03b172b75`; immutable F3.2c packet blob `b4594210975940df71b0e941841320d11defaa4c`; immutable F3.2d record blob `a996a6d7c2bfa3a15109085475868410fe354422`; canonical REQUIREMENTS blob `2cf8a34aa5621dcea114e2d5ec3dabc2b79aef84`; canonical RESOURCE-BUDGETS blob `09815b0a75c26a118f80c5cec008e695d319900b`; canonical TEST-ARCHITECTURE blob `ecb3867ca2842f6d011288d3dcd94d2b1997e516`. These hashes are supporting byte-identity audit locators only, not authentication, audit proof, owner response, selection, or publication authority.
- **Exact commit/path map:** Commit A appends only `docs/DECISION-LOG.md`; Commit B adds only `docs/f3/F3.2M-D11-Q002-SD-ATTACHMENT-EPOCH-DECISION-PACKET.md` and changes only `tools/verify-host-boundary.sh`; Commit C changes only `tools/verify-current-stage.sh`. The chain is exactly three linear single-parent bodyless commits with no merge or fourth commit and cumulative changes to only those four paths; documentation modes remain `100644` and verifier modes remain `100755`.
- **Closed decision-input world:** the packet asks only `F32C-D11-Q-002` and presents only `F32M-D11-Q002-OPT-001` through `F32M-D11-Q002-OPT-003` in order. All three dispositions remain `PROPOSED — UNSELECTED`; the option-1 recommendation is analysis only and is not selected. No owner answer, approval, default, hybrid, fourth option, or implicit selection is recorded or inferred.
- **Published-source and historical-text boundary:** F3.2c, F3.2d, and F3.2l are published source stages. F3.2d supplies the current Q-001 clarification. The F3.2c and F3.2d `LOCAL AND UNPUBLISHED` text and the F3.2c Q-001-unanswered wording remain byte-identical immutable historical source text; they are not current-publication assertions and are not normalized or corrected. F3.2m alone is local and unpublished.
- **Protected canonical state:** the exact current `QK-REQ-BND-003` row remains unchanged; `QK-LIM-PSBT-027` remains the sole permanent non-reusable tombstone and non-value sentinel; `QK-LIM-PSBT-026` remains live and `OPEN`; former row-027 Requirement/Test consumer links remain absent; `QK-TST-DIFF-004` remains `PLANNED — NOT RUN`; `ALL GATES OPEN` and STOP-SHIP remain unchanged.
- **Explicit exclusions and non-effects:** no stable media identity or mechanism; identifier, hash, serialization, authenticator, filename, collision, filesystem, writer, commit primitive, target behavior, atomicity, durability, receipt, residue-absence, route-set, completion, retry, same-versus-distinct media, visibility, orphan, QR self-check, signature-insertion, or D-09 semantic-field policy is selected. Unchanged-epoch retry and Q-005, Q-003 through Q-012, D-09, D-11, OD-05/06, the profile, clauses, dependencies, every QK-LIM other than preserving enacted state, QK-TST, evidence, gates, and STOP-SHIP remain unresolved or unchanged as applicable.
- **Effect and local stop:** authorizes local preparation, mechanical verifier binding, and independent read-only audit of one non-binding, non-enacting F3.2m decision packet only. No canonical or historical byte is changed except this strict append, no implementation or media I/O is performed, and the completed chain must remain local and unpublished.
- **Later authority boundaries:** an owner response or selection, any canonical amendment, and publication each require separate explicit later owner authority. Publication authority must name the exact independently audited 40-hex commit.

### QK-AUTH-F3.2N-D11-Q002-SD-ATTACHMENT-EPOCH-DIR-REC-001 — F3.2n D-11 Q-002 physical SD attachment-epoch owner-direction record preparation authorization

- **ID:** QK-AUTH-F3.2N-D11-Q002-SD-ATTACHMENT-EPOCH-DIR-REC-001
- **Date:** 2026-08-21
- **Approver:** Project owner
- **Owner words exactly (literal six-paragraph block follows):**

Approve, as a non-enacting owner answer and direction for separately authorized later D-11 construction or canonical work, F32M-D11-Q002-OPT-001 exactly as printed in the published F3.2m packet at commit f41e26799d0c4522b91e1de72d27cf473116b3ab, packet blob 11714544ea89b3475d65a8a5634b7fdab65f5972:

“If separately selected later, whenever a later-selected export plan includes SD, require the exact physical SD attachment epoch to be present: perform bounded read-only preflight before review and approval; bind the session-scoped non-authenticating epoch and exact reviewed preflight facts to approval; removal or replacement ends that epoch and, where an active attempt exists, ends that attempt; permit no continuation or substitute-media output under that approval; and require a new medium to receive a new plan and full review and approval. Retry under an unchanged attachment epoch remains F32C-D11-Q-005-open. This disposition selects no media-identity mechanism and does not define the content of any filename or collision fact.”

Approve all and only that disposition. Approve no alternative, hybrid, subset, paraphrase, normalization, substitution, or additional change.

This approval answers only F32C-D11-Q-002 by selecting the conditional physical SD attachment-epoch direction for future work whenever a later-selected export plan includes SD. It does not enact that direction, amend QK-REQ-BND-003 or any other canonical byte, select D-11 or an export plan, or authorize implementation or media I/O. Preserve the immutable F3.2m packet and its three PROPOSED — UNSELECTED rows as byte-identical historical source material. Unchanged-epoch retry remains F32C-D11-Q-005-open.

Authorize preparation and independent audit of a non-enacting docs-only F3.2n D-11 Q-002 physical SD attachment-epoch owner-direction record from published commit f41e26799d0c4522b91e1de72d27cf473116b3ab, recording only this exact approval and reproducing the selected option text byte-for-byte, with only the mechanically necessary Decision Log and verifier bindings. Use exactly three linear single-parent commits: Commit A appends only docs/DECISION-LOG.md; Commit B adds only docs/f3/F3.2N-D11-Q002-SD-ATTACHMENT-EPOCH-OWNER-DIRECTION-RECORD.md and changes only tools/verify-host-boundary.sh; Commit C changes only tools/verify-current-stage.sh. Stop locally after independent audit; publication requires separate exact-commit authority.

No canonical REQUIREMENTS, RESOURCE-BUDGETS, TEST-ARCHITECTURE, ARCHITECTURE, or other canonical edit; no change to the published F3.2d Q-001 clarification; no answer to F32C-D11-Q-003 through Q-012, including no unchanged-epoch retry or Q-005 answer; no route-set or completion rule, same-versus-distinct input/output media rule, filename or collision rule, visibility, commit, orphan, QR self-check, signature-insertion, or D-09 semantic-field rule; no identifier, hash, serialization, authenticator, stable media identity, media-identity mechanism, filesystem, writer, commit primitive, target behavior, atomicity, durability, receipt, or residue policy. No D-11, D-09, OD-05/06, profile, clause, dependency, QK-LIM, QK-TST, evidence, gate, or STOP-SHIP change. Preserve QK-LIM-PSBT-027 as the sole permanent non-reusable tombstone, QK-LIM-PSBT-026, ALL GATES OPEN, and every other canonical and existing historical byte. No implementation, testing, corpus, vector, fixture, evidence, media I/O, hardware, firmware, license, setting, credential, other-branch, tag, release, publication, remote, or other change.

- **Published source boundary:** F3.2m published commit `f41e26799d0c4522b91e1de72d27cf473116b3ab`, tree `9ae24ea3278252b0a0f6703d2cbc381d59859dcf`, and immutable packet blob `11714544ea89b3475d65a8a5634b7fdab65f5972`. These are supporting byte-identity audit locators only, not authentication, audit proof, enactment, or publication authority.
- **Exact commit/path map:** Commit A appends only `docs/DECISION-LOG.md`; Commit B adds only `docs/f3/F3.2N-D11-Q002-SD-ATTACHMENT-EPOCH-OWNER-DIRECTION-RECORD.md` and changes only `tools/verify-host-boundary.sh`; Commit C changes only `tools/verify-current-stage.sh`. The chain is exactly three linear single-parent bodyless commits with no merge or fourth commit; documentation modes remain `100644` and verifier modes remain `100755`.
- **Selection and historical boundary:** all and only `F32M-D11-Q002-OPT-001` is recorded as an owner-approved non-enacting direction. The immutable F3.2m packet remains byte-identical, and all three of its rows remain `PROPOSED — UNSELECTED` as historical source material. The packet's analysis recommendation is historical analysis; the owner selection is recorded only by this later record.
- **Effect and local stop:** this authorization permits only local preparation, mechanical verifier binding, and independent read-only audit of the one non-enacting direction record. It changes no canonical byte, does not enact the selected direction, and grants no implementation, media-I/O, or publication authority. The completed chain must remain local and unpublished.
- **Later authority:** a separately selected export plan, any construction or canonical action, implementation or media I/O, and publication each require separate explicit later owner authority. Publication authority must name the exact independently audited 40-hex commit.

## Standing rules

| Date | ID | Decision | Rationale |
|---|---|---|---|
| 2026-08-23 | QK-DEC-015 | D-11 Q-003: one approval authorizes exactly one output route. | Removes cross-route ambiguity and gives one observable outcome. |
| 2026-08-23 | QK-DEC-016 | D-11 Q-004: success requires complete verified output on the selected route only; partial output is failure. | Creates binary completion without partial-success claims. |
| 2026-08-23 | QK-DEC-017 | D-11 Q-005: an explicit retry is permitted only in the unchanged session/attachment epoch and re-delivers the frozen signed artifact byte-identically; it never re-runs signing, and any new signing pass is a new cycle requiring fresh approval. | Handles transient delivery failure without reusing approval for new signing or changed facts. |
| 2026-08-23 | QK-DEC-018 | D-11 Q-006: the same SD medium is permitted; input is never modified, output uses the Q-008 deterministic distinct filename and Q-007 full readback; dual media is recommended but not required, and this decision is revisitable when hardware slot count is selected. | Avoids mandatory mid-flow swaps and single-slot failure modes while Q-007/Q-008 cover overwrite risk. |
| 2026-08-23 | QK-DEC-019 | D-11 Q-007: output commits only after bounded write, finalization, synchronization, reopen, and exact readback verification. | Writing bytes alone does not prove a complete recoverable artifact. |
| 2026-08-23 | QK-DEC-020 | D-11 Q-008: use a deterministic distinct filename derived from unsigned-transaction identity and fail closed on collision, never overwriting or auto-renaming; the exact naming scheme remains an implementation detail. | Makes collisions meaningful and prevents silent change to reviewed output facts. |
| 2026-08-23 | QK-DEC-021 | D-11 Q-009: incomplete temporary artifacts are invalid, ignored for recovery/use, reported clearly, and never automatically promoted. | Partial files cannot become authorized output. |
| 2026-08-23 | QK-DEC-022 | D-11 Q-010: self-check decodes rendered symbol data in memory through a bitmap round-trip; for multipart output decode each frame, reassemble fully, and compare with approved bytes, making no camera-hardware claim. | Proves generated symbol data rather than physical capture reliability. |
| 2026-08-23 | QK-DEC-023 | D-11 Q-011: insert signatures deterministically in canonical public-key order and reparse/revalidate after each insertion; canonical serialization is a separate later milestone. | Deterministic ordering reduces ambiguity and detects mutation. |
| 2026-08-23 | QK-DEC-024 | D-11 Q-012: D-09 commits network, wallet/policy identity, unsigned transaction, input values/scripts, per-input sighash flags, outputs, change-output identification facts including key-origin/derivation data, fee, sequences, locktime, selected route, and approval-bound preflight facts. | These facts determine transaction meaning and change destination. |
| 2026-08-23 | QK-DEC-025 | Cycle token: mint an opaque monotonic per-cycle token at Approve and require matching tokens on RevalidationPassed and SignatureProduced. | Makes approval binding compiler-checked without wallet data. |
| 2026-08-23 | QK-DEC-026 | Protocol-Lab freeze: QuietKey-Protocol-Lab is frozen as read-only legacy/reference work; nothing from it becomes QuietKey source, dependency, decision, or evidence without explicit Owner authorization and review. | Prevents parallel experimental work from becoming implicit product authority or scope drift. |
| 2026-08-23 | QK-DEC-027 | Authority ordering: QuietKey Core Architecture v1 (`docs/qk2/00-core-architecture-v1.md`) is the supreme authority for system and security architecture; the QK2 working drafts elaborate it, with the PSBT Checklist governing the signing pipeline; QK2-HW-P0.1 governs prototype hardware except its superseded 2-of-2 basis line, bech32 footer-token target, 99-roll dice test, and KMAC-related rows; QK2-04 and CloakVault v3 are superseded historical records, with QK2-04's Gate A test methodology retained; where documents disagree, Core Architecture v1 is correct. | One Owner-ratified precedence order prevents conflicting documents from becoming implicit authority. |
| 2026-08-23 | QK-DEC-028 | D-11 Q-006 finalized: the same-SD-medium decision of QK-DEC-018 is final and its revisitable flag is removed, because the selected hardware confirms exactly one microSD socket and one card slot. | The open hardware condition of QK-DEC-018 is now resolved by the ratified hardware blueprint. |
| 2026-08-23 | QK-DEC-029 | D-11 Q-008 corrected: the deterministic output filename is derived from the input filename as name-signed.psbt per the PSBT Checklist, superseding the QK-DEC-020 naming detail; the collision rule is unchanged — fail closed on collision, never overwrite or auto-rename. | The governing PSBT Checklist fixes the naming scheme that QK-DEC-020 left as an implementation detail. |
| 2026-08-23 | QK-DEC-030 | M3 parsing: unknown and proprietary PSBT records are preserved verbatim as bounded byte ranges; the exact rejection list is PSBT v2 global fields, Taproot fields, duplicate keys, and non-minimal CompactSize. | Preserving unknown records keeps interoperability while the bounded rejection list keeps the v0 profile closed. |
| 2026-08-23 | QK-DEC-031 | Candidate constants: MAX_INPUTS=100, MAX_OUTPUTS=32, SD artifact cap 2 MiB, QR export cap 256 KiB / 256 frames / 120 s, alongside the existing MAX_SIGNERS=15 and MAX_PATH_DEPTH=12. | Explicit bounds make resource limits reviewable and testable instead of implicit. |
| 2026-08-23 | QK-DEC-032 | Post-M3 validation: when witness_utxo is present it must exactly equal the referenced previous-transaction output. | An unverified witness_utxo permits value and script substitution against the signer. |
| 2026-08-23 | QK-DEC-033 | Post-M3 validation: an input already at complete signature threshold receives verification and export only, never an added signature. | Signing past threshold adds no authorization and would silently change reviewed transaction facts. |
| 2026-08-23 | QK-DEC-034 | Resource-Budget Registry process amendment: owner-ratified CANDIDATE constants for the HOST scaffold may be recorded in the Decision Log and in code, explicitly labeled candidates with no conformance claim; final values still require each affected row's full freeze pack and registry authorization. This rule retroactively covers QK-DEC-031 and does not authorize or edit any RESOURCE-BUDGETS value. | Recording explicit candidate bounds keeps HOST-scaffold work reviewable and testable without bypassing the registry's freeze-pack authority. |
| 2026-08-23 | QK-DEC-035 | HOST-scaffold CANDIDATE constants with no conformance claim: MAX_KEY_BYTES=128 (QK-LIM-PSBT-007); MAX_VALUE_BYTES=1 MiB / 1,048,576 (QK-LIM-PSBT-008); MAX_RECORDS_PER_MAP=64 shared across QK-LIM-PSBT-002/009/010, with every known, unknown, and proprietary record counting and QK-LIM-PSBT-012 explicitly covered; MAX_TX_OUTPUT_SCRIPT_BYTES=10,000 (consensus MAX_SCRIPT_SIZE). Final values remain OPEN pending freeze packs and registry authorization. | Explicit per-key, per-value, per-map, and per-script candidate bounds close unbounded-resource surfaces while final registry values stay OPEN. |
| 2026-08-23 | QK-DEC-036 | M5 canonical PSBT-v0 serializer normalization is exactly: (1) canonical record ordering within each map per the PSBT Checklist — ascending decoded numeric key type, then raw key data lexicographically; and (2) minimal CompactSize encoding. Every record's complete key and value bytes remain byte-verbatim; no record is added, dropped, or rewritten. S9 redundant-SIGHASH_ALL stripping is deferred to the future semantic emit layer. | Freezes the complete structural normalization set needed before signature insertion without introducing semantic emit behavior. |
| 2026-08-23 | QK-DEC-037 | M6 performs structural signature validation only: strict-DER form, low-S, exactly 33-byte compressed-pubkey syntax, trailing sighash byte, and per-input count of structurally valid partial signatures against m-of-n parsed from the witness script. Expose only a STRUCTURAL-CANDIDATE threshold status. Cryptographic verification and enacted QK-DEC-033 export-only behavior are deferred to a future separately ratified libsecp256k1 FFI milestone under Core Architecture v1 section 3.3; hand-rolled curve arithmetic is prohibited; a structurally threshold-complete but cryptographically unverified input changes no behavior in M6. S6 is default-deny: every PSBT_IN_SIGHASH_TYPE value and every signature trailing byte must be SIGHASH_ALL. | Separating structural signature analysis from deferred cryptographic verification keeps M6 reviewable without ever treating an unverified signature as authorization. |
| 2026-08-23 | QK-DEC-038 | SHA-256 is implemented in-repo from FIPS 180-4 with no dependency and checked against byte-verbatim NIST CAVP vectors recorded in SOURCE-REGISTER; txid is double-SHA256 over witness-stripped serialization streamed from existing spans, with no serializer changes. No FIPS/CAVP validation claim. | An in-repo FIPS 180-4 implementation checked against recorded NIST vectors provides txid computation without new dependencies or validation claims. |
| 2026-08-23 | QK-DEC-039 | QK-DEC-034 HOST-scaffold CANDIDATE limits, no conformance claim and registry values remain OPEN: MAX_PREVTX_INPUTS=8192, MAX_PREVTX_OUTPUTS=8192, MAX_PREVTX_SCRIPT_BYTES=10000, MAX_PREVTX_WITNESS_ITEMS=1000 per input, MAX_PREVTX_WITNESS_ITEM_BYTES=10000 per item, all within existing MAX_VALUE_BYTES=1 MiB. | admit large real-world batch/consolidation funding transactions without leaving nested fields unbounded. |
| 2026-08-24 | QK-DEC-040 | M7 vendors libsecp256k1 exactly at signed annotated tag v0.8.0: tag object 18f07c42218765cd46148d74d9fe575795f56dce, commit 6e2c8bc4ecdc6e71dbe7a368f360d8d453ce435d, tree ad8f91b605a0bc4ad4df97059c4131792abca5ed. Tag-signature verification and signer-key fingerprint 6A8F9C266528E25AEB1D7731C2371D91CB716EA7 are recorded as provenance evidence only, explicitly not ratified authorship proof. The complete 189-entry upstream tracked tree is vendored byte-identically with modes under third_party/libsecp256k1, including COPYING and every nested notice file, with no exclusions or additions inside the subtree and no .git. The tree carries mixed notices: root/base MIT COPYING, examples CC0 EXAMPLES_COPYING, bundled Wycheproof Apache-2.0 WYCHEPROOF_COPYING. The owner authorizes retention and use for this M7 import only; this does not select QuietKey's project license and does not close OD-08. | A pinned, signature-checked, whole-tree byte-identical import keeps vendor provenance reviewable without making authorship or licensing decisions. |
| 2026-08-24 | QK-DEC-041 | M7 product build selection is exactly src/secp256k1.c, src/precomputed_ecmult.c, src/precomputed_ecmult_gen.c, compiled and archived in that order. Literal system cc and ar are invoked directly by build.rs via std::process::Command without a shell; no Cargo dependencies, dev-dependencies, or build-dependencies; outputs only in OUT_DIR; no network, configure, CMake, or autotools; environment CFLAGS are ignored, never injected. Product flags are exactly: -std=c90 -O2 -fPIC -ffunction-sections -fdata-sections -fvisibility=hidden -DSECP256K1_NO_API_VISIBILITY_ATTRIBUTES=1 -DECMULT_WINDOW_SIZE=15 -DCOMB_BLOCKS=43 -DCOMB_TEETH=6 -Wall -Wextra -Werror -pedantic -Wcast-align -Wnested-externs -Wno-long-long -Wno-overlength-strings -Wno-unused-function -Wshadow -Wstrict-prototypes -Wundef, plus only the vendor include and src include paths. All ENABLE_MODULE_*, VERIFY, COVERAGE, USE_EXTERNAL_ASM, USE_EXTERNAL_DEFAULT_CALLBACKS, and forced-widemul macros remain undefined. The build is host-only and fails closed unless HOST equals TARGET and the target family is unix; no OD-01, reproducibility, or target claim is made. | Freezing the exact compile closure, flags, and toolchain invocation keeps the native build reviewable, dependency-free, and incapable of silently widening beyond the ratified units. |
| 2026-08-24 | QK-DEC-042 | M7 FFI/API boundary: one private Rust FFI module with deny(unsafe_op_in_unsafe_fn). Infrastructure may declare the immutable secp256k1_context_static object; the exact upstream function declarations and calls are only secp256k1_ec_pubkey_parse, secp256k1_ec_pubkey_serialize with compressed flag 258 and outlen 33, secp256k1_ec_pubkey_tweak_add, secp256k1_ecdsa_signature_parse_der, and secp256k1_ecdsa_verify. No selftest and no sixth function. One static verification-only context; upstream default abort callbacks are retained as defense in depth while fixed-size safe inputs and prechecks make illegal-argument callbacks unreachable. Every 0/1 return is mapped and any other code fails closed. Opaque C objects never escape the wrapper; no signing, seckey, context-create, randomize, destroy, normalize, or optional-module API exists. Truthfulness: the unmodified native archive contains other dormant base symbols; "exact five" means the Rust-declared and callable wrapper surface, not the archive symbol table. | A single audited unsafe boundary with an enumerated five-function surface keeps cryptographic verification reviewable and prevents silent API growth toward signing or key handling. |
| 2026-08-24 | QK-DEC-043 | M7 KAT/test boundary: tests use the byte-identical bundled file third_party/libsecp256k1/src/wycheproof/ecdsa_secp256k1_sha256_bitcoin_test.json, directly pinned to C2SP/wycheproof commit 7ae4532f417575ced2b1cbbabed81a7fecfaef5d, path testvectors_v1/ecdsa_secp256k1_sha256_bitcoin_test.json, blob add468f59a42cf5444343f3b6ca87162afae130e, SHA-256 1be8742064fec73d670339f0036dec56b21baa94cb2d8e0fbbb6fb480f733869; the file is not duplicated. It is 299,012 bytes with 99 groups and 463 contiguous cases: 162 valid, 301 invalid, zero acceptable. Its provenance is Apache-2.0, and its named schema is absent at that pin, so no schema-validation claim is made. M7 tests the verification boundary only. Existing NIST assertions explicitly remain 65 Short, 64 Long, and one Monte seed with ordered COUNT 0..99 / 100 checkpoints; they are inspected and re-run, not duplicated. | Pinning the bundled corpus and its exact counts makes the verification-boundary evidence reproducible without new dependencies or duplicated vectors. |
| 2026-08-24 | QK-DEC-044 | M8 BIP143 SIGHASH_ALL digest semantics: the per-input preimage is exactly versionLE4 || hashPrevouts || hashSequence || current 36-byte outpoint || scriptCode with its minimal CompactSize length prefix || the exact 8-byte little-endian amount from the QK-DEC-032-verified prevtx output || sequenceLE4 || hashOutputs || locktimeLE4 || the exact 4-byte little-endian suffix 01 00 00 00; the digest is double-SHA256 of that preimage. hashPrevouts, hashSequence, and hashOutputs are each double-SHA256 over their specified full concatenations: all raw 36-byte outpoints, all raw 4-byte little-endian sequences, and all exact CTxOut serializations (amountLE8, minimal CompactSize script length, script). For native P2WSH the scriptCode is the witnessScript with its minimal CompactSize length prefix; a witnessScript containing an actual OP_CODESEPARATOR opcode — determined by parsed opcode semantics, never a raw byte scan, because 0xab may occur inside pushed data — fails closed with a dedicated category and no truncation semantics. M8 classification precedence places CodeSeparator before unsupported sighash for overlapping rows, while tests independently assert both classification axes. One SIGHASH_ALL constant binds the one-byte trailing signature sighash (exactly 0x01, stripped before DER parsing) and the 4-byte digest suffix so they cannot diverge. | Pinning the exact preimage layout and failing closed on code-separator and non-ALL semantics keeps the digest engine reviewable and incapable of silent truncation or signing-adjacent behavior. |
| 2026-08-24 | QK-DEC-045 | M8 KAT and verification boundary: official BIP143 known-answer material comes only from bitcoin/bips commit 857a7debc6625a3dadbaecee1ee7b2ed5e8ada75, bip-0143.mediawiki, imported as a bounded local public fixture of strictly enumerated exercised public hexadecimal tokens with provenance recorded in SOURCE-REGISTER; the full file, any private-key line, reference C++ code, prose, and complete signed-transaction payloads are never committed. All 14 official digest rows are exercised with one disposition each: the five SIGHASH_ALL rows end to end at the digest layer with asserted intermediate hashes, complete preimages, and final digests; the nine non-ALL rows and the four CodeSeparator rows are explicit expected fail-closed classifications with asserted categories, never filtered or skipped. Only ALL rows 1, 2, and 7 are positive signature-verification KATs through qk-secp; ALL rows 13 and 14 contain high-S signatures that must assert HighS and are never normalized. qk-secp is added only as an internal path dependency of qk-psbt; the already-audited qk-secp five-function FFI/public API and the vendor subtree remain byte-identical. Every existing partial signature is verified against its exact map pubkey and the computed digest; a partial-signature map pubkey outside the input witnessScript member set has its own dedicated rejection category and is never ignored; any malformed, off-curve, or invalid signature rejects the whole verified candidate even when m other signatures already verify. | Bounded public-token import plus complete row accounting makes the cryptographic evidence reproducible without secret or fundable material and without widening the audited verification boundary. |
| 2026-08-24 | QK-DEC-046 | M8 dispositions and exclusions: signature verification is a separate read-only entry point deriving everything from the same parsed view; the M6 structural analyzer, its output, and its legacy precedence are preserved, and M6 structural facts remain explicitly non-authoritative. Threshold is per input: each input carries a verified signature count and a named status BelowThreshold or CryptographicallyVerifiedThreshold; the aggregate status set is exactly AllInputsBelowThreshold, MixedInputCompleteness, and VerifyAndExportOnly, and VerifyAndExportOnly — enacting QK-DEC-033 only as a returned disposition — is returned if and only if every input's verified count reaches that input's m. It is a fact and no-additional-signature direction only: it performs and authorizes no export and bypasses no later S4/S7, ownership, or change checks. Before any cryptography an input requires a witnessScript, rejects final-script fields and redeem-script/wrapped routes, and requires the selected prevout scriptPubKey to be exactly the native P2WSH program 00 20 SHA256(exact witnessScript) as required coherence, never descriptor ownership or change classification. M8 contains no signing, signature insertion, serializer change, finalization, extraction or media export, no HMAC/SHA512/BIP32, no descriptor ownership or change classification, no S4/S7 authorization, no target or release claim, and no M9 work. | Per-input thresholds with a closed aggregate status set give one truthful completeness fact while keeping export, authorization, and ownership decisions outside M8. |
| 2026-08-24 | QK-DEC-047 | M8 incident disposition: commits 86acb94 and cc19589 entered history containing a hand-rolled curve implementation used as a test signer with fixed nonce k=1 and recoverable fixture keys, violating QK-DEC-037 and the no-secrets rule. The current tree was repaired in f637df0, which regenerated all committed fixtures from high-entropy ephemeral keys with per-message nonces. History is retained unmodified under the standing no-rewrite policy as synthetic valueless material with zero confidentiality impact. Standing rule 1: any test-signing or fixture-generation capability, however synthetic, requires prior ratification before it exists even in a working tree. Standing rule 2: verification fixtures are generated outside the repository with the generation procedure documented in the fixture header. | Durable incident recording plus standing rules keeps the violation, its repair, and its prevention reviewable without history rewriting. |
| 2026-08-24 | QK-DEC-048 | M9 private hash boundary: qk-bip32 implements SHA-512 pinned to FIPS 180-4 semantics (1024-bit/128-byte block, 128-bit length field, 80 rounds) and HMAC-SHA512 pinned to FIPS 198-1 semantics with B=128 and L=64: a key longer than 128 bytes is hashed first, a shorter key is zero-padded to 128 bytes, ipad=0x36, opad=0x5c, and the MAC is H((K0 xor opad) concatenated with H((K0 xor ipad) concatenated with text)). Production inputs are public only: chain_code[32] as the key and exactly serP33 followed by indexBE4 as the message. The SHA-512 and HMAC-SHA512 modules and functions are private; no general public hash or HMAC API exists. Explicitly authorized, narrowly under QK-DEC-047 standing rule 1 and AGENTS rule 1: only the committed public NIST HMAC Key= KAT fields and the decoded public BIP32 chain-code/pubkey facts enter Git, as synthetic valueless test bytes, never secrets. | Pinned primitive semantics with private modules give CKDpub a verifiable foundation without widening the public cryptographic surface. |
| 2026-08-24 | QK-DEC-049 | M9 exact CKDpub and frozen public surface for qk-bip32, enumerated before any code exists. Derivation: I = HMAC-SHA512(key = parent chain code, data = canonical serP(parent) followed by ser32BE(index)); only index < 2^31 is accepted; I splits into IL and IR; IL >= secp256k1 n is rejected as the named error InvalidTweak before any FFI call (IL = 0 is allowed); child key = tweak-add(parent, IL) using only unchanged qk-secp parse/serialize/tweak-add with no sixth FFI function; a qk-secp tweak rejection after a valid IL is named PointAtInfinity; child chain = IR; depth increments by one and depth 255 rejects as DepthOverflow. Deliberate deviation from the BIP32 skip-to-next-index generation procedure: this validator evaluates the presented index and never increments, scans, retries, or searches. COMPLETE public surface: pub struct PublicNode with pub fields depth: u8, chain_code: [u8; 32], compressed_public_key: [u8; 33], deriving Debug, Clone, Copy, PartialEq, Eq; pub enum CkdPubError, closed, unit variants HardenedIndex, DepthOverflow, InvalidParentKey, InvalidTweak, PointAtInfinity, deriving Debug, Clone, Copy, PartialEq, Eq, with a Display impl emitting exactly the fixed texts "hardened index rejected", "depth overflow", "invalid parent public key", "invalid tweak", "point at infinity"; pub fn derive_public_child(parent: &PublicNode, index: u32) -> Result<PublicNode, CkdPubError>. Nothing else is public: no public constants, no public modules, no public hash or HMAC API, no private derivation. | The smallest workable surface with a closed error set freezes observable behavior before implementation and keeps every primitive private. |
| 2026-08-24 | QK-DEC-050 | M9 KAT, fixture, and exclusion boundary. Official CKDpub facts enter Git only as decoded public fields from bitcoin/bips bip-0032.mediawiki at commit 857a7debc6625a3dadbaecee1ee7b2ed5e8ada75 with exact blob provenance (blob 558bbaab60d63e3e50648816d5e3449cfb887f85, 28032 bytes, SHA256 e5e00a8289db2f681052cf24a745320afc225e66b25d1e489a7c884d2fc7f11f), covering exactly six nonhardened transitions across test vectors 1 and 2. Base58Check decoding and validation happen outside Git and the exact procedure is documented in the fixture header per QK-DEC-047 standing rule 2; no Base58 token, xpub or xprv text, seed, private scalar, fingerprint, or source prose or code enters Git. NIST imports carry exact hashes, sizes, line endings, and complete parsed and executed counts, with no FIPS or CAVP validation claim. M9 exclusions: no Base58 or xpub text API, no HASH160 or fingerprints, no BIP48 or profile or path policy, no descriptors or wallet_id, no PSBT key-origin trust, no ownership, change, S4, or S7 authorization, no signing, private CKD, xprv, or seed handling, no signature insertion or finalization, no serializer, export, media, card, target, or release work, and no M10 work. QK-LIM-PSBT-011 and QK-LIM-PSBT-025 remain OPEN; no scanning or call-cap claim is made. | Bounded public-fact fixtures decoded outside the repository keep official vectors verifiable while excluding every secret-bearing or scope-expanding artifact. |
| 2026-08-24 | QK-DEC-051 | M10 narrowly supersedes only QK-DEC-050's M9 exclusion of Base58/xpub text and freezes this complete qk-bip32 public-surface addition: `DecodedXpub` with public fields `public_node: PublicNode`, `parent_fingerprint: [u8; 4]`, and `child_number: u32`; closed unit-variant `XpubDecodeError` with exactly `InputTooLong`, `InvalidBase58Character`, `DecodedLength`, `ChecksumMismatch`, `UnsupportedVersion`, `InvalidRootParentFingerprint`, `InvalidRootChildNumber`, `InvalidPublicKeyPrefix`, `InvalidPublicKey`, and `CryptographicBackendInvariant`; and `decode_mainnet_xpub(input: &[u8]) -> Result<DecodedXpub, XpubDecodeError>`. The struct and enum derive only Debug, Clone, Copy, PartialEq, Eq; the error has fixed Display texts, respectively: "input too long", "invalid base58 character", "decoded length invalid", "checksum mismatch", "unsupported extended key version", "root parent fingerprint must be zero", "root child number must be zero", "invalid public key prefix", "invalid public key", and "cryptographic backend invariant", plus the mechanical Error implementation. No other new public item, method, constant, module, or trait implementation exists; every M9 item and behavior remains unchanged. | A closed, pre-enumerated public API and non-attacker-bearing errors prevent implementation-driven surface growth. |
| 2026-08-24 | QK-DEC-052 | M10 strict mainnet xpub decoding is fixed as follows. Input is at most 112 bytes and every byte must belong to the exact case-sensitive Bitcoin Base58 alphabet `123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz`; no Unicode, whitespace, NUL, trimming, normalization, alternate alphabet, or silent acceptance. After the length gate and complete alphabet scan, a fixed `[u8; 82]` big-endian multiply/add accumulator with correct leading-`1` accounting decodes exactly 82 bytes, bounded by 112*82 digit-byte steps; overflow or any other decoded length is `DecodedLength`. SHA256d(payload)[0..4] is checked before interpreting any payload field. Version is exactly big-endian 0488b21e; every other version is `UnsupportedVersion`. Layout is version 0..4, depth 4, parent fingerprint 5..9, child number big-endian 9..13, chain code 13..45, compressed public key 45..78, checksum 78..82. At depth zero, nonzero fingerprint rejects before nonzero child number; at depth above zero all child-number metadata, including 80000000 through ffffffff, is accepted and no parent linkage is inferred. Public-key prefix must be 02/03; ordinary qk-secp parse/off-curve rejection is `InvalidPublicKey`, while unexpected qk-secp backend or serialization status is `CryptographicBackendInvariant`. Fixed precedence is InputTooLong, InvalidBase58Character, DecodedLength, ChecksumMismatch, UnsupportedVersion, InvalidRootParentFingerprint, InvalidRootChildNumber, InvalidPublicKeyPrefix, InvalidPublicKey, with backend-invariant mapping only at the qk-secp boundary. SHA-256/SHA256d are private clean-room FIPS-180-4 modules only for this Base58Check path; no allocation, I/O, network, randomness, logging, recursion, unsafe code, bigint, Base58 encoder, serializer, or public/general Base58, SHA, or checksum API. | Fixed memory, complete lexical validation, checksum-before-semantics, and a closed precedence chain make hostile text handling bounded and deterministic. |
| 2026-08-24 | QK-DEC-053 | M10 imports exactly 19 unique public xpub cases from the pinned QK-DEC-050 BIP32 source: 12 valid public xpubs at lines 222, 225, 228, 231, 234, 237, 244, 247, 250, 253, 256, 259; safe official negatives at lines 295 and 297 (`InvalidPublicKeyPrefix`), 300 (`InvalidRootParentFingerprint`), 302 (`InvalidRootChildNumber`), 304 (`UnsupportedVersion`), and 307 (`InvalidPublicKey`); plus one locally derived `ChecksumMismatch` made only by changing line 222's final Base58 character from 8 to 9, preserving the public payload and changing checksum ab473b21 to ab473b22. Decoding, validation, and local mutation occur outside Git; only the public tokens, expected categories, and decoded public fields enter the fixture. Lines 293, 303, and 308 are excluded in full because they contain private-form material; every xprv-form line, seed, private scalar, source prose/reference code, the complete mediawiki, decoder/generator, downloads, and intermediates are excluded. M10 adds no xprv/private CKD, HASH160/fingerprint calculation, path parsing, descriptor, wallet_id, ownership/change, PSBT integration, signing/insertion/finalization/export, media/target behavior, conformance/release claim, or M11; QK-LIM-PSBT-011 and QK-LIM-PSBT-025 remain OPEN. | Complete public-case accounting and explicit private-bearing exclusions preserve the no-secrets boundary while exercising every decoder gate. |
| 2026-08-24 | QK-DEC-054 | M11 adds one HOST-only `qk-descriptor` crate that accepts exactly two borrowed 445-byte ASCII descriptors: a 436-byte body plus `#` and eight BIP-380 checksum symbols. Receive is exactly `wsh(sortedmulti(2,A,B,C))` with three `[lowercase-fp/48'/0'/0'/2']XPUB111/0/*` expressions; change is the byte-identical body except all three branches are `/1/*`. Apostrophe is the only hardened marker; no whitespace, Unicode, alternate spelling, normalization, serializer, formatter, encoder, or general descriptor grammar exists. Both sides are validated receive-first at each stage: length, delimiter, checksum characters, descriptor characters, checksum, exact grammar, pair match, all xpub decodes, all depths, all child numbers, then duplicate account nodes. Change xpub bytes must match receive; only receive xpubs are decoded through unchanged M10, with depth exactly 4 and child number exactly 80000002. Origin fingerprints are metadata, need not match xpub parent fingerprints, and may be equal; equal `PublicNode` material across roles rejects. | One byte-canonical paired profile with closed precedence prevents normalization ambiguity and metadata from disguising duplicate authorities. |
| 2026-08-24 | QK-DEC-055 | M11 wallet ID is exactly SHA256(receive445 including checksum, one raw 00 byte, change445 including checksum), a fixed 891-byte transcript computed only after complete pair validation. At a supplied index below 80000000, script derivation performs A then B then C, branch then index through unchanged presented-index-only CKDpub: at most six calls and no scan, retry, increment, or fallback. Equal final keys reject before fixed BIP-67 compare-swaps `(0,1),(1,2),(0,1)`. The witness script is exactly `52 21 K0 21 K1 21 K2 53 ae` (105 bytes); native P2WSH scriptPubKey is exactly `00 20 SHA256(witness_script)` (34 bytes). Branch 1 is a public derivation fact only, never ownership/change classification or authorization. All checksum/hash/script helpers and fixed constants remain private and fixed-memory. | A pinned transcript and bounded derive-then-sort construction make every exposed public fact deterministic without widening into ownership or policy. |
| 2026-08-24 | QK-DEC-056 | M11 freezes the complete public surface to opaque `DescriptorPair` with only `wallet_id`; `DerivedScript` with public fixed-array witness-script and scriptPubKey fields; the closed parse-error set `InvalidDescriptorLength`, `InvalidChecksumDelimiter`, `InvalidChecksumCharacter`, `InvalidDescriptorCharacter`, `ChecksumMismatch`, `NonCanonicalDescriptor`, `DescriptorPairMismatch`, `InvalidAccountXpub`, `InvalidAccountDepth`, `InvalidAccountChildNumber`, `DuplicateAccountXpub`, `CryptographicBackendInvariant`; the closed derive-error set `HardenedIndex`, `InvalidTweak`, `PointAtInfinity`, `DuplicateDerivedKey`, `InternalInvariant`; and exactly the parse, receive-derive, and change-derive functions. Errors are unit variants with fixed non-echoing text. Fixtures are limited to the eight pinned BIP-380 checksum tokens, BIP-67 vectors 2 and 4 public key/sort/script tokens, three local public descriptor-pair records, and 23 local checksum-correct public negatives. Local xpubs use already-public valid points with fixed-label public chain codes/fingerprints; generators and intermediates remain outside Git. No private derivation/material, arbitrary descriptor/miniscript, addresses, PSBT connection, ownership/change proof, signing/finalization/export, recovery/media/card/target behavior, product/release claim, or M12 is authorized. Grammar-derived HOST bounds are not QK-LIM values or target evidence; QK-LIM-DSC-001/002, the index-policy ceiling, and OD-05 remain OPEN. | Freezing the smallest observable surface and a public-only, completely accounted fixture boundary keeps M11 reviewable without implying product capability. |
| 2026-08-24 | QK-DEC-057 | M11 Option A allocation boundary: `qk-descriptor` performs zero direct allocations and keeps its own checksum, hash, parser, wallet-ID, sorting, and script helpers fixed-memory; the already-approved M9 `qk-bip32` dependency remains frozen and byte-identical. Each complete HOST receive or change derivation inherits exactly six bounded 165-byte allocations from the six required CKDpub calls, and one receive plus one change inherits exactly twelve; tests assert both count and size. This exception is HOST evidence only. Core Architecture v1 section 3.5 allocation-free derivation remains a production-core obligation for Gate C; no gate closes and no product or target claim follows. | Recording the direct-versus-transitive allocation boundary preserves M11 evidence without misrepresenting the frozen M9 dependency or weakening the production-core requirement. |
| 2026-08-24 | QK-DEC-058 | Correction to QK-DEC-056: the M11 negative fixture contains 22 checksum-correct public pairs plus 1 intentionally checksum-invalid precedence pair, 23 total. QK-DEC-056 remains unchanged as the historical statement it corrects. | Separating checksum-accepted negatives from the single checksum-precedence case keeps fixture provenance and parser-stage evidence factually precise. |
| 2026-08-24 | QK-DEC-059 | M11 fixture-authority incident disposition: commits `2c405ae` and `abbb0fa` reused BIP32 KAT public points whose private counterparts are publicly derivable from the KATs. No secret bytes entered Git, but every historical descriptor containing those authorities is permanently NEVER-FUND. The current M11 fixtures replace all three authorities and every dependent public oracle with NUMS points generated as SHA256(`QuietKey/M11/NUMS/v1` ASCII || `00` || role byte `A`/`B`/`C` || big-endian u32 counter), rejecting x >= secp256k1 p and off-curve x and selecting even y; consumed counters are A=0, B=1, C=0. Standing rule: local descriptor authorities must not reuse a public KAT point or any point with a known private counterpart, and two independent implementations outside Git must agree byte-for-byte on every NUMS authority before fixture generation; generators and intermediates remain outside Git under QK-DEC-047. | A deterministic nothing-up-my-sleeve construction and permanent historical disposition remove known-key fixture authorities without mischaracterizing public KAT bytes as secret material or rewriting published history. |
| 2026-08-24 | QK-DEC-060 | M12 adds one separate HOST-only `qk-psbt` descriptor-ownership entrypoint taking one already-parsed `PsbtView` and one caller-supplied `DescriptorPair`. The caller is responsible for authenticating D; on device D comes from the card per Core Architecture v1 section 5.3. M12 proves no descriptor authenticity or persistence. The unchanged structural and QK-DEC-032 full-prevtx phase runs first. Equal origin-fingerprint values remain accepted by M11 parsing, but M12 rejects a pair whose A/B/C fingerprints cannot uniquely map roles with `AmbiguousDescriptorFingerprints`. Existing M6 and M8 entrypoints, outputs, and precedence remain compatible. | Separating trusted-D supply from hostile-PSBT ownership analysis prevents descriptor metadata or this HOST scaffold from becoming an authentication claim. |
| 2026-08-24 | QK-DEC-061 | M12 proves each input only from exactly three PSBT BIP32 derivation records: exact 33-byte compressed key data; exact little-endian path `[fingerprint/48'/0'/0'/2'/{0,1}/index]`; one uniquely mapped A/B/C role each; no duplicate or foreign role; one shared branch and index; and index at most 65535. Records are hints, never authority. `ProvenWalletInput` is returned only after role-preserving derivation from D and two separate named equalities: a present witnessScript must byte-equal the reconstructed script, and `00 20 SHA256(reconstructed_script)` must byte-equal the QK-DEC-032 selected prevout scriptPubKey. Missing witnessScript is allowed only on this new route. The reconstructed prevout-bound script is the effective script for existing sighash, membership, BIP143, and qk-secp checks; public M8 still requires the record. The result contains a record-truthful `SemanticCandidate`, separate existing cryptographic facts, and separate wallet facts, never a relabelled `VerifiedSemanticCandidate`. | Reconstructing from D and binding the result to the selected prevout makes hostile derivation records non-authoritative while preserving truthful record and cryptographic facts. |
| 2026-08-24 | QK-DEC-062 | M12 output classification is closed: zero relevant D roles is `NotProvenOwned`; one or two coherent relevant roles is `NotProvenOwned`; a complete coherent A/B/C set whose role keys and exact unsigned-output script match D is branch 1 `ProvenChange(index)` or branch 0 `ProvenSelfTransfer(index)`. Relevant contradictions, duplicate roles, mixed coordinates, cap breach, or complete-set script mismatch reject with named categories. Foreign output metadata neither proves nor defeats a valid D set. `NotProvenOwned` is neither recipient authorization nor renderability, and every output class is a returned fact only. | Closed fact classes distinguish descriptor evidence from recipient policy and prevent partial or foreign metadata from becoming change authority. |
| 2026-08-24 | QK-DEC-063 | M12 narrowly expands `qk-descriptor` with `DescriptorPair::origin_fingerprints()` in A/B/C order and exactly two role-preserving claim matchers, `match_receive_derivation_claims` and `match_change_derivation_claims`, which compare supplied A/B/C keys before BIP67 sorting and return an optional `DerivedScript`; no xpub, node, chain-code, arbitrary-branch, or general derivation surface is exposed. HOST candidates under QK-DEC-034 are `MAX_CHILD_INDEX=65535` for QK-LIM-PSBT-011 and `MAX_CHILD_DERIVATIONS=792` for QK-LIM-PSBT-025. The structural maximum is 132 routes (100 inputs plus 32 outputs) times six inherited CKDpub calls: 792 bounded 165-byte allocations, 130680 cumulative requested bytes, with one 165-byte CKDpub buffer logically live. `qk-bip32` remains frozen; `RESOURCE-BUDGETS.md` remains byte-identical and both registry values remain OPEN. | A role-preserving matcher is sufficient for ownership proof, while explicit HOST caps and the inherited allocation ledger bound work without widening M11 internals or authorizing registry values. |
| 2026-08-24 | QK-DEC-064 | M12 fixture scope is one locally generated static public NUMS fixture produced outside Git by two byte-agreeing independent implementations using the QK-DEC-059 tag `QuietKey/M11/NUMS/v1` and a QK-DEC-047 procedure header. It contains no generator, intermediate, signer, scalar, nonce, curve-arithmetic, key-like private, funded, or imported source material. Authorities and derived keys are screened against committed BIP32 KAT and known-key sets. Positives contain zero signatures; unchanged M8 KATs continue to exercise the shared signature engine. The closed inventory is exactly 45 cases: 14 returned-fact cases and 31 named rejections, covering witnessScript presence/absence, receive/change inputs, M12-only equal-fingerprint rejection, output classes, multi-I/O and exact 100/32 maxima, derivation counts and every path component, branch/index 65535/65536, mixed coordinates, role/fingerprint/key mismatches, separate witness/prevout equalities, partial/extra/foreign output evidence, M8-before-wallet precedence, mutation/no-panic, no write, and serializer non-drift. | One completely accounted public fixture and closed case histogram exercise the new boundary without adding test signing or known-private fixture authorities. |
| 2026-08-24 | QK-DEC-065 | M13 adds one separate HOST-only recipient-script-facts entrypoint that first consumes the complete unchanged M12 descriptor-ownership analysis. It classifies only outputs whose M12 fact is `NotProvenOwned`; `ProvenChange(index)` and `ProvenSelfTransfer(index)` remain unchanged and have no recipient fact. The bounded returned surface is exactly a six-variant recipient type plus the exact borrowed raw program/hash/data bytes and their absolute source span, in unsigned-output order; it adds no address encoding or display string. Existing M6, M8, and M12 APIs and precedence remain unchanged. | Keeping recipient facts parallel to unchanged ownership facts prevents destination syntax from relabelling descriptor-proven outputs or becoming address/rendering policy. |
| 2026-08-24 | QK-DEC-066 | M13 accepts exactly six destination templates by byte-exact full-script match: P2WPKH `OP_0 PUSH20`, P2WSH `OP_0 PUSH32`, destination-only P2TR `OP_1 PUSH32`, P2PKH, P2SH, and canonical OP_RETURN. P2TR is a recipient-script fact only and adds no Taproot input, signing, ownership, or QK-DEC-030 behavior. Canonical witness programs for versions 2 through 16 reject as `FutureWitnessVersion`; version 0 or 1 with any nonselected program length or any witness-looking truncated/noncanonical shape rejects as `MalformedWitnessProgram`. Bare pubkey, bare multisig, and every other script have separate named rejections. | Exact templates and distinct future-versus-malformed categories prevent generic witness syntax or legacy bare scripts from silently widening recipient policy. |
| 2026-08-24 | QK-DEC-067 | Canonical OP_RETURN has value zero, occurs at most once, and is `OP_RETURN` followed by no push or exactly one minimally length-encoded push: `OP_0` for empty data, direct pushbytes for lengths 1 through 75, and PUSHDATA1 only for lengths 76 through 80. `MAX_OP_RETURN_PAYLOAD_BYTES=80` is a QK-DEC-034 HOST candidate whose registry freeze pack remains owed. In output order, a second OP_RETURN rejects before its value or encoding; otherwise nonzero value precedes push-shape checks. Multiple pushes, nonminimal encoding, malformed push, and payload length 81 or greater reject separately. M6/M8/M12 complete before any M13 classification. Dust and amount-warning policy remain excluded. | A parsed, bounded, precedence-fixed data-carrier subset avoids fixed-offset interpretation while preserving all earlier verification and ownership failures. |
| 2026-08-24 | QK-DEC-068 | M13 fixture scope is one locally authored static public fixture under QK-DEC-047/QK-DEC-059 containing six literal repeated-byte script templates and exactly 31 closed cases: 17 returned facts and 14 named rejections. It reuses the committed M12 NUMS descriptor input fixture and contains no new authority, derivation, generator, signer, scalar, nonce, private-bearing material, address, funded object, or upstream import. Coverage includes all six templates, OP_RETURN lengths 0/1/75/76/80/81, no-push and OP_0, one/two OP_RETURN, zero/nonzero value, witness versions 0 through 16 and wrong lengths, stable categories, ownership/M8 precedence, exact 32/33 output bounds, mutation/no-panic, no write, and serializer non-drift. | One public, valueless, completely accounted inventory exercises the additive HOST fact boundary without introducing signing or new key authority. |
| 2026-08-24 | QK-DEC-069 | M13 narrowly supersedes `F32B-PSBT-Q-002` only at the raw destination-side P2TR script-classification seam; BIP341/BIP350 vectors, addresses/rendering, and every Taproot semantic, input, signing, and ownership mechanism remain excluded. Historical M13 fixture bytes in `629f644` and `288358e` are retained, permanently NEVER-FUND synthetic material with zero confidentiality impact. OP_RETURN category semantics distinguish actual pushes from arbitrary opcodes while retaining QK-DEC-067's external overlap order: all prior phases, output order, second OP_RETURN, nonzero value, then tail/category gates. The M13 offset rewalk performs zero temporary allocations and allocates only the at-most-32 returned-facts `Vec`. Current fixtures follow QK-DEC-059/QK-DEC-047 and introduce no new authority or KAT point. | A narrow append-only correction keeps historical public bytes accountable while fixing destination syntax categories and removing avoidable bounded-work allocations without widening Taproot or public API scope. |
| 2026-08-24 | QK-DEC-070 | M14 enacts D-09 v1 and supersedes QK-DEC-024 only by making its full bind set exact. Canonical schema order is: `schema_version` u8=`1`; network u8 `BitcoinMainnet=1`; retained S0 intake provenance `InputSource` u8 `MicroSd=1`/`Qr=2`; 32-byte SHA-256 of exact immutable S0; 32-byte M11 `wallet_id`, the available wallet/policy identity; three four-byte descriptor origin fingerprints in fixed A/B/C role order; u32-length-prefixed exact unsigned-transaction bytes; transaction version u32; locktime u32; input count u32; then each input in unsigned-transaction order as explicit index u32, wire-order outpoint txid 32 bytes, vout u32, selected-prevout amount u64, u32-length-prefixed selected-prevout scriptPubKey, sequence u32, effective sighash u32, proven M12 branch u32, proven child index u32, M8 verified-signature count u32, and verified-input status u8 `BelowThreshold=1`/`CryptographicallyVerifiedThreshold=2`; output count u32; then each output in unsigned-transaction order as explicit index u32, amount u64, u32-length-prefixed exact raw scriptPubKey and ownership tag u8 `NotOwned=1`/`ProvenChange=2`/`ProvenSelfTransfer=3`; `NotOwned` is followed by recipient u8 `P2wpkh=1`/`P2wsh=2`/`P2tr=3`/`P2pkh=4`/`P2sh=5`/`OpReturn=6` and u32-length-prefixed exact raw program/hash/OP_RETURN bytes, while each proven-owned tag is followed by its child index u32; checked total input u64, total output u64 and exact fee u64; and M8 aggregate status u8 `AllInputsBelowThreshold=1`/`MixedInputCompleteness=2`/`VerifyAndExportOnly=3`. Zero is reserved for every network, route, ownership, recipient, verified-input and aggregate tag; mappings are wildcard-free and never Rust enum casts. Counts, indices, version, locktime, sequence and sighash are u32 little-endian; amounts, totals and fee are u64 little-endian. Every variable byte field has a u32 little-endian length immediately before it; no CompactSize frames a review field, while the exact unsigned-transaction value retains its already-ratified internal bytes verbatim. There is one representation and no normalization beyond M6-M13. S0 SHA-256 is the sole raw-input identity; no second unsigned-transaction serialization digest exists. Exact unsigned-transaction bytes bind its identity/content, and the individually encoded M6-M13 facts are exactly the approval-bound preflight facts: no arbitrary or caller-supplied preflight blob exists. The canonical transcript contains no full S0, previous transaction, PSBT map, effective witnessScript, source span, category total or opaque preflight. The reconstructed witnessScript and M13 source spans are deterministic/redundant from `wallet_id` plus branch/index plus S0 and are not separate fields. No proven/not-owned/debit category aggregate or fee-rate field exists in v1. | A closed nonzero-tag byte schema binds exact source and transaction content plus every authoritative review fact without inventing opaque preflight data or duplicating deterministic evidence. |
| 2026-08-24 | QK-DEC-071 | M14 review hashing is exactly SHA-256 over ASCII `QuietKey/D-09/review/v1`, then one raw `00` byte, then QK-DEC-070 canonical bytes. `qk-psbt` alone constructs the session-free review object, canonical bytes, and hash with its existing private SHA-256; it knows no `CycleToken`, runner, approval, session, caller-supplied hash, or opaque preflight payload. Factor/card/session facts, signer-set attestation, warning classifications, fee-range policy, rendered addresses/UI, approval UI, signing/insertion, finalization, export and media behavior are v1 non-claims and require a schema-version bump after an authenticated source milestone. `InputSource` binds retained S0 intake provenance/cap only, never D-11 output-media or session identity. | Domain separation and a versioned, session-free object prevent cross-protocol reuse while keeping unavailable facts visibly deferred instead of invented. |
| 2026-08-24 | QK-DEC-072 | `MAX_CANONICAL_REVIEW_BYTES=19272` is the exact D-09 v1 canonical maximum and a QK-DEC-034 HOST candidate only; every affected registry value remains OPEN and no target-RAM or conformance claim is made. Arithmetic is exact: fixed/top = 124: schema1 + network1 + source1 + S0hash32 + wallet_id32 + fingerprints12 + unsigned_tx_length4 + version4 + locktime4 + input_count4 + output_count4 + totals24 + aggregate1. maximum accepted unsigned transaction = 5,535: version4 + vin_count1 + 100*(outpoint36 + empty_script_len1 + sequence4) + vout_count1 + [one OP_RETURN output (amount8 + script_len1 + script83 =92) + 31*(amount8 + script_len1 + script34 =43)] + locktime4. each canonical input = 107; 100 =10,700: index4 + txid32 + vout4 + amount8 + prevout_script(4+34) + sequence4 + sighash4 + branch4 + child4 + verified_count4 + verified_status1. canonical outputs = one OP_RETURN 185 +31 other max outputs 88 =2,913: OP_RETURN: index4 + amount8 + raw_script(4+83) + ownership1 + recipient1 + data(4+80)=185. P2WSH/P2TR: index4 + amount8 + raw_script(4+34) + ownership1 + recipient1 + data(4+32)=88. total =124+5,535+10,700+2,913 = 19,272. domain ASCII is 23 bytes plus one NUL, so maximum streamed hash transcript is 19,296. ReviewHash is 32 bytes and is not part of canonical bytes. Construction rejects checked arithmetic above this exact candidate before one fallible exact reservation. | The bound follows only successful M13 shapes: at most one 83-byte OP_RETURN and every other accepted output script at most 34 bytes, rather than the parser's broader pre-semantic script allowance. |
| 2026-08-24 | QK-DEC-073 | M14 qk-host-sim production binding wraps the unchanged byte/API/graph of qk-host-model and preserves existing runner token semantics. The wrapper retains exact immutable S0 plus authenticated `InputSource` and `DescriptorPair`; no caller can supply a review hash or directly emit `ReviewConstructed`, `RevalidationPassed`, or another critical binding event through the wrapper. It builds the qk-psbt review, internally emits the existing `ReviewConstructed`, and computes/stores only the resulting `ReviewHash` after that transition succeeds. Ordinary `Approve` then mints the unchanged current runner token. Revalidation reparses the same retained S0 with the same source and descriptor, reruns M13/M14, rebuilds the hash, requires exact stored/recomputed hash equality and the current token, and only then internally emits existing `RevalidationPassed(token)`. Any halt, rejection, interruption, failed reparse/analysis/hash/equality, or completed cycle clears stored hash and token; the wrapper never emits `SignatureProduced`. qk-host-model receives no PSBT bytes, hash, context or session state. The M14 fixture is two public, valueless, permanently NEVER-FUND cases generated outside Git: one complete one-input/four-output schema instance and one semantically equal changed-but-parseable S0 with an opaque global record. An independent Perl `Digest::SHA` constructor and Rust/OpenSSL strict decoder/re-encoder agreed on exact S0, unsigned-transaction value, canonical bytes and hashes. It reuses only the corrected M12 NUMS public lineage and contains no new point, generator, scalar, nonce, signer, KAT point, signing capability, funded object or external import. | Binding the real semantic hash at a closed simulator seam proves the unchanged token and physically approved review refer to the same immutable source while retaining the payload-free model boundary. |
| 2026-08-24 | QK-DEC-074 | QK-DEC-072’s singular “one fallible exact reservation” refers only to construction of the canonical-transcript `Vec` after its checked bound. The unchanged M6–M13 analyses and Review input/output fact vectors retain their existing separately bounded allocations. This clarification changes no schema, cap, registry, allocation, or semantic rule. | The allocation wording must distinguish canonical transcript construction from the preexisting bounded returned-fact allocations without changing M14 behavior. |
| 2026-08-24 | QK-DEC-075 | M15 enacts QK-DEC-023 deterministic signature insertion; the owner’s QK-DEC-021 tuple locator is corrected because QK-DEC-021 remains the D-11 incomplete-temporary-artifact rule. M15 proves only final-only in-memory non-escape plus discard-and-lock on failure; filesystem temporary-file recovery/report behavior remains later. For M15 only, the zero-incoming-signature, exactly-two-new-signatures-per-input, and original-relative-order assumptions in planned QK-TST-DIFF-002/F3.2b are narrowly superseded: every already-present valid signature is preserved byte-for-byte, only the exact missing descriptor roles needed to reach threshold are inserted, complete inputs remain untouched during a mixed batch, and final order is canonical M5/BIP-174 map order. QK-DEC-036 remains unchanged: an explicit SIGHASH_ALL record is never stripped. Canonical test-row alignment remains owed and no Gate C claim follows. | The current Core/checklist rule preserves verified existing signatures, while the M5 comparator—not arrival history—defines deterministic emitted placement. |
| 2026-08-24 | QK-DEC-076 | M15 adds one production HOST method on `ReviewBoundWorkflow` and no qk-host-model event/API change. Public input is a borrowed list of `SubmittedSignature { input_index: u32, role: DescriptorRole::{A,B,C}, der_signature: &[u8] }`; the caller supplies no pubkey, key type, raw record, sighash byte, selected pair, review hash, or token. The wrapper derives the role pubkey from the M12-proven descriptor records and appends the sole allowed `01` sighash byte. Before any allocation or mutation the method requires `SignPermitted`, the retained current cycle token, an approved M14 review/hash, exact current-token equality, and a fresh rebuild of immutable S0 whose hash equals the approved hash; any failure locks and clears the binding. Requests are normalized by ascending input index and then ascending derived compressed-pubkey bytes, which is exactly QK-DEC-023 and canonical BIP-174 partial-signature map order. Existing or pending exact signature bytes reject as `DuplicateSignature`; two distinct pending signatures for one input/role reject as `DuplicateRole`; a different response for an already-occupied role rejects as `SignatureConflict`; a request for a complete input or a wholly complete candidate rejects as `ThresholdAlreadyMet`. No input receives more than two insertions, no role receives more than one, and every incomplete input must end at threshold exactly. | Deriving placement from authenticated D and checking the live approval binding before mutation prevents caller-selected key placement, replay, or post-review substitution. |
| 2026-08-24 | QK-DEC-077 | M15 first consumes M5 to form one canonical pre-insertion baseline. It then inserts exactly one type-02 partial-signature record privately, reparses the complete candidate, reruns M6–M13 cryptographic and ownership/destination analysis, requires M5 fixed-point canonical bytes, proves the exact one-record delta with every preexisting complete key/value byte unchanged, and repeats after each record. Intermediate buffers never escape. The approved pre-insertion ReviewHash and its original S0 SHA-256 identity remain the immutable authorization anchor and are never recomputed, replaced, or re-approved. Each ordinary post-insertion M14 object necessarily has a new mechanically entailed S0 identity; it is used only as current evidence. A separate non-M14 comparison freezes the approved S0 identity and permits exactly the affected verified count, its entailed per-input threshold status, and the entailed aggregate-status changes; every other QK-DEC-070 field is byte-equal. Final emission requires every input threshold-complete, aggregate `VerifyAndExportOnly`, a final fresh parse/M13 pass, exact delta closure, and M5 idempotence; only then may one owned canonical PSBT byte vector escape. The wrapper internally emits the existing tokened `SignatureProduced`, then `SignatureVerified` and `OutputReparsed`, narrowly superseding only QK-DEC-073’s no-`SignatureProduced` clause for this method. Finalization and transaction extraction do not exist. | Separating the frozen approval anchor from mechanically changed raw identity makes the permitted delta exact without treating a post-insertion hash as a new approval. |
| 2026-08-24 | QK-DEC-078 | M15 HOST bounds are exact candidates under QK-DEC-034, not registry freezes or target claims: at most 100 inputs times two new records = 200 insertions; strict low-S DER is at most 71 bytes, stored value DER plus fixed `01` is at most 72, one encoded partial-signature record is at most `1+34+1+72=108`, and total growth is at most 21,600 bytes. Final bytes must remain within the retained InputSource cap, so worst-case eligible starting sizes are 2,075,552 bytes for microSD and 240,544 bytes for QR. Every affected map remains within 64 records and 15 signer-bearing records. Literal per-record revalidation permits at most 200 complete M13 reruns, 158,400 inherited CKDpub calls and 20,100 cumulative signature verifications; this is bounded HOST evidence only. The fixture is a second, fixture-only, permanently NEVER-FUND BIP48 descriptor lineage with its own wallet_id, generated outside Git under the QK-DEC-047 M8-F procedure using high-entropy ephemeral keys and one unique high-entropy nonce per genuine signature, with separate Python and Perl implementations agreeing byte-for-byte before all secrets, generators and intermediates are destroyed. This is the sole narrow exception to QK-DEC-059’s no-known-private-counterpart rule; the NUMS default remains mandatory everywhere non-signing. All nine committed account/child public keys and six genuine signature r values are screened against committed KAT, QK-DEC-059/064 NUMS, decoded/Rust-array/contiguous-hex corpora and small multiples. The owner’s QK-DEC-058 screening locator is corrected here because QK-DEC-058 is only the M11 count correction. The public fixture embeds complete bytes for exactly 22 cases: four emissions, fifteen named rejections and three binding rejections, including every successful intermediate PSBT, ordinary current-S0 M14 bytes and explicitly non-M14 frozen comparison bytes. No signing or fixture generator, seed/xprv/scalar/nonce, finalizer, extractor, address/UI, broadcast, media I/O, persistence, target behavior, or M16 enters Git. | One narrowly disposed signing lineage supplies externally reproducible public verification evidence without weakening the permanent NUMS default or introducing signing capability into the repository. |
| 2026-08-24 | QK-DEC-079 | QK-DEC-076's phrase “before any allocation” means before any M15 canonical-baseline, normalization, candidate, or insertion-buffer allocation. The fresh M14 rebuild necessarily performs its already-ratified bounded M6–M14 fact and transcript allocations under QK-DEC-074 after the state, retained-hash, retained-token and current-token gates, and exact rebuilt-hash equality is then required before M15 allocates or mutates a candidate. | This narrows an overbroad allocation phrase without weakening the owner-required rule that exact live approval binding precedes every insertion mutation. |
| 2026-08-24 | QK-DEC-080 | QK-DEC-078's cumulative verification count is corrected for preserved complete inputs. Let `c` inputs already be complete with the maximum three valid descriptor-role signatures, let `e` of the other `100-c` inputs carry one valid signature, let new insertions be `n=200-2c-e`, and let initial signatures be `k=3c+e`. The exact per-record verification work is `n*k+n*(n+1)/2`, maximized at `c=25,e=0,n=150,k=75` as 22,575 qk-secp verifications. The two mandatory pre-loop passes over retained S0 and the M5 baseline make the exact whole-method maximum 22,726 at `c=26,e=0,n=148,k=78`. Per-record CKDpub work remains `200*132*6=158,400`; including those two pre-loop M13 passes gives a whole-method maximum of `202*132*6=159,984`. All figures remain bounded HOST candidates only. | Existing complete inputs can contribute three preserved valid signatures to every later per-record verification pass, so exact work accounting must include that state rather than assuming every transaction starts empty. |
| 2026-08-24 | QK-DEC-081 | M16 adds exactly one final-only public operation, consuming `ThresholdCompletePsbt` and returning one opaque owner of finalized-PSBT bytes, raw witness-transaction bytes, txid and wtxid; there is no constructor from arbitrary PSBT, witness, transaction, hash or token bytes and no intermediate artifact escapes. The capability privately retains its M15 `InputSource`. Finalization reparses with that source, requires an M5 fixed point, reconstructs each witnessScript from the three M12-proven type-06 keys, and reruns M8 over every existing partial signature; when the optional type-05 record is absent it is added only to a private non-escaping verification copy. Aggregate `VerifyAndExportOnly` is required before any output allocation. Rejection consumes the capability and returns no bytes. Named precedence is: capability parse; canonical-input equality; witness-script/derivation coherence; cryptographic verification; threshold completeness; signature selection/order; checked length and cap arithmetic; exact input-map transform; finalized-PSBT reparse, fixed point and record delta; raw-transaction reparse through EOF; base-transaction and witness equality including `WitnessOrderMismatch`; txid/wtxid hashing. The stable error surface carries no attacker bytes. `qk-host-model` and the qk-psbt public surface remain unchanged; the existing private qk-psbt SHA-256 source is compiled privately at this HOST seam solely for SHA256d identifiers, exposing no general hash API. | A linear consuming capability makes successful M15 completion the only production entry while rechecking every fact available without reopening approval or exposing a general finalizer. |
| 2026-08-24 | QK-DEC-082 | Each M16 input must carry the exact M12/M15 shape: exactly three type-06 records whose distinct compressed keys and 28-byte BIP48 path values reconstruct one 105-byte `OP_2 PUSH33 K1 PUSH33 K2 PUSH33 K3 OP_3 OP_CHECKMULTISIG` witnessScript in strict BIP67 byte order; zero or one type-05 record, with a present value required to equal that reconstruction byte-for-byte; and two or three M8-verified member partial signatures. Partial-signature record order must map to strictly increasing positions in the sorted witnessScript or `WitnessOrderMismatch` rejects; exactly the first two signatures by script position are selected. The final witness is exactly four items: one zero-length dummy, those two complete DER-plus-`01` signature values in script-position order, and the reconstructed 105-byte witnessScript. The finalized input map adds exactly one empty-key-data type-08 record, has no type-07 record, removes every type-02 through type-06 record, preserves type-00/type-01 UTXOs and every other unknown/proprietary complete key and value byte-for-byte, and remains in M5 numeric-type/key-data order; global and output maps are byte-frozen. Preexisting type-07/type-08 fields, a nonmatching derivation key, duplicate/non-sorted script key, wrong stack, wrong selected signature or any other delta rejects. Reparse also requires each final witness signature to occupy the same increasing script positions or returns `WitnessOrderMismatch`. | Exact stack order is consensus-load-bearing, while a closed map transform prevents finalization from becoming a channel for unrelated PSBT mutation. |
| 2026-08-24 | QK-DEC-083 | M16 raw extraction splices the exact version, base input/output body and locktime slices from the PSBT global unsigned transaction around BIP141 marker/flag `00 01` and the exact per-input final witnesses; it never starts from a reserialized transaction. A private bounded parser then consumes the raw result through EOF, reconstructs its witness-stripped serialization, requires byte equality with the approved unsigned transaction and requires every parsed witness to equal its type-08 value before identifiers are computed. `txid` is SHA256d of the exact witness-stripped bytes and `wtxid` is SHA256d of the exact raw bytes; API arrays are the raw 32 SHA256d output bytes, while conventional RPC hex reverses them for display. Three QK-DEC-034 HOST candidates are const-derived and const-asserted: maximum witness serialization per input `254 = 1+1+2*(1+72)+1+105`; maximum type-08 record `259 = 1+1+3+254`; and maximum raw transaction `30,937 = 5,535+2+100*254`. A valid M15 artifact's finalized PSBT is at least 149 bytes smaller per input because the conservative cancellation `3*64 + 2*36 - (2+3+110) = 149` counts the mandatory three derivation records and two signature record frames against the final record while deliberately ignoring removed witnessScript, optional sighash/redeemScript and any third signature. No registry value or production allocation claim is frozen. | Reparse-and-rebind of the exact extracted bytes makes both transaction identifiers and the witness/base relationship consequences of the approved immutable transaction rather than serializer assumptions. |
| 2026-08-24 | QK-DEC-084 | The M16 Bitcoin Core differential suite lives only under `differential/m16-bitcoin-core/`, outside the Cargo workspace and `tools/check.sh`, and is invoked separately. It accepts only the SOURCE-REGISTER-pinned Bitcoin Core v28.0.0 binaries after exact archive, binary and version checks; no release archive, binary, data directory, cookie or wallet is committed. Unknown-free cases require byte equality with Core `finalizepsbt` and exact raw transaction equality. Each unknown-bearing case contains minimally encoded numeric type 255 key `fdff00` and type 256 key `fd0001`: QuietKey emits numeric order 255 then 256, while Core emits raw-key order 256 then 255. That nonempty reproducible order-only delta is expected, never a failure; decoded fields, extracted transaction, record scope/count, complete keys and values remain equal, and every unknown/proprietary value is preserved byte-for-byte. A fresh isolated regtest run imports the committed deterministic seed block, mines only maturity blocks and requires `testmempoolaccept` `allowed=true` for positives plus named rejection for mutations; a normalized committed transcript records every command class, source/fixture/harness hash and result so the procedure can be rerun. Raw transactions are network-independent: testing mainnet-profile objects on regtest widens no product code, and regtest exists only in this harness. The external signing corpus follows the full QK-DEC-047 destruction procedure, uses high-entropy ephemeral scalars and a unique high-entropy nonce per signature with two separately written implementations agreeing byte-for-byte, screens all new public keys and signature r values under QK-DEC-059/QK-DEC-064/QK-DEC-078, and is a distinct permanently NEVER-FUND mainnet lineage; this corrects the owner's QK-DEC-058 locator because that row is only the M11 count correction. Regtest coins are valueless by construction. M16 adds no broadcast, signing/generation capability, wallet/RPC/network product code, arbitrary/legacy/wrapped/Taproot spend path, finalization beyond native P2WSH sorted 2-of-3, media I/O, persistence, UI, warning/amount/fee-range policy, target claim, Gate closure or M17 work. | A version-locked external oracle tests both serialization agreement and node acceptance without importing Core, regtest or signing authority into the product boundary. |
| 2026-08-25 | QK-DEC-085 | M17 narrowly amends QK-DEC-026 by Owner decision: the A1/A2 segment is reactivated and production A1 work proceeds in this repository under every standing rule. QuietKey-Protocol-Lab is re-scoped solely as the physical evidence bench, writable only for capture-corpus custody and reader research under the same evidence and provenance rules. No code migrates from the bench into this repository or from this repository into the bench while OD-08 remains open; all other legacy/reference material remains non-authoritative. | Reactivating one product lane while fixing a one-way authority boundary permits current A1 work without turning the former laboratory into a second implementation tree. |
| 2026-08-25 | QK-DEC-086 | The launch A1 decoy-template policy permits exactly two formats: recipe and travel tips. Decoy text comes only from a design-time, human-reviewed, frozen template library; offline LLM assistance is permitted during development authoring only, no generation of any kind occurs at provisioning time, and every page is fully self-contained under Core Architecture v1 section 5.2. M17 does not implement the library, authoring workflow, templates or rendering. | Frozen self-contained human-reviewed text removes runtime generation and network dependence while leaving the visual implementation to its later milestone. |
| 2026-08-25 | QK-DEC-087 | The A1 codec lane's acceptance measure is page-level recovery probability under a bounded multi-capture retry model, not per-token or per-symbol decode rate. Token/symbol outcomes may be retained only as diagnostics; the retry bound, corpus, thresholds and final physical parameters remain M18/Gate-A work under OD-04. | A user succeeds by recovering the page within bounded captures, so the acceptance unit must match that recovery event. |
| 2026-08-25 | QK-DEC-088 | The located spike-capture custody set remains byte-untouched at QuietKey-Protocol-Lab commit `aec25bb6ab30790faf3cbf55169a3b4777572d6c`, path `artifacts/cloakvault/spike/captures/`. The canonical set is exactly 29 unique JPEG originals totaling 59,148,370 bytes; its sorted 29-line entry block has SHA-256 `bdb0cd18c5f752546b1239cbd739b272347f1311410ea22d9e5ac1330b4d4174`. This repository records the hashes, not capture bytes, in `docs/evidence/M17-A1-SPIKE-CAPTURES.sha256`, 4,061 bytes, 49 LF-terminated lines, SHA-256 `b32cb252412099f33950e63c56ae2ca986e752ccab8c36df373215d2a23fc6d1`. Three files remain in the frozen bench tree but are excluded from the 29: byte-identical duplicate `captures/baseline-0-std-S01..jpeg`, SHA-256 `c8064f8ce898bcbfb64d8b52be3b361f662254e1781009fdf3ebb267f3dd23d3`; source-sheet `captures/CloakVault capture-spike sheet v1.pdf`, SHA-256 `1cf45a7e32997ee480e4cdf824d4f7c80c513bdb3f26a4c4f59805d93d8f43b9`; and verdict `captures/QuietKey_GateA_Spike_Verdict.pdf`, SHA-256 `0df86918b4852d9848c56236a497d010f30292beb6b4c6f2bf714e34a840e210`. Nothing is deleted or moved. | A commit-pinned manifest gives the product repository durable custody provenance while the original media stays only on the re-scoped evidence bench. |
| 2026-08-25 | QK-DEC-089 | M17 adds HOST-reference crate `qk-a1` implementing the Core Architecture v1 section 5.1 capsule in-repo: SHA-256 per FIPS 180-4, HMAC-SHA256 with the 64-byte SHA-256 block, HKDF-SHA256 per RFC 5869, and ChaCha20-Poly1305 IETF AEAD per RFC 8439. This is HOST reference evidence only; the production core's reviewed constant-time implementation is a separate Gate-C obligation that M17 does not discharge. The document key is exactly `HKDF-Extract(salt=wallet_id, IKM=A2)` followed by `HKDF-Expand(PRK, info=ASCII "QuietKey/A1/v1", L=32)`. The caller supplies the 96-bit nonce and no M17 API generates randomness. The canonical storage/interchange capsule is exactly 67 bytes: magic `QKA1` = `51 4b 41 31` at `[0,4)`, coding version `01` at byte 4, crypto version `01` at byte 5, mainnet network `01` at byte 6, nonce `[7,19)`, 32-byte ciphertext `[19,51)`, and 16-byte tag `[51,67)`. AAD is exactly 39 bytes: the complete seven-byte header followed by the full 32-byte wallet_id. Whether M18 prints the constant header verbatim or strips it before codec encoding and has the reader reconstruct it is deliberately open because a verbatim header gives every printed token the same recognizable prefix; M18 must resolve that question against the disguise threat model without changing the canonical capsule. The entire public surface is `A1Error::{InvalidCapsuleLength, InvalidMagic, UnsupportedCodingVersion, UnsupportedCryptoVersion, UnsupportedNetwork, AuthenticationFailed}`, `encrypt(&[u8;32], &[u8;32], &[u8;12], &[u8;32]) -> [u8;67]`, and `decrypt(&[u8;32], &[u8;32], &[u8], &mut [u8;32]) -> Result<(),A1Error>`; errors follow that listed precedence, no general hash/HMAC/HKDF/AEAD API is public, and decrypt leaves its output unchanged unless tag verification succeeds. Existing NIST SHA-256 imports retain exact 65-short, 64-long and 100-Monte count assertions; M17 imports exactly 225 NIST HMAC-SHA256 cases including shorter-than, equal-to and longer-than-blocksize keys, three RFC 5869 SHA-256 cases, two RFC 8439 AEAD datasets, and all 325 pinned Wycheproof ChaCha20-Poly1305 cases with exact parsed-count and verdict assertions. Capsule fixtures are generated outside Git under QK-DEC-047 by two separately written implementations agreeing byte-for-byte, using only patterned obviously-public A2 and Seed-A bytes, fixed public nonces, and the M11 GOLDEN NUMS-lineage wallet_id `a79890dcde9c8517ad860ab3a907ff01652bbe200a561028ceb66e22e8c9d543`; temporary procedures and intermediates do not enter Git. Excluded from M17 are the print codec, Reed-Solomon, base32 alphabet, reader, templates and rendering implementation, entropy, provisioning, nonce generation, media I/O, and every production constant-time, target or Gate claim. | Freezing a byte-exact, domain-bound capsule and closed failure surface before the physical codec separates deterministic crypto correctness from later disguise and capture performance decisions. |
| 2026-08-25 | QK-DEC-090 | M18 resolves QK-DEC-089's print question by stripping exactly the seven fixed header bytes from the print representation and encoding only `capsule[7..67]`, the 60-byte nonce/ciphertext/tag body. The 67-byte QK-DEC-089 form remains the sole canonical storage/interchange capsule. Decode reconstructs exactly `51 4b 41 31 01 01 01` before returning a capsule candidate, thereby fixing coding version, crypto version and network to the ratified v1 mainnet constants. Any future capsule version requires an explicit codec-level discrimination decision; no implementation may infer, negotiate or silently substitute those bytes. The exact case-sensitive print alphabet in index order is lowercase ASCII `23456789abcdefghijkmnpqrstuvwxyz`. RS bytes are packed MSB-first into five-bit symbols; an incomplete final symbol has zero low-order pad bits, and decode rejects nonzero unused bits. There is no trimming, case folding, aliasing, Unicode, punctuation acceptance or normalization. M18 adds no checksum or URL-like wrapper. | Removing the constant prefix improves the disguise geometry while exact reconstruction preserves QK-DEC-089 authentication semantics and makes version evolution an explicit format decision. |
| 2026-08-25 | QK-DEC-091 | M18 ratifies exactly three calibration profiles and no production default: systematic shortened RS(72,60), RS(76,60) and RS(80,60) over GF(256), with primitive polynomial `x^8+x^4+x^3+x^2+1` encoded as `0x11d`, primitive element `alpha=0x02`, and consecutive generator roots beginning at `alpha^0`. For parity count `r` in `{12,16,20}`, the parent is RS(255,255-r); shortening prepends exactly `(255-r)-60` zero message bytes, encodes systematically, then omits those leading zero symbols while retaining the 60 data bytes followed by all `r` parity bytes. The corresponding lowercase-base32 lengths are exactly 116, 122 and 128 symbols, with four, two and zero unused low-order pad bits. Their RS-byte correction conditions are respectively `2e+s <= 12`, `2e+s <= 16` and `2e+s <= 20`. Every API call names a profile explicitly. The family is calibration input only: OD-04, the final profile, footer geometry, retry bound, thresholds and every QK-LIM-A1 value remain open. | A closed candidate family measures length against correction margin without prematurely freezing a physical profile. |
| 2026-08-25 | QK-DEC-092 | M18 decode input is an exact-length sequence of either one known alphabet symbol or an explicit erasure at each position. Known symbols pack MSB-first into the selected RS byte string. Every RS byte whose eight-bit span overlaps any erased five-bit symbol is marked erased; the resulting byte positions are deduplicated and their count is checked against the selected parity capacity before decoding begins. The decoder performs full bounded error-and-erasure correction, accepts only one codeword satisfying the selected profile, and enforces `2e+s<=r` for the actual corrected error count `e` and deduplicated erasure count `s`. `MalformedSymbol`, `OverCapacity` and `NoUniqueCodeword` are distinct named rejections; exact symbol count, canonical pad bits and malformed erasure representation have their own named rejections and precede RS work. Failure leaves the output unchanged. A successful RS result is only a reconstructed 67-byte capsule candidate: capsule authentication remains exclusively in `qk-a1`, and any capsule-layer rejection is returned as that layer's rejection and is never retried or reinterpreted by RS. | Erasure geometry follows the actual bit overlap, while unique bounded correction and a separate AEAD gate prevent OCR uncertainty or parity from becoming an authenticity claim. |
| 2026-08-25 | QK-DEC-093 | M18 adds dependency-free HOST-reference crate `qk-a1-codec` with fixed-memory candidate encode/decode only; it exposes no general GF(256), Reed-Solomon or base32 primitive, performs no direct allocation, I/O, network access, randomness, rendering or OCR, and makes no target or Gate claim. Under QK-DEC-047, two separately written outside-Git implementations re-encode the exact 29 QK-DEC-088 spike cases under QK-DEC-090 and all three QK-DEC-091 profiles and must agree byte-for-byte before exact constructed inputs, outputs, manifest hashes and page-level bounded-retry calibration facts are committed. The 29 original JPEGs and three exclusions remain untouched in the evidence bench; neither media nor code migrates between repositories under QK-DEC-085. Re-encoding the earlier corpus is calibration input only and cannot select a production profile or stand in for new physical captures using the ratified alphabet. Excluded are a checksum, URL framing, template library, rendering, reader/OCR/camera, entropy, provisioning, nonce generation, media I/O, final profile or threshold selection, production constant-time claims and target claims. | Separating deterministic codec geometry from later physical trials permits reproducible comparison without treating old captures or HOST execution as final format evidence. |
| 2026-08-25 | QK-DEC-094 | M19 is solely a preregistered, outcome-blind, two-phase physical print/capture corpus and custody milestone; it selects no production parameter and changes no product or reader code. This row commit must be pushed before any M19 rendering, printing, damage application or capture; later Phase 1, Phase 2 and aggregate manifests are separate conventional-prefix evidence commits under this one frozen protocol, and no schedule fact may be changed from observed pixels or results. The only payload inputs are the exact 18 QK-DEC-093 payloads at product source commit `8d321f318ff9b6cdc1065407b26ada447e219215`: lineages T0 through T5 under RS(72,60), RS(76,60) and RS(80,60), with exact 116-, 122- and 128-symbol strings; no payload is regenerated, normalized, wrapped as data, separated or changed. Exactly two locally composed ASCII decoy bodies form the QK-DEC-086 human-reviewed frozen M19 library; each displayed `\n` below expands to one byte `0a`, the final displayed escape is a final LF, and no other bytes exist. `RECIPE_BODY` is 1,112 bytes, 28 LF, SHA-256 `ec51fb35a729d2c29bcae039209994bdf5fcd20fb92eb886155a8b4cfde929b9`, and is exactly `ROASTED TOMATO AND WHITE BEAN STEW\nA simple supper for four\n\nINGREDIENTS\n2 tablespoons olive oil\n1 medium yellow onion, finely chopped\n2 garlic cloves, minced\n1 teaspoon smoked paprika\n1/2 teaspoon dried thyme\n2 cans (400 g each) white beans, drained and rinsed\n1 can (400 g) chopped tomatoes\n250 ml vegetable stock\n1 tablespoon tomato paste\n1 teaspoon red wine vinegar\nSalt and black pepper\nA small handful of flat-leaf parsley\n\nMETHOD\n1. Warm the olive oil in a wide saucepan over medium heat.\n2. Add the onion and a small pinch of salt. Cook for 8 minutes, stirring occasionally, until soft.\n3. Add the garlic, paprika, and thyme. Stir for 30 seconds.\n4. Add the beans, tomatoes, stock, and tomato paste. Bring to a gentle simmer.\n5. Cook uncovered for 20 minutes, stirring twice, until the sauce is thick but still spoonable.\n6. Stir in the vinegar. Season with salt and black pepper.\n7. Divide among four warm bowls and finish with parsley.\n\nSERVING NOTE\nServe with toasted bread or boiled potatoes. Cool leftovers promptly, cover, and refrigerate for up to two days. Reheat once until steaming throughout.\n`. `TRAVEL_BODY` is 1,417 bytes, 20 LF, SHA-256 `69745a655c87647f32b86c832ad40a3f1903e4510125ad1a272651cb37050230`, and is exactly `A QUIET DAY BY TRAIN\nPractical notes for an unhurried city visit\n\nBEFORE LEAVING\nCheck the outbound and return times the evening before. Save the timetable offline and write the final return time on paper. Pack a refillable bottle, a light layer, a small umbrella, and one simple snack. Wear shoes that remain comfortable on stone streets.\n\nON ARRIVAL\nLeave the station by the main exit and pause before choosing a direction. Note the station name and the street used for the return. Walk the first ten minutes without a fixed list; this makes the scale of the centre easier to understand.\n\nMORNING\nChoose one museum, market, or historic building and give it a full hour. Arrive near opening time. Keep the rest of the morning flexible, and stop for coffee somewhere with visible prices and room to sit.\n\nMIDDAY\nEat before the busiest lunch period if possible. A bakery, soup counter, or small cafe is usually faster than a formal meal. Refill the water bottle and check the return platform only after lunch.\n\nAFTERNOON\nTake one longer walk along a river, park, or residential street. Turn back while there is still generous time. Aim to reach the station twenty minutes before departure.\n\nUSEFUL HABITS\nKeep tickets and identification in the same secure pocket. Photograph no private documents. Carry out litter, respect quiet areas, and leave gates exactly as found. If plans change, choose the simpler route home.\n`. Every master is fully self-contained A4 portrait at 100% Actual Size with 16 mm margins; body text is exact unmodified Liberation Fonts 2.1.5 `LiberationSerif-Regular.ttf`, 393,576 bytes, SHA-256 `058ea80864aef09a23f45cbec2bb5400bc3dfbdea01c3f10538a21fcb497fb74`, at 10.5 pt and line height 1.3; the footer is exact unmodified `LiberationMono-Regular.ttf`, 319,508 bytes, SHA-256 `f2b83c763e8afd21709333370bed4774337fae82267937e2b5aea7e2fbd922c1`, at 7.5 pt and line height 1.3 with ligatures disabled and no fallback. The official 2.1.5 tag resolves to commit `4b0192046158094654e865245832c66d2104219e`; official TTF archive is 2,385,008 bytes with locally computed retrieval SHA-256 `7191c669bf38899f73a2094ed00f7b800553364f90e2637010a69c0e268f25d0`, and tagged OFL-1.1 `LICENSE` is 4,414 bytes, SHA-256 `93fed46019c38bbe566b479d22148e2e8a1e85ada614accb0211c37b2c61c19b`, Git blob `aba73e8a403084a93a245ca00e4a0db007886e0a`; the complete exact TTFs and license accompany the bench print inputs, every self-contained rendered master embeds the pinned full fonts, font readiness is required before rendering, and every rendered master is hashed; before any font byte enters the bench, the source pin and OFL facts are published in SOURCE-REGISTER, while no font binary enters this repository. The footer is fixed at left 16 mm, right 16 mm and bottom 16 mm and contains only the exact payload, with a presentation-only visual break after symbol 64, hence line lengths 64+52, 64+58 and 64+64; QK-DEC-090's exact token remains one 116-, 122- or 128-symbol sequence with no separator, wrapper or normalization. The 64-symbol first line is exactly 101.6165 mm in the pinned mono font with ruler acceptance `101.6165 +/- 1.0 mm` on the first page of every print batch. With A4 top-left origin in millimetres the footer line-box zone is `F = x[16,117.6165], y[274.12,281.00]` and the fixed rounded challenge point is `C=(67,277.5)`, invariant across payloads. Printing uses the Owner-supplied 27-inch 2019 iMac on macOS 15.7.7, Adobe Acrobat Actual Size 100%, Samsung Xpress M2026W firmware V3.00.01.15 with ECO and toner-save off, and two sealed 500-sheet Double A A4 80 gsm reams from one manufacturing lot because the 522 corpus pages cannot fit one ream; scrap and preflight sheets come from ream 1 and are counted, printer page counter is recorded per batch, and batch-level ream mapping remains visible. At first use the applicable manifest records and freezes across both phases the master-generation application/version, Acrobat version, identified operator, VARDERA variant and measured cup-foot diameter, Nescafe Gold variant/lot, coffee scale, 200 mL and 15 mL measuring tools, kettle, one constant sandpaper grit, eraser/blade/syringe identities, ruler/mat/cutter identities, both ream package/lot identities, and locate objects photographed at setup; if lamp glare is used it likewise freezes lamp/bulb identity, setting, distance and angle. iOS version is recorded from EXIF per capture and its first value is frozen across both phases. None of these record-at-use facts blocks this row; any later identity change is a procedural deviation. Capture uses one iPhone 16 Pro Max, stock Camera, main 48 MP Fusion camera at 1x, 4:3 24 MP JPEG under Most Compatible, macro auto-switch, zoom, flash, Night mode, Live Photos and ProRAW off; the operator stands at the fixed table with elbows braced and portrait framing. Fill is primary over nominal distance: beginning near 35 cm, with roughly 23 cm expected, the scrap preflight uses live view without zoom to require all four page corners and the complete perimeter in frame and page height `85 +/- 3%` of the oriented image; it also confirms main-camera focus on footer glyphs. If focus is marginal, the preflight fixes fill once to the `75..80%` branch rather than enabling macro. The selected branch, achieved lens-to-page distance, stance/page marks, resolution, orientation and focal fields are recorded and then reused. Standard lighting is indirect daylight in the same room and position during 13:00..15:00 local with curtains fully open; dim uses the same setup with curtains fully drawn and electric lights off. Glare uses sessions no longer than 20 minutes under either source. With direct sun, at each session start the page is positioned so a specular band crosses F, session start/end and position are recorded, and solar motion permits repositioning only at the next session start. If direct sun is unavailable, a lamp is permitted; its first-use lamp/bulb identity, setting, distance, angle and page position are recorded and frozen for subsequent lamp sessions. For either source, equivalent means only that the scrap preflight shows one continuous specular band crossing both line boxes of F; this is recorded before scheduled captures and no token result is inspected. EXIF is recorded per capture and never gates acceptance. Reference expectations only are standard ISO 50..250 and exposure 1/250..1/100 s, dim ISO 800..3200 and 1/60..1/15 s, and glare ISO 25..100 and 1/2000..1/500 s; these bands trigger no relabel, exclusion or retake. Every damage level uses a fresh page, is never cumulative, targets F/C exactly where specified, and stops at its fixed input count rather than observed appearance. One recorded operator performs every hand-applied coffee, water, scuff, fade, scratch, fold, edge and crumple action across both phases; an operator change is a procedural deviation. Coffee uses 7.0 g Nescafe Gold in 200 mL tap water added 60 s after kettle automatic cutoff, stirred 15 s and stood 5 min; for each page 15 mL is placed in the VARDERA saucer. Every contact repeats exactly: wet the cup foot for 5 s, drip over the saucer for 5 s, then contact C for 45 s. Levels 1/2/3 use 1/3/6 sequential contacts at C, the 30-minute flat air-dry timer starts after the final contact, there is no blotting or heat, and capture begins within two hours after drying completes. Water uses room-temperature tap water through the record-at-use 5 mL needleless oral syringe: levels 1/2/3 deliver 0.5/1.0/2.0 mL at C over 5 s, then air-dry flat 30 minutes without blotting or heat. Scuff uses a fresh 30x30 mm coupon of the one record-at-use grit per page and single-operator hand consistency: one pass is a 40 mm left-to-right stroke across F followed by lifting for return, and levels 1/2/3 use 5/15/30 passes with no result check, brushing or extra pass. Fade uses a new record-at-use HB pencil with attached eraser per page, first flattened by 20 strokes on counted ream-1 scrap; one cycle is a 40 mm left-right-left rub across F at nominal 45 degrees under the same operator's hand pressure, levels 1/2/3 use 50/150/300 cycles tallied in groups of 25, and every 50 cycles the eraser receives exactly five cleaning strokes on counted scrap, never an appearance endpoint. Scratch uses a fresh record-at-use box-cutter blade per page, one indexed notch extension, a metal ruler and cutting mat: level 1 is one 40 mm single-pass score across F, level 2 is three parallel single-pass scores 5 mm apart, and level 3 adds three perpendicular single-pass scores for six total; angle is nominally 30 degrees with ordinary single-operator one-hand pressure, no line receives a second pass, and the blade is immediately retracted. Fold uses the vertical line x=67 mm with print inward; one cycle closes the fold fully, runs the same thumb top-to-bottom exactly twice and unfolds flat, levels 1/2/3 use 1/5/20 cycles, then rest flat 30 minutes without pressing or ironing. Edge uses ruler, mat and cutter to remove one 20 mm-wide rectangular bottom-edge notch centered at x=67 mm with depth 17/22/27 mm for levels 1/2/3; top cuts y=280/275/270 intersect, within notch width x[57,77], respectively 1.00 mm, 6.00 mm and the complete 6.88 mm of F's vertical span, deliberately forming the recoverable, borderline and beyond-cliff ladder. The removed piece remains with the page and is never reattached or trimmed. Crumple level 1 brings all four corners into both hands, crumples continuously for 10 s once and unfolds only by corners; level 2 uses three 30 s cycles, unfolds by corners and rotates 90 degrees clockwise between cycles; both rest flat under their own weight for 30 minutes with no smoothing and no size or appearance endpoint. Locate is a source class, not damage: level 0 is a fresh undamaged page flat and centered on the bare table with the phone roughly normal; level 1 is a fresh undamaged page rotated 15 degrees clockwise, with the record-at-use book, mug, pen and keys on photographed painter-tape marks 50 mm outside its edges and never occluding it, and the phone nominally 20 degrees off normal. Unless a class states a later window, mechanical-treatment capture begins within two hours of completion. Common safety is hot liquid kept stable on its saucer and away from electronics, liquid work over a tray, eye protection for abrasion/cutting, cutting away from the body on a mat, and blade retraction between actions. The cell set and ordinals are exactly QK-DEC-088 manifest order: c00 baseline-0-dim-S01, c01 baseline-0-glare-S02, c02 baseline-0-std-S01, c03 baseline-0-std-S02, c04..c06 coffee-1/2/3-S03/S04/S05, c07..c08 crumple-1/2-S21/S22, c09..c11 edge-1/2/3-S23/S24/S25, c12..c14 fade-1/2/3-S12/S13/S14, c15..c17 fold-1/2/3-S18/S19/S20, c18..c19 locate-0/1-S26/S27, c20..c22 scratch-1/2/3-S15/S16/S17, c23..c25 scuff-1/2/3-S09/S10/S11, and c26..c28 water-1/2/3-S06/S07/S08. The inherited names are informative labels only; these operations define M19, and locate remains distinct from physical damage. Let lineage `l=0..5` denote T0..T5, profile `p=0,1,2` denote RS(72,60), RS(76,60), RS(80,60), payload ordinal `q=3*l+p`, and fixed cell ordinal `c=0..28`; print the Recipe master exactly when `(q+c) mod 2=0`, otherwise Travel, with c never changed by completion order. Each cell therefore has all 18 payloads exactly once, nine Recipe and nine Travel, six pages per profile, 18 pages and 54 scheduled captures. This is a fractional design: each payload-cell pair exists in one template only, rather than the 1,044-page full cross, so template effects are estimated across pairs, while profile-template balance is exact within every cell at three Recipe and three Travel pages per profile. Phase 1 is fixed to T0 and T3 across all profiles: 12 masters, six pages per cell, 174 pages, 87 pages per template, 58 per profile and exactly three scheduled captures per page for 522 captures. Before Phase 2 the Owner confirmation line is exactly `Owner checkpoint: masters printed correctly, damage recipes behaved as written, apparatus stable, captures usable.` This checkpoint is procedural only, has no decode or outcome criterion, and any procedural deviation stops Phase 2 and returns to planning. Phase 2 is already fixed to T1, T2, T4 and T5: 24 masters, 12 pages per cell, 348 pages, 174 per template, 116 per profile and 1,044 scheduled captures. Full M19 is exactly 36 masters, 522 pages, 1,566 scheduled canonical captures, 261 pages/783 captures per template, 174/522 per profile, 87/261 per lineage, 29/87 per payload, and lighting split 1,458 standard, 54 dim and 54 glare. Phase 1 and Phase 2 are one third and two thirds of every aggregate, per-template and per-profile count; 18 masters occur on 15 pages and 18 on 14, split 6/6 in Phase 1 and 12/12 in Phase 2. The three scheduled captures are a bench capture-attempt count only, not a product retry bound, order or QK-LIM value; OD-04 remains fully open for profile, thresholds, product retry bound/order, production font and footer geometry. For each scheduled capture the page is fully removed to a staging surface, the phone is lowered to waist height for 5 s, the page is reseated, framing is reacquired, 2 s elapse, and the shutter is triggered. If an image is obviously blurred, cuts off any page edge or shows the wrong cell, it is preserved, hashed and logged as excluded and the same scheduled slot is repeated immediately; the first image without one of those three faults is the sole canonical capture and no later image is taken for that slot. No EXIF expectation triggers a retake, and the canonical corpus remains exactly three scheduled captures per page. Except for that exact retake rule, any wrong master/page/material/count/time/placement, print jam or misprint, unplanned spill/contact/tear/cut, setting/identity/operator change, missing or corrupt file, equipment movement/failure or safety issue stops the current phase. The expected slot and facts become a tombstone; every waste, misprint, accidental or extra artifact is retained, hashed and excluded; nothing is deleted, and no replacement, reprint, re-treatment, renumbering or slot reuse occurs under this schedule without new Owner direction. Per page the manifests record print, damage-start, damage-end, drying-complete and capture timestamps, phase, cell, payload, profile, template, page/position, scheduled-attempt identity, every retake/deviation, record-at-use facts, print-input hashes and original-capture hashes. For each phase the bench manifest commit is first and pins the payload source commit plus the QK-DEC-094 commit; the later product phase manifest commit reproduces its inventory entry block and pins the exact bench commit SHA; the final product aggregate commit pins both bench phase commits and both product phase-manifest commits. This acyclic chain is the required both-end manifest record and makes the inter-phase batch variable visible; only text manifests and SOURCE-REGISTER rows enter this repository, while all masters, setup photographs, scrap/preflight images, pages and capture media remain solely in QuietKey-Protocol-Lab under QK-DEC-085, with the prior 29 originals and three exclusions untouched and no code moving in either direction. No decoder of any kind, including the legacy reader, OCR, RS decode or capsule authentication, runs against fresh captures in M19; the report is inventory only and records zero decode outcomes and zero success, failure, confidence, correction or recovery labels. The iPhone corpus is calibration input only and cannot close Gate A. Excluded are Reader v0.2 or legacy-reader execution, OCR tuning, profile/default/threshold selection, page-level recovery calculation, launch-template or production-font/footer selection, product rendering, provisioning, entropy or nonce generation, production media I/O, target claims, Gate claims and any product-code or test change. Because M19 is a corpus-evidence milestone, the AGENTS.md seven-of-ten source/test commit floor is waived only from the QK-DEC-094 commit through the final M19 manifest commit; every other standing rule remains in force. | Freezing the exact balanced pages, household procedures, phase boundary and custody before fresh pixels exist prevents reader feedback from shaping the corpus while keeping every production codec, retry and Gate decision open. |
| 2026-08-25 | QK-DEC-095 | M19 Phase 1 paper custody and pre-distinguishing-event print acceptance supplement QK-DEC-094 only as follows. Exactly 174 externally labelled sleeves are keyed one-to-one to the fixed page ID and position; labels exist only on sleeves and stations, nothing is ever written on a page, and page identity is never established by reading page content. At most one page is in transit at any time; a page outside its sleeve is either in transit or resting at a treatment, drying or capture station physically paired with its sleeve label, and every drying station holds exactly one page. A page's first distinguishing event is its first damage action or, for an undamaged baseline or locate page, its first canonical capture. Before that event, pages printed from the same exact master are fungible: an identity ambiguity confined to such pages may be resolved by arbitrary reassignment among their scheduled page IDs and positions when the reassignment is recorded, and this is not a procedural deviation; cross-master reassignment is prohibited. At and after the first distinguishing event, fungibility and reassignment end, and any ambiguity follows QK-DEC-094's unchanged phase-stop and tombstone rule. Before the first distinguishing event, each page is measured with the ruler from the bottom paper edge to the printed footer's lower line-box bottom at x=67 mm and from the left paper edge to the first-line start. Both nominal offsets are 16.0 mm and each is accepted only within 2.0 mm of nominal, equivalently the closed interval 14.0..18.0 mm. Every damage class except edge retains QK-DEC-094's nominal C unchanged because its applied feature is large relative to the accepted 2.0 mm print shift. For edge levels 1, 2 and 3, the 20 mm-wide notch remains centered at nominal x=67 mm, and actual cut depth is respectively 17, 22 or 27 mm plus the measured bottom-to-footer offset minus 16.0 mm; this preserves the frozen intersections relative to the printed footer of 1.00 mm, 6.00 mm and the complete 6.88 mm span. The measured y-offset and adjusted cut depth are recorded, and ruler precision of approximately 0.5 mm is accepted and recorded. Before the first distinguishing event only, an out-of-tolerance, QK-DEC-094 scale-check-failed or visibly defective sheet is retained, hashed, excluded and tombstoned, and a like-for-like replacement is printed from the same exact master and recorded. This narrow outcome-blind exception neither stops the phase nor authorizes schedule renumbering, cross-master substitution or any post-distinguishing replacement; discovery at or after the first distinguishing event follows QK-DEC-094's unchanged stop regime. The Phase 1 manifest records both offsets and every replacement per page. All other QK-DEC-094 protocol, custody, no-result, retake, deviation and exclusion terms remain unchanged. | External one-page custody prevents mix-ups without marking corpus pages, while pre-outcome same-master fungibility, measured registration and vertical edge compensation preserve the frozen schedule and footer intersections without adapting to reader results. |
| 2026-08-25 | QK-DEC-096 | The QK2 authority import is the exact Owner-supplied set under `docs/qk2/`: the twelve PDFs at manifest filenames `01` through `12`, `MANIFEST.md` and `SHA256SUMS` verbatim, plus the LEA-local Core Architecture v1 Markdown as `00-core-architecture-v1.md`. All twelve packaged PDF hashes must reproduce `SHA256SUMS` before commit; any mismatch stops the import. The imported `00` file is 56,152 bytes with SHA-256 `0fc1f35afaecc0cf79f05fa8d2797282a4e4b828a1267ea878e7f6e6a7e9a8e3`. Authority follows the imported manifest: `00` is supreme for system and security architecture; `01` through `10` are subordinate elaborations, with `06` governing the PSBT signing pipeline; `12` governs prototype hardware except its listed superseded residue, namely the "2-of-2 remains binding" basis line, bech32 footer-token target, 99-digit dice test and KMAC-related Gate B rows; and `11` is historical except QK2-04 section 3.2 Gate A methodology. `MANIFEST.md` and `SHA256SUMS` are byte-preserved navigation and integrity carriers, not additional authorities; no imported byte is normalized or rewritten. | Committing the Owner-held bytes and their precedence in one stable path makes the governing document set locally complete without allowing subordinate or historical text to override Core Architecture v1. |
| 2026-08-25 | QK-DEC-097 | A narrow exception to QK-DEC-094's no-decode rule permits only three designated non-corpus scrap sheets printed from the existing Phase 1 T0 masters, exactly one under each QK-DEC-091 profile and counted from ream 1. The Owner photographs each scrap only as a record, transcribes its exact 116-, 122- or 128-symbol footer by eye into the fixture-only HOST smoke harness, and marks every human-uncertain position as an explicit erasure, never a guess. The harness uses only the committed public, patterned, permanently NEVER-FUND T0 A2 and GOLDEN NUMS wallet_id values, reports the RS result and capsule AEAD verification result, and exposes no reader, OCR, image, camera, generation or plaintext-output path. Success requires all three transcriptions to authenticate; any failure stops and returns to the Owner. Scrap results change no QK-DEC-094/095 schedule, profile, threshold, capture, retry, corpus or Gate fact, and this exception never permits decoding a scheduled page or fresh M19 capture. | A closed manual-transcription check can catch print-to-authentication setup errors before the corpus clock starts without observing or adapting the preregistered corpus. |
| 2026-08-25 | QK-DEC-098 | M20 is the active paper-only Gate B card-feasibility milestone while Owner-side M19 physical work remains active. Its capability matrix is anchored one-to-one to QK-TST-BENCH-002's seven future exact-card assertions: (a) secp256k1 signing correctness and performance; (b) derivation behavior for the least-authority payload; (c) on-card RNG characterization; (d) APDU protocol behavior including byte layouts; (e) write atomicity and power-cut behavior; (f) storage endurance; and (g) independent B/C signing keys with the same A2/D. The matrix also records atomic provisioning, A2 storage/export, fixed-role and `wallet_id`/D binding, commodity CCID reachability, timing, interfaces and unresolved bench work. The candidate shortlist records dated availability, region and currency, single-unit cost, exact model/SKU, and source-linked uncertainties. Every cell is classified as direct documentation, vendor claim, inference, or unresolved; a paper result never substitutes for a QK-TST-BENCH-002 measurement record. M20 selects no production card, resolves no part of OD-02, executes no experiment or canonical test, and closes no Gate. Purchases, vendor commitments, applet or APDU code, protocol freezing, run registration, physical measurements and production-card selection are excluded. | Aligning the paper survey with the eventual seven-assertion oracle exposes missing evidence early while preserving the exact-card decision and every physical claim for later specimen work. |
| 2026-08-25 | QK-DEC-099 | The M20 paper shortlist adds an exact contact-only search for the C1 platform class and equivalent C2/C3 paths. A qualifying candidate must be a dated, currently purchasable exact model/SKU whose registered sources positively establish ISO-7816 contact operation and the absence of ISO-14443/contactless hardware in that delivered variant; seller silence, a generic family name, software disabling, applet inaccessibility or an unverified order-code inference does not establish contact-only status. Where no qualifying variant is located, the matrix and SOURCE-REGISTER must instead name the catalogs, regions and retrieval date searched and record only the bounded finding that no exact purchasable contact-only variant was located there. For a qualifying exact card, the absence of a radio interface deletes the ISO-14443 attack surface and the corresponding contactless-exclusion condition while leaving every QK-TST-BENCH-002 assertion and other exact-card obligation unchanged. Availability is a dated paper fact, not a production selection or durability claim. M20 still makes no purchase or vendor contact, resolves no part of OD-02, runs no specimen, freezes no APDU protocol and closes no Gate. | Preferring a positively identified contact-only variant removes an unnecessary bearer interface without weakening the exact-card evidence threshold or treating catalog absence as a universal nonexistence claim. |
| 2026-08-25 | QK-DEC-100 | The Owner records completion of the exact QK-DEC-097 three-scrap HOST smoke run. The manually transcribed T0 scrap for each profile produced `Rs72_60: rs=PASS corrected_errors=0 erased_bytes=0 aead=PASS fixture=PASS`; `Rs76_60: rs=PASS corrected_errors=0 erased_bytes=0 aead=PASS fixture=PASS`; and `Rs80_60: rs=PASS corrected_errors=0 erased_bytes=0 aead=PASS fixture=PASS`; every transcription contained zero explicit erasure marks. QK-DEC-097's three-authentication success condition is therefore met, and QK-DEC-094/095 Phase 1 printing and sleeving are cleared. Damage application remains gated on rig readiness, and scheduled corpus capture remains gated on the ratified rig-amendment row; neither is cleared here. No scheduled page or fresh M19 capture was decoded, and the scrap-only results change no M19 profile, threshold, retry, schedule, corpus, OD-04 or Gate fact. | Records the closed scrap-only setup result and advances only the already-ratified pre-damage custody sequence. |
| 2026-08-25 | QK-DEC-101 | By Owner direction, M19 changes from QK-DEC-094/095's 522-page, 1,566-capture two-phase profile-comparison matrix to an anchor-and-synthetic design. The old matrix, its phase counts, balance rule, checkpoint and QK-DEC-100 clearance to print and sleeve Phase 1 are superseded before any scheduled corpus page was printed, damaged or captured; zero scheduled-corpus decode outcomes exist, while QK-DEC-100's three expressly non-corpus scrap results remain only their recorded setup check. This is an outcome-blind workload redirect, not an adaptive change. The exact frozen masters, decoy bodies, damage recipes and severity ladders remain unchanged inputs; QK-DEC-095's general custody and registration controls, the Camera Module 3 bench rig as the required fresh-capture instrument with its exact protocol amendment still owed, the zero-decoding rule for newly scheduled physical material, and the both-end manifest and custody chains remain binding. Fresh physical pages will form a small anchor set solely to calibrate optical damage-class and severity morphology, not to estimate profile performance; the three-profile comparison moves to a separately specified synthetic-degradation pipeline. Exact anchor inventory, synthetic model, validation criteria against the anchors, legacy morphology-reference registration and any close-result confirmation round remain owed by a new preregistration pushed before corresponding execution. OD-04 remains open, and Gate A O01 on the real camera stack remains the final physical judge; this row selects no profile, threshold, retry rule, production footer or Gate fact. | Preserving outcome-blind physical controls while redirecting manual work to morphology anchors permits a preregistered synthetic profile comparison without treating synthetic data as final physical evidence. |
| 2026-08-25 | QK-DEC-102 | Owner architecture input: A1 copy redundancy is a documented recovery mitigation. The user is expected to retain the printed kit copy and is encouraged to retain private digital copies of the byte-identical generated page; each copy is the same logical A1 factor and creates no additional signer or custody factor. Capsule security does not depend on copy scarcity: duplication changes neither the capsule nor its key, nonce, wallet binding or threshold semantics. A publicly posted copy remains cryptographically survivable as A1 alone under the existing model, but creates durable exposure and linkability that works against the cloak's discretion and association goal, so product documentation must not recommend public posting. QK-THR-021 remains unchanged: later A2 disclosure makes every captured, private or public A1 copy decryptable signer-A material. Intentional private copies do not relax A1/card separation, rotation-and-sweep duties, the cloud/network-printing prohibition or required printer-spooler and cache disposal. Consequence for OD-04: profile selection optimizes first-attempt page recovery and footer geometry subject to a preregistered adequate damage margin, rather than maximizing correction margin; OD-04 remains open until its physical evidence and Owner selection exist. | Copy redundancy improves recovery availability without making ciphertext scarcity a cryptographic control or weakening the cloak, factor separation or A2-disclosure boundary. |
| 2026-08-25 | QK-DEC-103 | Owner ratifies M19-R as the exact anchor-and-synthetic preregistration under QK-DEC-101. The fresh physical anchor set is exactly 29 pages, one page for each frozen QK-DEC-094 cell `c00..c28`, using the fixed T0 RS(80,60) payload throughout; in cell order the template alternates Recipe for even `c` and Travel for odd `c`, yielding 15 Recipe and 14 Travel pages. This set calibrates optical damage-class and severity morphology only and provides no physical profile comparison. Every frozen damage operation and level remains unchanged, including three distinct fade pages at exactly 50, 150 and 300 cycles. The 29 pages are captured in one scheduled Camera Module 3 rig session with exactly three canonical captures per page, 87 captures total; capture 1 is the calibration partition and captures 2 and 3 are holdouts. Anchor rendering, treatment and capture may begin only after a separate Owner-ratified rig-amendment row freezes the rig constants and the rig is ready. The 29 QK-DEC-088 spike originals and 19 Bridge originals must be registered by exact file hash, byte count, path and bench commit before use and remain old-format morphology references only, never decode, profile or Gate evidence. Before comparison-image generation, a pushed preregistration freezes the deterministic synthetic-model class and version, bounds, seeds, partitions and per-cell acceptance tolerances for geometry, damage-mask placement and footer overlap, connectedness, contrast and luminance, blur and edge spectrum, and glare; capture 1 may calibrate those values, while captures 2 and 3 remain held out. Validation is per cell, and any cell outside its frozen tolerances stops the comparison without adaptive retuning or regeneration. Only after all 29 cells pass may the pipeline generate exactly 1,566 paired images, `18 payloads x 29 cells x 3 realizations`, applying each lineage/cell/realization degradation identically across RS(72,60), RS(76,60) and RS(80,60). Scoring uses one frozen Reader build; first-attempt authenticated page recovery is primary and its frozen three-attempt recovery curve is secondary, while symbol metrics remain diagnostic and the product retry rule stays open. A quantitative close-result rule is frozen before scoring, and any targeted physical confirmation requires a later preregistration. After synthetic scoring and before any OD-04 ratification or profile recommendation, another Owner-ratified row must predeclare the material-agreement rule and authorize scoring that same frozen Reader build against real holdout captures 2 and 3. Material synthetic-to-real disagreement returns the model to a newly preregistered examination and never permits adaptive retuning or replacement of the scored comparison; agreement under the predeclared rule is required before a recommendation reaches the Owner. QK-DEC-101's zero-decoding rule therefore remains binding on every fresh anchor until that follow-up row. The carried custody, registration and both-end manifest chains remain binding; OD-04 and every production default remain open, and Gate A O01 on the real camera stack remains the final physical judge. | A one-page-per-cell anchor set fixes physical morphology while paired synthetic realizations compare profiles tractably, and the later frozen-build holdout gate prevents a synthetic recommendation from advancing without real-camera agreement. |
| 2026-08-25 | QK-DEC-104 | Owner ratifies Reader v0.2 as bench-only reader research in QuietKey-Protocol-Lab under QK-DEC-085; no source migrates between repositories, no decoder runs on the Camera Module 3 capture rig, and scaffolding may proceed now in parallel with M19-R and F8-G0. Development inputs are the frozen clean renders and a preregistered synthetic training partition; the fresh M19-R anchors remain unavailable to Reader execution until the QK-DEC-103 follow-up scoring row expressly authorizes captures 2 and 3. Reader v0.2 automatically locates and rectifies only the footer, does not parse decoy text, and may not consume the rig-only fiducials or adjacent scale bar as reader inputs. On the exact QK-DEC-090 grid it classifies only the ratified alphabet and emits exactly one known symbol or one explicit erasure per position, never normalization, aliasing, guessing or candidate substitution, producing an exact 116-, 122- or 128-position transcript for a separately authored codec-and-authentication adapter. The carried right-first-time requirement means one capture and one automatic authenticated page result with no manual crop, character edit, threshold adjustment, tuning or candidate selection; uncertainty yields an erasure or an explicit recapture request. Code, model, thresholds, configuration and training/scoring partitions are frozen and hashed before held-out scoring. Results report exact page-level first-attempt and bounded-retry counts; symbol-level results are diagnostics only. Production defaults and fallback UX, target-resource or sealed-stack claims, fresh-anchor execution before its later authorization, OD-04 resolution and every Gate claim are excluded. | A strict locate-rectify-erasure boundary measures one-shot user recovery without turning uncertain OCR, rig-only marks or manual intervention into authenticated input. |
| 2026-08-25 | QK-DEC-105 | Owner ratifies F8-G0 as non-mutating card-bench groundwork in this repository for the arriving J3R180 specimens; Protocol-Lab's physical-evidence and Reader scope is unchanged, and intake may proceed now in parallel with Reader v0.2. Before any card command, every delivered specimen is enrolled with supplier and order reference, packaging and markings, quantity, visible lot or serial identifiers, photographs and hashes, custody location, and sacrificial/non-sacrificial assignment. Every later observation binds only to the recorded delivered J3R180 revision and batch and transfers neither to J2R180 nor to any other variant; the M20 paper shortlist remains non-selecting. The exact CCID readers, host and PC/SC stack, tool versions and planned power-cut apparatus are recorded, with raw media kept outside Git under a hash manifest. Before probing, a separate registration ratifies a closed non-mutating APDU allowlist; the permitted intake captures only ATR, protocol negotiation, approved identity/status responses, timing, deterministic filenames and hashes, and any unexpected state stops the run. A bounded trace harness may be built against mock transcripts and public permanently NEVER-FUND fixtures only; management material and secret values never enter Git or traces. Seven complete future run registrations align one-to-one with QK-TST-BENCH-002: `(a)` secp256k1 signing correctness/performance, `(b)` least-authority derivation, `(c)` on-card RNG characterization, `(d)` APDU byte layouts and behavior, `(e)` write atomicity and power cuts, `(f)` storage endurance, and `(g)` distinct B/C signer keys and roles with identical A2/D plus D/`wallet_id` binding, path/policy, lifecycle and normal-operation non-exportability coverage beyond QK-F2E-013. Applet loading, key or storage mutation, power cuts, endurance execution, contactless bearer operation, APDU freezing, OD-02 resolution, production-card selection and every Gate claim require later Owner authorization. | Exact-batch enrollment, a closed non-mutating intake and seven prewritten run registrations establish a reproducible bench starting point without selecting a card, protocol or product behavior. |
| 2026-08-26 | QK-DEC-106 | Owner ratifies M21 as a hostile-input hardening campaign for the existing frozen parsers in `qk-psbt`, `qk-descriptor`, `qk-a1`, `qk-a1-codec`, and `qk-card-trace`. The selected tooling is a ring-fenced `fuzz/` area outside the `host` Cargo workspace, with its own pinned toolchain and lockfile and a separately reviewed dependency allowlist recording every allowed package's exact name, version, checksum, provenance, license, and purpose. `tools/check.sh` exempts exactly tracked `fuzz/**/Cargo.toml` manifests from its general no-external-dependency rule and adds a matching fail-closed check that every dependency entry in every such manifest appears in that allowlist with the same pinned version and checksum; no fuzz dependency may be reachable from a product crate. Every fuzz build enables overflow checks and debug assertions. Each parser receives coverage-oriented byte mutation or dependency-free structured-input generation, persistent hash-registered seed and finding corpora, deterministic minimization and reproduction, and crash/reject-consistency oracles: bounded execution must return valid acceptance or the exact named rejection and must never panic, hang, or accept an input outside the parser's frozen contract. The first report records per-target execution counts and exact minimized-corpus hashes. No product-code behavior changes without a reproduced finding; each finding lands as an ordinary fix commit with a minimized regression fixture. If the affected crate is frozen, the same fix also receives a decision row recording the change: frozen status never blocks a defect fix. Existing Reader, M19-R, rig, and F8 lanes and every physical gate remain active and unchanged. | A separately governed coverage-guided area explores hostile parser state more effectively than only deterministic in-workspace generators while keeping its extra toolchain and dependencies outside the production workspace and making every dependency, corpus, rejection, and resulting fix reviewable. |
| 2026-08-26 | QK-DEC-107 | Named post-Gate-A entropy-input research item “dice grid”: 25 loose d6 dice, exactly 13 black and 12 white, are mixed by shaking a closed box and settled with exactly one die in each cell of a 5x5 grid. The terminal camera transcribes the 25 face values; the user confirms the displayed grid against the physical dice; and the confirmed values serialize into the same canonical transcript format as the manual dice mode, with exactly five shakes per 256-bit secret. Counted entropy is exclusively the face values. The black-and-white color pattern contributes zero counted bits and exists only as a mixing-health indicator and machine-vision aid; positional, color-arrangement, and all other arrangement entropy are explicitly not counted because any such claim is CloakGrid-class and requires its own study. This is a post-Gate-A research item outside v1 scope: no implementation or bench work is authorized now. Before any later ratification as an input method, separate evidence must establish read accuracy on the fixed camera, measured inter-shake face independence, batch die-bias testing, and settle mechanics guaranteeing exactly one die per cell. The two ratified production entropy modes remain unchanged. | Recording the concept without crediting unstudied arrangement entropy preserves the production entropy boundary while making its physical and measurement prerequisites explicit. |
| 2026-08-26 | QK-DEC-108 | Owner amends only the collaboration loop’s single-active-build-task limit: up to two software milestones may be concurrently active alongside the physical-trigger lanes, each separately ratified by the Owner with its own scope. Every commit and every session summary names the milestone or lane to which it belongs. Concurrent lanes must not modify the same files; where overlap is unavoidable, the affected commits are serialized and the session summary records that serialization. The LOA reviews each pushed range partitioned by lane. If an LOA report states that a range exceeded auditable depth, one active lane must close before any other lane opens. The separate one-active-review-task limit and all other standing rules remain unchanged, including rows-first and one commit per change. | Bounded software concurrency permits progress around physical waits while explicit attribution, file separation, serialization, and lane-partitioned review preserve traceability and review depth. |
| 2026-08-26 | QK-DEC-109 | Owner ratifies M22 as a QK-DEC-108 software lane: add the dependency-free HOST-reference crate `qk-bbqr` for only the v1 BBQr profile, with encoding, frame decoding, and a one-stream, one-frame-per-transition bounded reassembler. The normative external wire authority is only `coinkite/BBQr` commit `8dc7ef07d0d520763cc0001a885ca8d29ac8719a`, specification blob `6a2e6b22cd8645a74d74b5e4ff1469f1a68fd962`; the pinned SeedSigner blobs `6a2b2d23266e7eabbe367efedcc2993be9468801`/`3c86b6a0dec7905f815f22b5df5dafb2b0326dd5` and COLDCARD blobs `3b222d43323c4735d543f0076292655fa493b713`/`f868c49a1080ae712e9a68dea3a1c2130940be1f` are behavioral cross-check sources only. Any disagreement resolves against the specification; any specification ambiguity stops and returns to the Owner. The specification's isolated “seven-character header” sentence conflicts with its own field enumeration, examples and eight-character overhead statement, and this Owner ratification resolves that known ambiguity only: canonical frames are ASCII `B$2P` followed by an uppercase two-digit base36 declared part count, an uppercase two-digit base36 zero-based part index, and an unpadded uppercase RFC 4648 Base32 body; lowercase, padding, aliases and normalization reject. Declared count is 1 through 256 and index is strictly below it. Every non-final part decodes to one identical byte length that is a multiple of five; the final part is the nonempty remainder and may not exceed that length. The encoder accepts an explicit caller-supplied non-final part length, with no default or QR-sizing policy. QK-DEC-034 HOST candidates, not registry freezes or target claims, are `MAX_DECLARED_PARTS=256`, `MAX_FRAME_TEXT_BYTES=4296`, `MAX_BODY_SYMBOLS=4288`, `MAX_PART_DECODED_BYTES=2680`, `MAX_TOTAL_DECODED_BYTES=262144`, identical duplicates at most the declared count, and total submissions at most twice the declared count and never above 512; checked multiplication and cumulative length reject before allocation or write, one candidate stream exists, and no silent restart occurs. Out-of-order unique parts are accepted. After a stream is established, header disagreement in encoding, file type or declared count returns respectively named `StreamEncodingMismatch`, `StreamFileTypeMismatch` or `StreamPartCountMismatch` before body decoding and separately from `ConflictingDuplicate` and `DuplicateWorkExceeded`; identical duplicates within the work cap are reported without mutation, conflicting duplicates reject immediately, and completion emits only the exact fully covered bytes. All other file types, encodings, compression, malformed or noncanonical Base32, invalid geometry, missing parts, overflow and over-limit conditions reject under named errors. Public interoperability fixtures use obviously public synthetic PSBT-like bytes, are generated outside Git by two agreeing implementations under QK-DEC-047, record exact source-case derivation and constructed bytes, and assert exact counts and hashes; no SeedSigner or COLDCARD code enters the repository. Unit, property, hostile-corpus and existing ring-fenced fuzz coverage exercise ordering, truncation, missing parts, geometry, duplicate classes, header mismatch precedence, canonical encoding and every cap edge. Camera input, QR image recognition or rendering, display policy, timing, performance, production-resource or Gate claims are excluded; the fixed-hardware performance obligation and the A1-constant, rig and card physical-trigger lanes remain unchanged. | A specification-led, profile-locked HOST codec makes the selected transport testable while conservative candidate bounds, strict geometry, explicit mismatch precedence and clean-room cross-checks contain hostile work without pre-empting physical limits. |
| 2026-08-26 | QK-DEC-110 | Owner ratifies M23 as a QK-DEC-108 software lane and the first HOST-simulator slice of the v1 sign-or-reject pipeline, ending only at a typed immutable `ReviewReady` fact and never at approval or signing authorization. Intake first compares the caller slice length with the existing `InputSource` cap, then fallibly owns exactly one immutable S0 artifact copy, records SHA-256 of those exact bytes, and parses and reparses only that retained copy; microSD remains 2 MiB, assembled QR remains 256 KiB, and qk-bbqr remains the unchanged outer type-P/uncompressed-Base32 assembler. PSBT magic, full consumption, map framing, one global unsigned transaction, and absent-or-explicit-zero PSBT version are mandatory under the existing named structural rejections. The caller supplies authenticated D as the QK-DEC-060 `DescriptorPair`; M23 proves nothing about D authenticity. Every input requires the QK-DEC-032 full previous transaction, exact txid and vout selection, MoneyRange value and script proof, and QK-DEC-063 descriptor reconstruction at its record-supplied branch/index with no skip path. If `witness_utxo` is additionally present, its complete serialized amount-plus-script value must be byte-exactly equal to the selected proven output or `WitnessUtxoMismatch` rejects. The three derivation records retain all M12 role, coordinate, duplicate and branch/index rules. An absent input sighash record means SIGHASH_ALL; a present record and every existing partial-signature suffix must equal little-endian/byte value 1 or `UnsupportedSighash` rejects. Existing partial signatures receive only the already-ratified bounded syntax, strict-DER and low-S checks in this slice; their cryptographic verification is deferred to the signing slice, and M23 carries no verified-signature count, per-input threshold state, aggregate completeness, signability or export claim. Every branch-1 descriptor-owned output is `ProvenChange`; every other output, including branch-0 `ProvenSelfTransfer`, is classified against exactly QK-DEC-066's P2WPKH, P2WSH, destination-only P2TR, P2PKH, P2SH and canonical OP_RETURN set. OP_RETURN is parsed as bounded Bitcoin Script, never at a fixed offset, with QK-DEC-067's zero value, at-most-one output, zero-or-one minimal push and 80-byte payload rules and existing distinct named rejections; bare pubkey, bare multisig, future witness versions, malformed witness programs and the unknown/nonstandard catch-all remain distinct rejections. Checked u64 MoneyRange arithmetic produces total input, total output and exact fee; negative or uncomputable fee rejects under the existing named categories. The Owner closes the v1 fee-policy values as `QK-FEE-POLICY-V1`. For each input, the fixed witness estimate is exactly CompactSize item-count 4, one zero-length dummy, two CompactSize-length-prefixed 72-byte signatures including their SIGHASH_ALL byte, and one CompactSize-length-prefixed actual descriptor-reconstructed witness script. BIP141 estimated weight is checked `4 * exact_unsigned_tx_bytes_len + 2` for marker/flag plus the sum of those witness serializations; estimated virtual size is checked `ceil(weight / 4)`. The internal fee-rate fact is checked `floor(fee_sats * 1000 / estimated_vsize)` milli-satoshi per virtual byte. After all preceding facts exist, fee strictly greater than 5,000,000 sats rejects as named `EmergencyFeeCeilingExceeded` before warning construction. Otherwise warnings are evaluated independently, never suppress one another, are nonfatal, and appear only in this fixed order: tag 1 `W-FEERATE-LOW` when fee rate is below 1,000 milli-sat/vB; tag 2 `W-FEERATE-HIGH` when it is at or above 200,000; tag 3 `W-FEESHARE-HIGH` when checked `fee_sats * 20` is at or above total input; and tag 4 `W-FEEABS-HIGH` when fee is at or above 1,000,000 sats. Checked policy arithmetic failure rejects as named `FeePolicyArithmeticOverflow`; no percentage rule is fatal. These are v1 policy values and may change only through a future Owner-ratified policy identifier. QK-DEC-071 therefore forces a separate D-09 schema v2 while QK-DEC-070/v1 and the complete M14-M16 API, bytes, hashes and behavior remain frozen. The exact hash domain is ASCII `QuietKey/D-09/review/v2/QK-FEE-POLICY-V1`, then one raw `00`, then canonical v2 bytes; deliberate duplicate binding also encodes length-prefixed ASCII `QK-FEE-POLICY-V1` in the canonical object. Canonical v2 retains QK-DEC-070 order and encodings—fixed-width little-endian integers, u32 length prefixes, no review-level CompactSize—but removes verified-signature count/status and aggregate status. Exact order is schema u8 `2`; network u8; source u8; S0 SHA-256; wallet_id; A/B/C fingerprints; policy identifier; exact unsigned transaction; raw transaction version; locktime; input count; each input's index, outpoint txid, vout, proven amount, proven scriptPubKey, sequence, effective sighash, branch and child index; output count; and each output's index, amount, exact scriptPubKey and ownership. `NotOwned` encodes recipient type and exact program/hash/OP_RETURN data; `ProvenChange` encodes child index; `ProvenSelfTransfer` encodes child index plus its required P2WSH recipient type and exact program. The tail is total input, total output, fee as u64; estimated vsize as u32; fee rate as u64; warning count as u32; then the ordered u8 warning tags. Raw per-input sequence values are canonical; each typed per-input and aggregate direct-RBF fact is deterministically `sequence < 0xfffffffe` and is therefore bound without a redundant canonical boolean or a claim about actual network replaceability. `MAX_CANONICAL_REVIEW_V2_BYTES=18934`, maximum estimated vsize 11,886, and maximum hash transcript 18,975 bytes are QK-DEC-034 HOST candidates only: fixed/top including the maximum three simultaneously applicable warning tags is 162; maximum accepted unsigned transaction 5,535; 100 inputs at 102 each is 10,200; outputs are one 185-byte OP_RETURN plus 31 92-byte self-transfers, 3,037; total `162+5535+10200+3037=18934`; the 40-byte domain plus NUL yields 18,975. Low-rate and high-rate are disjoint, so at most three of the four warning tags coexist. Construction uses checked length arithmetic and a fallible exact reservation. qk-psbt alone constructs the session-free v2 facts, canonical bytes and hash; qk-host-sim owns S0 and D, exposes the immutable review only at `ReviewReady`, and any intake, parse, semantic, policy, build or reparse failure clears all partial binding and ends Locked with its stable named category. Fixtures are exact public, unsigned, permanently NEVER-FUND NUMS-derived data under QK-DEC-047/QK-DEC-059 with every constructed byte embedded and two separately written outside-Git constructors agreeing; exact threshold edges, both hashes and stable failure precedence are asserted. Ring-fenced M21-style M23 semantic and workflow fuzz targets use hash-registered minimized corpora and named-error/no-panic/no-mutation/stable-rehash oracles. Signing, signature insertion or generation, cryptographic verification of existing partial signatures, threshold completion, cards or sessions, approval UI, address rendering, export, camera/display/media, persistence, target performance and every Gate claim are excluded; all physical-trigger lanes remain unchanged. | One owned immutable intake and one closed, policy-identified review commitment make every currently sourced v1 pre-approval fact reproducible and substitution-resistant without importing unavailable signing, session, UI or target claims. |
| 2026-08-26 | QK-DEC-111 | Owner ratifies M24 as a QK-DEC-108 software lane and a HOST-only signing/finalization evidence slice that consumes the successful M23 `ReviewReadyWorkflow`, including its privately retained immutable S0 and caller-authenticated D. Before any cryptographic call or mutation it reparses only that S0, rebuilds the complete schema-v2 typed review, and requires exact typed-fact and review-hash equality. This separate continuation carries no approval, cycle-token, card-session, transport or export authorization claim; the M14-M16 public APIs, schema-v1 bytes and hashes remain frozen. Every existing partial signature is verified, without threshold-based skipping, against its descriptor-derived A/B/C role key and exact QK-DEC-044 BIP143 SIGHASH_ALL digest before it contributes to threshold; existing `StrictDer`, `HighS`, `UnsupportedSighash`, `PartialSignaturePubkeyNotInWitnessScript`, `DescriptorDerivationKeyMismatch` and `SignatureVerificationFailed` categories retain their precedence. M24 narrowly supersedes only QK-DEC-042's verification-only callable boundary while leaving the pinned libsecp256k1 v0.8.0 vendor tree, three-C-unit build and flags byte-identical: the private FFI adds exactly `secp256k1_context_create`, `secp256k1_context_destroy`, `secp256k1_ec_seckey_verify`, `secp256k1_ecdsa_sign`, `secp256k1_ecdsa_signature_normalize` and `secp256k1_ecdsa_signature_serialize_der`, plus the explicit `secp256k1_nonce_function_rfc6979` pointer; signing uses a non-static `SECP256K1_CONTEXT_NONE` context, null extra nonce data, destroys the context on every return path, always normalizes before DER serialization, and then uses the unchanged parse/verify path for mandatory self-verification. At this row's parent qk-secp contains no secret-bearing type, so M24 establishes the Owner-required qk-secp-only boundary as opaque, fixed-length, move-stable, non-Clone/non-Copy/non-Debug/non-Display `SecretKey` storage: import accepts one mutable 32-byte source, validates scalar range, unconditionally volatile-wipes that source, and owned storage is volatile-wiped with a compiler fence on every error path and Drop; no accessor exposes bytes. Raw signing-key material never crosses the safe wrapper except into the listed FFI calls and never enters logs, panic strings, error values or test snapshots. These are HOST behaviors only and discharge no target remanence, context-randomization, side-channel, constant-time or Gate-C obligation. Terminal role A signing uses the exact input digest and descriptor-derived role-A key, pinned RFC6979, low-S normalization and strict DER; the sole `01` sighash byte is appended only at PSBT insertion, after immediate cryptographic self-verification, and failure returns no signature. Mock card inputs stand in only for roles B and C; callers supply input index, role and DER-only signature, never a pubkey or digest. Each terminal or mock signature is syntax/low-S checked and cryptographically verified against its derived role key and exact digest before insertion; `InvalidMockSignature` is distinct and an invalid mock never enters a candidate PSBT or witness. Existing and new records retain QK-DEC-076's ascending input index then ascending compressed-pubkey tuple, one signature per role, at-most-threshold insertion and distinct duplicate/role/threshold categories; every insertion is the sole byte delta, followed by canonical reparse, schema-v2 fact rebind and complete cryptographic verification. Finalization retains QK-DEC-081/082 exactly: each native sorted 2-of-3 witness is one empty dummy, then the selected two complete DER-plus-`01` signatures ordered by their pubkeys' positions in the exact 105-byte sorted witness script, then that script. After assembly, a separate fresh raw-transaction parse through EOF rebinds the witness-stripped transaction to M23 and independently recomputes and verifies both selected signatures on every input, in addition to all pre-insertion and per-step checks; `WitnessOrderMismatch`, `FinalSignatureVerificationFailed` and existing finalization failures reject closed. The sole QK-DEC-047/QK-DEC-059 fixture-key exception is one M24-only public, permanently NEVER-FUND BIP48 lineage. The exact public BIP32 seed strings, recorded verbatim in every fixture header, are ASCII `QuietKey/M24/NEVER-FUND/fixture-only/role-A/v1`, `QuietKey/M24/NEVER-FUND/fixture-only/role-B/v1` and `QuietKey/M24/NEVER-FUND/fixture-only/role-C/v1`; each string's bytes are the complete BIP32 seed to `HMAC-SHA512(key=ASCII "Bitcoin seed", data=seed)`, yielding master scalar `IL` and chain code `IR`, then CKDpriv follows exactly `m/48'/0'/0'/2'/{branch}/{index}` with hardened data `00 || ser256(kpar) || ser32BE(i)`, nonhardened data `serP(point(kpar)) || ser32BE(i)`, child scalar `(parse256(IL)+kpar) mod n` and child chain `IR`. Any master or child `IL >= n`, zero scalar, or point failure aborts generation with no skip, retry or search. Master fingerprints, account xpubs, descriptor pairs, wallet_id, route keys, BIP143 digests and RFC6979/low-S/DER signatures are therefore reproducible from the public strings alone. Every M24 fixture file states `PERMANENTLY NEVER-FUND`, records all constructed bytes and identifies those public seed strings and any deliberately printed derived scalars as the sole raw-key exception; the three authorities and their derived keys are never reused for any non-M24 purpose. Two separately written outside-Git implementations must agree byte-for-byte before commit on all keys, descriptors, digests, signatures, PSBT stages, witnesses, finalized transaction and fixture bytes; all account/route public keys and signature r values are screened against the committed KAT, QK-DEC-059/064 NUMS and QK-DEC-078/084 signing sets plus small multiples, then generators and intermediates are destroyed. Exact positive, boundary and mutation fixtures plus hash-registered minimized M21-style fuzz targets cover signing, invalid existing/mock/terminal signatures, tuple order, role/threshold caps, witness assembly, fresh-final verification, named-error/no-panic/no-partial-output and secret-surface rules. Real card sessions, approval binding, export transports, camera, display, performance, production key derivation/cleanup/constant-time claims and every Gate claim are excluded; physical-trigger lanes remain unchanged. | A separate non-authorizing HOST continuation closes the cryptographic path from the frozen schema-v2 review to a freshly reparsed native-P2WSH transaction while keeping the publicly reproducible fixture lineage and every unavailable approval, card, transport and target claim explicit. |
| 2026-08-27 | QK-DEC-112 | Owner ratifies M25 as a QK-DEC-108 software lane and a HOST-simulator export slice consuming only the opaque, freshly reparsed and verified M24 `FinalizedTransaction`; it creates no constructor from arbitrary bytes and carries no approval, route-authorization or session claim. The tier policy is exact: `SimpleRecovery` and `Inheritance` expose both the byte-identical finalized PSBT and byte-identical final raw transaction; `QuantumShelter` exposes only the raw transaction and never a finalized PSBT. Each typed artifact binds its kind, exact serialized byte length, SHA-256 of those exact bytes, and the transaction's raw-order 32-byte txid and wtxid facts inherited from and checked against finalization, so later display of transaction ID and output checksum consumes bound facts rather than recomputing them. For SD, the caller supplies one opaque 128-bit export nonce; HOST generates no randomness. The exact lowercase-ASCII final names are `qk-<32-lowercase-hex-nonce>-final.psbt` and `qk-<32-lowercase-hex-nonce>-final.tx`, and their temporary names are the corresponding final name followed by `.tmp`; this narrowly supersedes QK-DEC-029's input-derived spelling while retaining fail-on-collision, no overwrite and no automatic rename, and retry under QK-DEC-017 reuses the same frozen bytes and nonce-derived names. One mock-filesystem artifact attempt executes exactly new temporary file, complete write, file sync, close, reopen, exact length plus SHA-256 plus full parse verification, then atomic no-replace rename; the closed named failure set is `FullMedia`, `TemporaryCreateFailed`, `WriteFailed`, `SyncFailed`, `CloseFailed`, `ReopenFailed`, `VerificationMismatch`, `FilenameCollision` and `RenameFailed`. The atomicity invariant is exact and per artifact: a file appears under a final name only as a complete artifact that already passed reopen, hash and parse verification; every failure path leaves the destination namespace unchanged except for a possibly remaining temporary file, and temporary files are invalid, ignored by every consumer and never automatically promoted. The input is never overwritten or deleted. `SimpleRecovery` and `Inheritance` route completion still requires both artifacts; M25 neither claims two-file filesystem atomicity nor discharges real-media durability or power-loss behavior. BBQr export is only the finalized PSBT through the unchanged M22 uncompressed Base32 file-type-`P` encoder with an explicit caller part length; raw transactions are never mislabeled as type `P`. This row opens named decision `OD-09 — Quantum Shelter raw-transaction BBQr framing`: a future Owner-ratified profile extension is required before file type `T` or any other raw-transaction QR format exists, so Quantum Shelter export is SD-only until then and M25 pre-commits no type-`T` wire semantics. M25-only public, permanently NEVER-FUND fixtures are generated outside Git under QK-DEC-047 by two separately written implementations from a distinct high-entropy ephemeral signing lineage with unique high-entropy nonce per signature; exact artifacts and derivation-free public verification facts are recorded, every new public key and signature `r` is screened against committed KAT, NUMS and fixture-authority sets plus small multiples, and all secrets, generators and intermediates are destroyed before commit. Fault-injection tests exercise every lifecycle edge, input immutability, no overwrite, exact artifact facts, tier restrictions and temp-only failure residue; existing ring-fenced M21/M22 fuzzing covers export state and BBQr framing with hash-registered corpora and named-error, no-panic, no-wrong-acceptance and no-partial-mutation oracles. Real storage hardware or filesystem selection, power-cut evidence, secure deletion, approval semantics, card sessions, broadcast or receipt, camera, QR rendering, display, performance, production-resource limits and every Gate claim are excluded; QK-TST-DIFF-004's historical unfinalized-signed-PSBT oracle is not claimed; all physical-trigger lanes remain unchanged. | A capability-only HOST export boundary binds exact artifacts and models fail-closed publication without confusing finalized output with approval, raw-transaction QR selection, or physical-media evidence. |
| 2026-08-27 | QK-DEC-113 | Owner ratifies M26 as a QK-DEC-108 software lane and a HOST-only provisioning-mathematics slice in new dependency-free crate `qk-provisioning`; it samples no entropy and accepts only caller-supplied deterministic public test inputs. QKEC-1's canonical source record is now byte-exact: byte zero is version `01`; each following record is source tag u8, value length u16 big-endian, then value; tags are strictly ascending and unique; every value is exactly 32 bytes; required tags are terminal source `01`, card-B source `02`, and card-C source `03`; optional already-conditioned user-physical contribution is tag `04`; all tags `05` through `ff` reject. Canonical record length is therefore exactly 106 bytes without tag `04` or 141 bytes with it. This mapping and the Owner-ratified purpose bytes `Seed-A`, `Signer-B`, `Signer-C`, and `A2` resolve the subordinate QKEC-1 PDF's inconsistent page-4 `CARD`/`DICE`/`COIN`, page-15 terminal/card-B/card-C, and `Seed-B`/`Seed-C` labels in favor of supreme Core Architecture v1; the PDF's retired HMAC-SHA512 and incomplete companion-file vectors are not implementation inputs or claims. Closed record failures are `InvalidRecordLength`, `UnsupportedRecordVersion`, `UnknownSource`, `SourceOutOfOrder`, `DuplicateSource`, `InvalidSourceLength`, and `MissingRequiredSource`, in that precedence where applicable. One run accepts four separately owned canonical records, one per purpose; exact record-byte reuse across two purposes is `SourceSetReuse`, while non-equality is not claimed to prove physical independence. The ceremony identifier is exactly 16 caller-supplied bytes. For each purpose independently, salt is SHA-256 of exact ASCII `QuietKey/QKEC-1` followed immediately by that purpose's exact ASCII bytes and the ceremony identifier; PRK is HKDF-SHA256 Extract with that salt and the purpose's complete canonical record as IKM; output is HKDF-SHA256 Expand with exact ASCII info `QuietKey/256-bit-output` to exactly 32 bytes. There is no shared master, cross-purpose expansion, source substitution, retry, or search. The narrow claim is only that conditioning preserves unpredictability if at least one input source is unpredictable and independent of adversary-controlled inputs; it creates no entropy from broken or correlated sources, and this HOST slice supplies no source, health-test, reachability, independence, SP 800-90B, or production claim. Advanced mode accepts exactly four fresh transcripts. Entry accepts only ASCII `1` through `6`, rejects an invalid byte as `InvalidDiceSymbol` before storing it, rejects a 101st byte or finalization before 100 bytes as `DiceCount`, and hashes the literal separator-free 100 bytes with SHA-256; all six cross-purpose equality pairs are checked and any exact reuse is `TranscriptReuse`. No character filtering, whitespace, alternate base, 99-roll path, face-frequency rejection, cross-run reuse claim, or general transcript API exists. For Seed-A, Signer-B, and Signer-C, each 32-byte conditioner or dice result maps by the BIP39 checksum rule to exactly 24 English words; A2 remains raw 32 bytes. The complete entropy-to-master chain is BIP39 checksum mapping, NFKD normalization, and PBKDF2-HMAC-SHA512 with exactly 2048 rounds, 64-byte output, password equal to the NFKD space-joined mnemonic, and salt equal to NFKD ASCII `mnemonic` because the passphrase is fixed empty. In the only product profile, generated English words and the fixed salt are ASCII NFKD fixed points; M26 makes no general Unicode-normalization API or non-English claim. Published English BIP39 vectors may exercise their specified ASCII test passphrase through a private test-only helper, while every production entry point hard-codes empty. Mnemonic bytes, BIP39 seeds, master and child scalars, chain codes, and account private material are transient, fixed-size, non-Clone, non-Copy, non-Debug, non-Display, inaccessible outside the crate, and wiped on every return path; no mnemonic, seed, xprv, scalar, or private derivation result is exposed by a public API. Private BIP32 follows the fixed mainnet path `m/48'/0'/0'/2'`, uses HMAC-SHA512 with key `Bitcoin seed` at the master and standard hardened CKDpriv data thereafter, and rejects invalid master scalar, `IL` greater than or equal to the curve order, zero child, or backend failure at the presented step without BIP32 skip-to-next-index behavior. qk-bip32 remains byte-frozen and public-only. M26 narrowly widens only the unchanged pinned qk-secp/libsecp256k1 v0.8.0 boundary with `secp256k1_ec_pubkey_create` and `secp256k1_ec_seckey_tweak_add`; purpose-bound safe wrappers accept fixed 32-byte provisioning scalars, commit an output only on ordinary success, wipe scratch on every path, map every return code, and expose no general arithmetic, context, scalar accessor, signing, or randomness surface. Hand-rolled curve or scalar-order arithmetic and a second unsafe boundary remain prohibited. From the three distinct account nodes, M26 serializes exact mainnet xpubs, constructs the canonical receive `/0/*` and change `/1/*` descriptor texts under QK-DEC-054/055, reparses them through qk-descriptor, binds the resulting wallet_id, derives both branch-zero index-zero and branch-one index-zero P2WSH scripts, and encodes their first mainnet addresses only as BIP173 witness-version-zero P2WSH Bech32 with HRP `bc`; no decoder, Bech32m, nonzero witness version, other network, address type, or general Base58/descriptor/Bech32 API is added. A run accepts one caller-supplied 96-bit A1 nonce and calls the unchanged qk-a1 encrypt surface only after wallet_id exists; exact nonce reuse within that same run is `NonceReuse`, any second encryption under a different nonce is `AlreadyEncrypted`, and failure returns no capsule. The same nonce in another fresh HOST run is deliberately accepted because cross-run freshness is a terminal obligation to be discharged at hardware gates. Public results contain only the three exact account xpub texts, both full checksummed descriptor texts, wallet_id, first receive/change scripts and addresses, and the one 67-byte capsule; no entropy, transcript digest, mnemonic, seed, private key, xprv, PRK, IKM, or source byte is returned. Published BIP39 and BIP173 facts are imported only from exact source-pinned upstream blobs with exact parsed-case-count assertions; primitive SHA/HMAC/HKDF vectors are reused in place under existing source pins. M26 fixtures use distinct exact public `QuietKey/M26/NEVER-FUND/...` strings, deterministic public source samples, transcripts, ceremony identifiers and nonces; every fixture states `PERMANENTLY NEVER-FUND`, records the complete derivation procedure in its first provenance commit, excludes xprv and scalar text, and is produced outside Git by two separately written implementations agreeing byte-for-byte before import. This is the narrow authorized known-private fixture exception: its public entropy, mnemonics and seeds are deliberately reproducible and never reused outside M26. Unit, surface, mutation and end-to-end tests cover every exact constant, rejection and output fact; two existing-ring-fence fuzz targets cover canonical record/dice/run-state boundaries and the deterministic full chain with hash-registered minimized corpora, named-error, no-panic, no-partial-output, repeat-consistency and reparsing oracles. Hardware RNG sampling, card I/O or provisioning, print rendering, camera, display, ceremony UX, persistence, production cleanup or constant-time claims, target performance and every Gate claim are excluded; all physical-trigger lanes remain unchanged. | A source-less HOST reference can now reproduce the complete fixed provisioning mathematics and public wallet outputs while keeping real entropy, cards, ceremony behavior, nonce generation and all target evidence explicit future obligations. |
| 2026-08-27 | QK-DEC-114 | M26 pre-report review records one provenance-process deviation and its bounded repair. Commit `c2c6b5f` first imported the public `m26_provisioning_e2e.txt` fixture with the complete 72-fact body, constructor identities and hashes, exact source values, TLV shape, purposes, wordlist pin, exclusions and destruction procedure, but its header did not contain QK-DEC-113's complete conditioning-through-capsule derivation procedure. Therefore QK-DEC-113's statement that the complete procedure was already present in the fixture's first provenance commit was not executed as written. History remains unchanged under the standing no-rewrite policy. Before any qualifying M26 fuzz campaign or report, one bounded repair adds header-only conditioning, BIP39, BIP32, descriptor/address and A1 construction lines, leaves every one of the 72 ordered fact lines and their 6,824-byte body byte-identical, and re-registers the resulting full-file byte count, LF count, SHA-256 and Git blob in `SOURCE-REGISTER.md`; no authority, key, descriptor, address, capsule or output fact changes. This follow-up is not precedent: QK-DEC-047's first-provenance-commit rule remains unchanged for every future fixture. | The retained history states the actual provenance sequence while the bounded header repair restores a self-contained reproduction procedure without altering generated evidence. |
| 2026-08-27 | QK-DEC-115 | M26 pre-report release inspection reproduces a HOST cleanup defect in the frozen `qk-a1` crate: the optimizer removes its ordinary `fill(0)` calls, so the derived A1 document key and key-derived ChaCha20 material may remain in stack storage after `encrypt` or `decrypt` returns. Under QK-DEC-106, frozen status does not block this defect fix. Before the qualifying M26 campaign, a serialized ordinary repair adds one private optimization-resistant wipe boundary and routes all `qk-a1` secret-bearing SHA-256, HMAC-SHA256, HKDF-SHA256, ChaCha20, Poly1305, AEAD, document-key and candidate-plaintext scratch through it on every return path; source-surface tests lock the boundary and release-assembly inspection confirms the stores remain. The public API, exact 67-byte wire form, cryptographic outputs, rejection precedence, allocation behavior, dependency set and every M17 vector remain byte-frozen. This is HOST cleanup evidence only and makes no target-remanence, side-channel, constant-time, production-core or Gate-C claim. | The frozen HOST reference already attempted to clear derived secrets, but ordinary zero fills did not survive optimization; one private boundary restores the intended behavior without changing any protocol or public behavior. |
| 2026-08-27 | QK-DEC-116 | Owner ratifies M27 as a QK-DEC-108 software lane and a HOST-only deterministic wallet-mode screen-flow state machine; it models display/event order and logical cleanup but grants no approval authority, calls no signer, and does not integrate or widen the M24 signing capability. The exact flow kinds are `Provisioning`, `SigningA1B`, `RecoveryA1C`, and `RecoveryBC`; A1+C carries emergency-card labeling and both recovery routes carry the mandatory rotate-and-sweep result, while all real card exchanges remain abstract successful operations because card sessions are excluded. The closed screen kinds are `ProvisioningStart`, `TierSelection`, `EntropyModeSelection`, `CeremonyInput`, `CeremonyEcho`, `CeremonyConfirm`, `CeremonyCommitment`, `DerivationExplanation`, `ProvisioningResult`, `ProvisionB`, `VerifyB`, `ProvisionC`, `VerifyC`, `CreateA1`, `ScanBackA1`, `CoordinatorMaterial`, `Rehearsal`, `KitReady`, `FlowStart`, `Route`, `Transport`, `Intake`, `Factor`, `Validation`, `ReviewOverview`, `ReviewArithmetic`, `ReviewRecipient`, `ReviewChange`, `ReviewOpReturn`, `ReviewLocktime`, `ReviewSequence`, `ReviewFeePolicy`, `FinalApproval`, `PostApprovalFactor`, `AwaitingSigning`, `Export`, `TransactionResult`, and `RecoveryRotation`. The exact success terminal is `CompletedWiped`; failure is `FailedWiped` carrying one closed reason, and any later event returns only `Finished`. The P0.1 logical keypad vocabulary is exactly the nineteen visible keys `7`, `8/Up`, `9`, `CE/Delete`, red `C/CancelBack`, `4/Left`, `5`, `6/Right`, `Multiply`, `Divide`, `1`, `2/Down`, `3`, `Minus`, `Percent`, `0`, `Decimal`, `Plus`, and `Equals/ConfirmEnter`; the two physical switches under `0` map to the one logical `0`, and no calculator key is silently ignored in wallet mode. The closed non-key event set is operation completed, operation failed, ceremony echo ready, ceremony commitment ready, transport presented, camera presented, intake presented, media removed, approval hold started, approval hold completed, signing outcome, card removal, session timeout, shutdown, restart, and power loss. The closed wiping reasons are `InvalidTransition`, `Cancelled`, `OperationFailed`, `MediaRemoved`, `CardRemoved`, `SessionTimeout`, `Shutdown`, `Restart`, `PowerLoss`, `ReviewIncomplete`, `ReviewIdentityMismatch`, and `PostApprovalYield`; none carries attacker-controlled or secret bytes. Provisioning order is start, tier, the existing architecture's symbolic entropy-mode choice without selecting or widening product policy, then for each of Seed-A, Signer-B, Signer-C, and A2 a caller-bound ceremony unit that must execute input, exact echo, explicit user confirmation, and commitment before the next purpose, followed by a fixed factual derivation explanation, existing provisioning-result facts including the exact `wallet_id`, provision/verify B, provision/verify C, create A1, scan-back, coordinator material, rehearsal, kit-ready, and wiping completion. The machine may borrow the exact caller-supplied ceremony unit only for its echo and confirmation screens; it never copies, hashes, reformats, logs, exports, or retains that unit after confirmation or abort. Outside that transient exact echo it renders no mnemonic word, seed byte, transcript, or other secret; it exposes no statistical randomness meter or score, and every ceremony claim is a fixed screen kind rather than generated prose. M27 adjudicates no entropy-mode policy, transcript bytes, grid geometry, entry count, commitment construction, card protocol, or ceremony cryptography; all prior entropy authorities remain unchanged. Signing A1+B proceeds through start, route, transport, intake, pre-review factor operations, validation, exact review, final approval hold, awaiting signing outcome, export, exact transaction result, and wiping completion with no rotation screen. Recovery A1+C follows the same order but must visit `RecoveryRotation` after the result and before wiping completion. Recovery B+C performs its pre-review identity operations, the same exact review and approval, then abstract `PostApprovalFactor` operations for B and C in that order before awaiting signing outcome, export, result, rotation, and wiping completion. Review visitation is semantic rather than physical pagination and is exactly overview, arithmetic, every non-change non-OP_RETURN recipient or self-transfer in transaction order, every change output in transaction order, every OP_RETURN in transaction order, locktime, every input sequence/direct-RBF fact in input order, fee-policy facts with warnings in their existing canonical order, then final approval. Every required item must be visited in that order; no key or event skips an item, `C/CancelBack` may move backward only on an explicitly reversible pre-approval screen and never marks an unvisited item visited, and a completed approval hold is accepted only from the final review screen after the complete visit ledger. Each flow has an opaque process-unique provenance and monotonic approval-cycle token. Completed approval retains one inseparable binding of that opaque token and the exact immutable `ReviewV2Hash` supplied together with its `ReviewV2` by `ReviewReady`, without recomputation or replacement; this distinguishes current, stale, and foreign holds even when review hashes are equal. From completed hold until the matching signing outcome, transport, camera, and intake events are distinct `PostApprovalYield` rejections; no navigation or replacement-state event continues, while the B+C abstract post-approval factor operations are the only route-specific intermediate states. A signing outcome continues only when it presents the exact retained opaque token and review hash. Card removal and session timeout from every nonterminal state route directly to `FailedWiped`, as do operation failure, media removal, shutdown, restart, power loss, cancellation, every invalid state/event pair, and every identity mismatch. Review and result screens borrow exclusively from existing frozen fact objects: `ProvisioningArtifacts`, schema-v2 `ReviewV2`/`ReviewV2Hash`, and `FinalizedTransaction`/`ExportArtifacts`/`SdArtifactMetadata`; the machine derives, hashes, sums, classifies, truncates, converts, or reformats no fact and creates no address, warning, identifier, checksum, fingerprint, or display string, doing only typed field selection. Logical wiping clears every retained fact reference, visit marker, transient ceremony borrow, pending commitment, flow provenance/token, and approval identity before terminal state; this HOST behavior makes no physical-memory, target-remanence, or Gate claim. Exhaustive headless tests cover every state/event cell, all four complete paths, every review item, backtracking, early/foreign/stale holds, exact result-field ties, both mandated interruptions from every nonterminal state, post-hold no-yield, and stable wiped termination. One existing-ring-fence M21-style fuzz target uses hash-registered minimized corpora with deterministic-transition, named-error, no-panic, no-skip, no-secret-output outside the transient exact ceremony echo, exact-fact-selection, post-approval no-yield, and wipe-terminal oracles. Pixel rendering, fonts, strings, screenshot evidence, display driver, physical keypad input, camera, real cards or sessions, real transports, performance, approval-to-M24 integration, production behavior and every Gate claim are excluded; pagination, screen layout and timing constants remain open under OD-05 and D-3, and all physical-trigger lanes remain unchanged. | A fact-selecting HOST screen machine can freeze full-review, ceremony-order, interruption, approval-identity and no-yield invariants before physical UI work without inventing layout, recomputing facts, or converting a simulator path into signing authority. |
| 2026-08-27 | QK-DEC-117 | Owner fixes v1 entropy-input, ceremony-verification, and coordinator-export product policy. V1 seed generation is dice-only: Multi-source is removed as a user-facing path, while QKEC-1 remains verified library mathematics under QK-DEC-113 with no v1 product entry point. The ceremony-wide `EntropyModeSelection` has exactly two transcript profiles. The `ManualKeypad` fallback accepts exactly 100 keyed d6 face values per secret, four fresh transcripts and 400 values per kit, unchanged from Core Architecture v1 section 4.2 and the shipped `qk-provisioning` validator. The intended-primary `DiceGrid` profile uses the exact QK-DEC-107 physical construction and exactly five confirmed 25-die shakes per secret: 125 camera-read d6 face values per secret, four fresh transcripts and twenty shakes per kit. Under both profiles a transcript is the exact literal ASCII sequence of digits `1` through `6` with no separators; every other byte rejects immediately and is never filtered, normalized, corrected, or ignored; one SHA-256 of the literal transcript produces the 32-byte secret. Exact transcript reuse across any pair of the four ceremony purposes is a hard error regardless of profile. At conservative worst-face probability `0.20`, 100 values provide about 232 bits of min-entropy and 125 provide about 290 bits; the fifth grid shake is the bias margin for kit-supplied dice reused across all four secrets. The manual fallback remains at 100 because that is the frozen architecture threshold, exceeds the cited 99-roll industry threshold, and avoids reopening the reviewed M26 validator. QK-DEC-107's face-values-only accounting, zero-bit color-pattern role, and exclusion of positional or arrangement entropy remain unchanged; only its post-Gate-A/outside-v1 disposition is superseded. Before the grid path may be implemented or accepted, fixed-camera read accuracy, measured inter-shake face independence, batch die-bias testing, and settle mechanics guaranteeing exactly one die per cell are v1-blocking evidence gates. Ceremony product policy requires every shake or manual entry to be echoed for user confirmation before acceptance, displays a commitment for each complete per-secret transcript, explains derivation in a fixed sequence that renders no mnemonic word, seed byte, transcript, or other secret, and displays the final wallet fingerprint; every displayed claim is factual, with no statistical randomness meter or score. A transcript is seed-equivalent ceremony-only secret material, is never exported, is wiped at ceremony end, and may be checked against an independent offline tool only during the ceremony. Watch-only coordinator export for Sparrow, Nunchuk, and similar software is SD-card-only in v1; QR remains transaction-only, and the exact descriptor-container format requires a separate Owner-ratified decision before implementation. This row authorizes only the separately directed HOST narrowing of `EntropyModeSelection` to `DiceGrid` versus `ManualKeypad`; `qk-provisioning` remains exactly-100-only, grid transcript acceptance requires a later row after its camera bench gates, REQUIREMENTS QK-REQ-ENT-003 and TEST-ARCHITECTURE QK-TST-UNIT-004 remain the manual ceremony and are unchanged, and all physical-trigger lanes remain unchanged. | Two explicit dice profiles preserve the frozen manual fallback while making the higher-margin grid path primary only after its physical evidence gates, without exposing QKEC-1 or widening QR export. |
| 2026-08-28 | QK-DEC-118 | Owner ratifies M28 as a QK-DEC-108 software lane and fixes the v1 Simple Recovery and Inheritance watch-only SD export to one unencrypted BIP-129 `BSMS 1.0` descriptor record; Quantum Shelter receives no descriptor export. The normative wire authorities are bitcoin/bips commit `7fe0b034ec967b52a5a28276419117326df93263` BIP-129 plus BIP-389, with BIP-380 checksum semantics retained from QK-DEC-054. Named coordinator compatibility is bounded solely to Sparrow 2.5.4 commit `8871f4f1af528a4673fee6129373c884e3267860` together with its exact drongo gitlink `080cf3f7cf74133ba68b369065d0f2e7ea4337da`, and libnunchuk commit `01778037708d8e97a2c0b7ea95259a4b4323520d`; no Nunchuk application version is claimed. The Sparrow evidence is `Bip129.importWallet` lines 245 through 336 plus drongo `OutputDescriptor` checksum and multipath handling; the libnunchuk evidence is its BSMS, descriptor and wallet-descriptor parsers. The two exact checksummed QK-DEC-054 D descriptors remain authoritative. The emitted descriptor line is derived only after strict D validation by replacing each registered branch element in lockstep with BIP-389 `<0;1>`, preserving every origin, account xpub and signer position; expanding the candidate back to receive `/0/*` and change `/1/*`, recomputing both BIP-380 checksums and reparsing must reproduce both original D byte strings exactly before any artifact exists and again from reopened bytes before publication, otherwise `DescriptorRoundTripMismatch` rejects. The canonical file is exactly four 7-bit-ASCII lines encoded as UTF-8, each LF-terminated including the last: line 1 literal `BSMS 1.0`; line 2 the single checksummed `wsh(sortedmulti(2,.../<0;1>/*...))` descriptor; line 3 literal `No path restrictions`; and line 4 the exact mainnet receive address at branch 0, index 0 derived from D. No BOM, CR, NUL, blank, comment, wallet-name, label, wallet-id, birthday, rescan hint, duplicated xpub, instruction or fifth line exists. Hardened markers are apostrophes only, signer order is exactly D's registered A/B/C expression order, the descriptor checksum is correct, and the first address is self-derived; each byte form is load-bearing and locked by positive, mutation and rejection-precedence tests. Line 4 carries receive branch 0/index 0 only; after coordinator import, product comparison consumes the two existing bound `ProvisioningArtifacts.first_addresses` facts and requires the coordinator-derived receive 0 and change 0 addresses to match them, without adding a fifth line or deriving or reformatting a comparison fact in this exporter. The exact final name is `qk-<32-lowercase-hex-caller-nonce>-watch.bsms`, where the caller supplies the opaque 128-bit nonce and HOST generates no randomness; the temporary name appends `.tmp`. Publication reuses the complete QK-DEC-112 mock-SD sequence and failure categories: create-new temporary file, complete write, sync, close, reopen, exact length/SHA-256/full format and semantic verification, then atomic no-replace rename. A final name appears only after verification; no input or existing destination is overwritten or deleted, and any remaining temporary file is invalid and ignored. Fixtures use the unchanged registered M11 GOLDEN public, permanently NEVER-FUND NUMS descriptor lineage under QK-DEC-047/QK-DEC-059 and never reuse M26's known-private outputs; two separately written outside-Git implementations agree byte-for-byte, the exact future fixture header is frozen now, and complete SOURCE-REGISTER provenance lands in this row-first commit before that test file is added byte-verbatim. Existing ring-fenced hostile-input coverage exercises exact construction, strict reparse, all four lines, filename and lifecycle behavior. Compatibility claims extend no further than the three pinned code commits; other coordinators require their own pinned evidence. Excluded are QR watch-only export, Quantum Shelter descriptor export, every address-pack format, secrets or xprvs, coordinator API/database/wallet automation, real storage and power-cut evidence, camera, display, performance, target-resource and Gate claims; transaction exports and every physical-trigger lane remain unchanged. Owner pre-authorizes exactly this one combined QK-DEC-118/source-registration documentation commit to create at most two trailing ten-commit windows containing six source-or-test commits and four documentation commits; the authorization covers no other documentation commit. M28 implementation commits after this row are source or test only. The directed Core Architecture v1 section 4.2 entropy-mode introduction repair may land only after M28 has at least six source-or-test commits; if M28 is smaller it waits for a later lane, and no other documentation commit may land until every trailing ten-commit window is back at the standing seven-of-ten floor. | One reversible, byte-canonical standards record gives both named coordinator paths the same watch-only file while exact D round trips and the existing publish lifecycle preserve the registered wallet identity and fail-closed output boundary. |
| 2026-08-28 | QK-DEC-119 | Owner ratifies M29 as a QK-DEC-108 software lane and a HOST-only typed `ManualKeypad` ceremony-entry session joining QK-DEC-113's unchanged `HostProvisioningRun::from_dice` boundary to QK-DEC-116's borrow-only provisioning screens. In manual ceremony-input context, logical P0.1 keys `1` through `6` append the matching literal ASCII face, `CE/Delete` removes the latest unconfirmed face, red `C/CancelBack` cancels and wipes, and `Equals/ConfirmEnter` submits only a complete transcript. Four input mistakes are distinct named state-preserving rejections: keys `7`, `8/Up`, `9`, `0`, `Decimal`, `Plus`, `Minus`, `Multiply`, `Divide`, or `Percent` return `InvalidFaceKey`; a face key at count 100 returns `TranscriptFull`; `CE/Delete` at count zero returns `EmptyDelete`; and `Equals/ConfirmEnter` at any count other than exactly 100 returns `TranscriptCountIncomplete`. Each returns the unchanged count and buffer, appends or deletes nothing, performs no wipe, and leaves the ceremony in entry state; this visible outcome satisfies both no-silent-ignore and immediate-rejection policy. The recorded usability basis is that the P0.1 rows place `7`/`8`/`9` directly above `4`/`5`/`6`, so an adjacent stray press must not destroy a nearly complete transcript. Only red-C cancellation and QK-DEC-116's closed interruption/failure events wipe during entry: operation failure, media or card removal, session timeout, shutdown, restart, power loss, invalid state transition, and dropped scoped state. Seed-A, Signer-B, Signer-C, and A2 remain the exact fixed purpose order. Each purpose owns one fixed 100-byte secret buffer; all four buffers remain simultaneously retained through the complete ceremony because QK-DEC-113's final validator and all six pairwise reuse comparisons consume them together. No purpose-local wipe occurs after confirmation. All transcript storage is non-Clone, non-Copy, non-Debug, non-Display, has no byte-returning public owner, and is optimization-resistant wiped at ceremony completion and on every cancellation, interruption, failure, or drop. Only after four complete confirmed transcripts exist are their exact literal slices passed together, without filtering, normalization, rehashing or alternate validation, to the unchanged `HostProvisioningRun::from_dice`; its existing `InvalidDiceSymbol`, `DiceCount`, and `TranscriptReuse` outcomes remain the sole transcript-acceptance authority, any failure wipes and yields no provisioning run, and `qk-provisioning` remains byte-unchanged. Every accepted purpose follows M27's exact input, borrowed full-transcript echo, explicit user confirmation, and caller-supplied commitment sequence before the next purpose; M29 defines no commitment construction, layout, pagination, display string, logging or export path. Headless tests and one hash-registered ring-fenced fuzz target lock every key, all count/delete/confirm boundaries, state preservation for the four named input rejections, all six reuse pairs, simultaneous retention, every wiping event, drop cleanup, fixed order, no skip, exact-echo-only secret exposure, stable named outcomes, no panic and the complete four-purpose path. DiceGrid, camera, any `qk-provisioning` change, screen layout/pagination/timing under OD-05 and D-3, real keypad, cards, transports, target cleanup or performance, production behavior and every Gate claim are excluded; physical-trigger lanes remain unchanged. The QK-DEC-119 row lands before QK-DEC-120 and before implementation; all M29 commits and summaries are lane-attributed, and only the host-library and fuzz-registration overlaps are serialized with M30 under QK-DEC-108. | A fixed-memory typed entry owner can make keypad mistakes recoverable while preserving exact M26 validation, M27 echo order, pairwise reuse checks and deterministic wiping without widening the grid, display or target boundaries. |
| 2026-08-28 | QK-DEC-120 | Owner ratifies M30 as a QK-DEC-108 software lane, closes OD-09 for v1, and extends only the existing uncompressed-Base32 BBQr profile with file type `T` for Quantum Shelter's final raw-transaction QR return. The normative wire authority remains the already-registered coinkite/BBQr commit `8dc7ef07d0d520763cc0001a885ca8d29ac8719a`, specification blob `6a2e6b22cd8645a74d74b5e4ff1469f1a68fd962`, whose file-type table defines `T` as a ready-to-send Bitcoin wire transaction and whose encoding table defines `2` as unpadded RFC 4648 Base32; no new source pin or source-register edit lands. `qk-bbqr` remains a byte-only codec and gains an exact typed `P`/`T` selection for frame encoding, standalone frame decoding, and one-candidate-stream reassembly. The codec does not parse PSBTs or transactions; only `qk-host-sim`'s capability boundary may supply the bound M25 final raw-transaction artifact to type `T`. Existing type `P` behavior remains PSBT-only and byte-frozen except for the typed dispatch needed to distinguish the two ratified labels. Canonical type-T frames are exact uppercase ASCII `B$2T`, an uppercase two-digit base36 declared count, an uppercase two-digit base36 zero-based index, then unpadded uppercase RFC 4648 Base32. Type T inherits every QK-DEC-109 HOST candidate and rule unchanged: declared count 1 through 256; maximum frame 4,296 bytes; maximum body 4,288 symbols; maximum decoded part 2,680 bytes; maximum reassembled payload 262,144 bytes; identical duplicates at most the declared count; submissions at most twice the declared count and never above 512; all non-final decoded parts one identical multiple-of-five byte length; one nonempty final remainder no larger than that length; checked arithmetic before allocation or write; out-of-order unique parts accepted; one candidate stream; no silent restart; and exact full-index coverage before payload release. A fresh stream accepts only its caller-selected `P` or `T`; after establishment, encoding, file type and declared count disagreements retain the distinct `StreamEncodingMismatch`, `StreamFileTypeMismatch`, and `StreamPartCountMismatch` precedence, while unsupported fresh-stream type, malformed/canonicality failures, geometry, caps, duplicate classes, incomplete and already-complete states remain named rejections with no partial output mutation. `qk-host-sim` adds strictly sequential type-T framing only through the `QuantumShelter` tier's existing bound `RawTransactionArtifact`; Simple Recovery and Inheritance raw artifacts gain no new QR capability, finalized PSBTs retain type P, and all M25 SD names, bytes, lifecycle, failures, atomicity and tier behavior remain unchanged. Completed T reassembly is freshly matched to the exact artifact bytes and its bound serialized length, SHA-256, txid and wtxid facts; the QR path performs no broadcast, receipt or network action. Exact public permanently NEVER-FUND M25 raw-transaction facts supply the positive fixtures without a new signing lineage; tests lock byte-exact frames, P/T separation, cross-type mismatch, order, every cap and duplicate edge, exact completion and round-trip metadata. Hash-registered ring-fenced fuzz targets cover typed encode/decode/reassembly and the Quantum Shelter export boundary with named-error, no-panic, no-wrong-acceptance, stable replay and no-partial-mutation oracles. Bare QR, file types other than P/T, Hex or compressed encodings, camera, QR image recognition/rendering, display, broadcast, receipt, real transport, approval, cards, storage changes, production resources, performance and every Gate claim are excluded; physical-trigger lanes remain unchanged. The QK-DEC-120 row lands second and before implementation, with no other documentation commit; all M30 commits and summaries are lane-attributed, files remain disjoint from M29 except that host-library and fuzz-registration commits are explicitly serialized and recorded under QK-DEC-108. | The already-authorized wire table gives Quantum Shelter an unambiguous final-transaction QR label while the unchanged M22 bounds, M25 artifact facts and tier capability boundary prevent type confusion or transport widening. |
| 2026-08-28 | QK-DEC-121 | Owner supersedes the active QuietKey v1 2-of-3/Card-A/B/C architecture with Core Architecture v2, Single Card plus Two-Envelope Kit, and ratifies migration slice 1. The native wallet is mainnet P2WSH `sortedmulti(2,A,B)` at the unchanged BIP48 path; A is terminal-held, B is the one required Key Card, Card C and every A+C/B+C route are removed, and an optional byte-equivalent spare B card may be created only during setup. Normal signing remains A1+B. The universal Kit consists of two sealed tamper-evident envelopes and is a complete wallet only when both shares are combined; outside the Kit the standing no-single-object principle remains, while each Kit envelope alone reveals no payload. The exact 96-byte payload is Seed-A entropy || Seed-B entropy || A2. Four fresh dice transcripts remain ordered Seed-A, Signer-B, Kit-R, A2; ManualKeypad uses 100 literal ASCII d6 values and DiceGrid uses 125, with pairwise transcript reuse rejected across all four. For Kit-R let `T=SHA256(literal transcript)`, let the 16-byte ceremony identifier be the first 16 bytes of the final `wallet_id`, let `salt=SHA256(ASCII "QuietKey/QKEC-1" || ASCII "Kit-R" || wallet_id[0..16])`, let `PRK=HKDF-SHA256-Extract(salt,T)`, and let `R=HKDF-SHA256-Expand(PRK, ASCII "QuietKey/Kit-R/pad/v1" || 00 || wallet_id,96)`. Share 1 is R and share 2 is payload XOR R. All setup copies use that one R so any share 1 pairs with any share 2; transcript, T, salt, PRK, R and payload scratch are wiped after all copies are emitted, and no post-setup regeneration API or state exists. Share secrecy is computational and capped by Kit-R transcript entropy, not a 768-bit information-theoretic claim; non-regeneration is a terminal lifecycle invariant and not physical anti-copying. The canonical share frame is exactly 142 bytes: ASCII `QKKS`, version `01`, share index `01` or `02`, full `wallet_id[32]`, share[96], and checksum[8] equal to the first eight bytes of `SHA256(ASCII "QuietKey/KitShare/v1" || 00 || preceding 134 frame bytes)`. The QR is fixed version 10, ECC Q, byte mode, no ECI, minimum ISO penalty mask with lowest-number tie, and four-module quiet zone. The fallback is the exact frame packed MSB-first in the ratified M18 alphabet as 228 symbols with four required zero pad bits and four presentation-only lines of 57 with no separators; its terminal input method is deliberately deferred to the mode-locked scanner slice. Two same-index shares, cross-wallet shares, bad padding, checksum, version, geometry, or noncanonical encodings reject by distinct named errors. Two complete Kits are printed by default, every envelope states separately versus together posture, seal doubt or a missing card leads to sweep, and periodic seal checks are required. A Kit session chooses before scanning exactly one mode: Kit-Spend signs only when every input is proven owned and exactly one output pays a new descriptor with no change; Kit-Restore signs nothing and either provisions a replacement B card after explicit confirmation that the old card remains physically in hand or reprints A1 with a fresh nonce. If the card is missing, the only door is Kit-Spend; a second live card cannot be added after setup. UTXO completeness is a watch-only coordinator responsibility, and card possession/destruction, absence of another live card, and envelope integrity are external human facts, never machine proofs. Kit screens lock the scanner to Kit frames. The Kit is universal across tiers; tiers govern packaging and coordinator export only. The frozen M17 capsule, M18 alphabet, M21 ring fence, M22 BBQr, P0.1 keys, dice-only policy, BIP39 profile, derivation path, mainnet, PSBT v0, normal A1+B, and transaction BBQr remain unchanged. QK-FEE-POLICY-V2 retains every v1 warning threshold and fatal ceiling but uses the 71-byte 2-of-2 witnessScript and maximum input witness `1+1+2*(1+72)+(1+71)=220`; the reference 182-byte transaction is 238 vB and the 100-input cap is 11,036 vB. Review schema v3 binds that policy under exact domain `QuietKey/D-09/review/v3`; v1 and v2 review objects reject without translation. The public v2 GOLDEN lineage uses the first 100 bytes of infinite repetitions `123456`, `234561`, `345612`, `456123` for Seed-A, Signer-B, Kit-R, A2, applies literal SHA-256, empty-passphrase BIP39/BIP32 for A/B and the construction above for Kit-R, and is a narrow known-private fixture exception across the v2 fixture family. Every such fixture states `PERMANENTLY NEVER-FUND PUBLIC PRIVATE MATERIAL`, is screened against all committed KAT and NUMS public-key sets, and is produced outside Git by two separately written constructors agreeing byte-for-byte with complete provenance in the first fixture commit. The exact v2 geometry is descriptor body/full 297/306 bytes, wallet-id transcript 613 bytes, origin starts [18,157], xpub starts [41,180], branch positions [153,292], 71-byte witnessScript, four bounded CKDpub derivations per route, and M28 multipath body/descriptor/record 305/314/408. Owner authorizes exactly eight consecutive documentation commits before code: this row; Core v2 plus manifest/source registration; root authority surfaces; threat/requirements/lifecycle; tests/traceability; Gates/budgets/roadmap/open items; F2/F8/M20; and F3/F4/differential/fuzz dispositions. The authorization covers only those commits and affected trailing windows and expires after seven consecutive source-or-test commits restore every trailing ten-commit window to at least seven source-or-test commits. The eleven migration slices are descriptor core and GOLDEN lineage; review schema and fee policy; signing/finalization/Core differential; provisioning and Kit-R ownership; screen topology; two-key coordinator export; Kit frame/combine; setup-only kit generation/print; mode-locked scanner; Kit-Restore; and Kit-Spend, with Owner plus LOA verification before each successor. Slice 1 is ratified now and is limited exactly to `host/qk-descriptor/Cargo.toml`, `src/lib.rs`, `src/descriptor.rs`, `tests/descriptor.rs`, `tests/allocation.rs`, `tests/surface.rs`, the three descriptor fixture files, `fuzz/fuzz_targets/qk_descriptor.rs`, `fuzz/corpus/qk_descriptor/`, active `fuzz/CORPUS-MANIFEST.tsv`, and the qk-descriptor registration logic in `tools/check-fuzz-corpora.sh`; it changes no downstream parser, review, signing, provisioning, export, screen, card, camera, or physical behavior. No new 2-of-3 implementation work is authorized. | One explicit v2 authority and dependency-ordered migration removes the third-card recovery topology while preserving frozen primitives and making the complete-wallet Kit's cryptographic, lifecycle, physical, and coordinator boundaries exact before code changes. |
| 2026-08-29 | QK-DEC-122 | Owner ratifies v2 migration slice 2 as the HOST-only parallel implementation of review schema v3 and `QK-FEE-POLICY-V2`; it adds no signing, approval, session, export, card, camera, display, target-resource, performance, production, or Gate claim, and the frozen v1/schema-v1/schema-v2 public behavior remains available only as migration residue for later slices. The sole v3 construction path accepts one immutable parsed PSBT v0, one caller-authenticated `DescriptorPairV2`, and the existing retained review context; it reruns the full-previous-transaction and optional-witness-utxo equality rules, exact two-role descriptor ownership and change reconstruction with no skip path, the six-template recipient policy including bounded Script-parsed OP_RETURN, checked MoneyRange/amount/fee arithmetic, and effective `SIGHASH_ALL`, while carrying no signature-verification, threshold-completeness, authorization, or export claim. Canonical v3 bytes are in this exact order: schema version, network, `InputSource`, S0 SHA-256, `wallet_id`, A then B origin fingerprints, fee-policy identifier, exact unsigned-transaction bytes, transaction version, locktime, ordered input count and facts, ordered output count and ownership/recipient facts, checked total input amount, total output amount, fee, estimated vsize, internal milli-sat/vB fee rate, warning count, and ordered warning tags; each input binds index, wire-order outpoint txid, vout, proven prevout amount and scriptPubKey, sequence, effective sighash, branch and child index, and each output binds index, amount, exact scriptPubKey, and its exact recipient, change, or self-transfer classification. Every integer is fixed-width little-endian, every variable field has a fixed-width u32 little-endian length, and no review-level CompactSize exists. The schema byte is exactly `03`, the policy identifier is exactly ASCII `QK-FEE-POLICY-V2` inside canonical bytes, and the review hash is SHA-256 over exact ASCII `QuietKey/D-09/review/v3` || `00` || canonical bytes; exact-identity checking rejects a first byte other than `03`, including v1 and v2, as `UnsupportedReviewSchemaVersion` before any `CanonicalReviewMismatch`, with no translation or fallback. `QK-FEE-POLICY-V2` uses BIP141 with the exact per-input witness `1+1+2*(1+72)+(1+71)=220` weight units, so the 182-byte reference transaction is 238 vB and the existing 5,535-byte/100-input HOST candidates yield 11,036 vB; fee rate is floor(fee-sats×1000/vsize), all arithmetic is checked, and the independently evaluated fixed warning order and inclusive boundaries remain `W-FEERATE-LOW` below 1000, `W-FEERATE-HIGH` at or above 200000, `W-FEESHARE-HIGH` when checked fee×20 is at or above total input value, and `W-FEEABS-HIGH` at or above 1000000 sats, while fee strictly above 5000000 sats is the named fatal rejection and no warning suppresses another. Two fingerprints and the unchanged field grammar give HOST candidates of 18,930 canonical bytes, 18,954 domain/separator/canonical transcript bytes, and 528 child derivations across 132 routes; these are const-derived scaffold caps under the standing open resource rows, not registry-value authorizations. Slice-2 fixtures use the QK-DEC-121 public v2 GOLDEN lineage, carry `PERMANENTLY NEVER-FUND PUBLIC PRIVATE MATERIAL`, and land with complete first-commit provenance from two separately written outside-Git constructors agreeing byte-for-byte; the migrated ring-fenced `qk_psbt_m23` target retains named-error, no-panic, no-wrong-acceptance, exact-schema, policy-boundary, and no-partial-output oracles with a minimized hash-registered corpus, while the `qk_host_sim_m23` target remains unchanged until its assigned successor slice. The v2 inventory is corrected: `qk-card-trace` moves from unaffected to revise, limited to `host/qk-card-trace/tests/registration.rs`. Owner authorized the already-landed registration repair commit `7d3082f63c46da2ece2beec466ec52cbaa7182de`, which replaced only superseded v1 documentation assertions in that test and restored the full green tree before slice 1. Owner superseded the earlier descriptor-fixture destination: commit `5b7cb77cc3f2f9ca7e1df6f3b0a2251e10d10f85` created `host/qk-psbt/tests/fixtures/descriptor_pairs_v1.txt` byte-identical at SHA-256 `3b295901bf29799a875965b4a86a50847f8c9e0d03802c26ad873e54f14ab350` and changed only `DESCRIPTOR_FIXTURE` in `host/qk-psbt/tests/descriptor_ownership.rs`, while the existing 45-case `descriptor_ownership.txt` stayed byte-identical at SHA-256 `79fee6c8b2ff3260f243cbe5ac55d6f954f1ad4d39c2d256df44609200889528`; no qk-psbt production behavior changed in that relocation. This slice is limited exactly to this row in `docs/DECISION-LOG.md`; `host/qk-psbt/src/lib.rs`, `limits.rs`, `semantic.rs`, and new `review_v3.rs`; new `host/qk-psbt/tests/review_v3.rs` and `tests/fixtures/review_v3.txt`; `fuzz/fuzz_targets/qk_psbt_m23.rs`, `fuzz/corpus/qk_psbt_m23/`, `fuzz/CORPUS-MANIFEST-M23.tsv`, and the corresponding qk-psbt-v3 registration logic in `tools/check-fuzz-corpora.sh`. No other file changes, no modification of `review_v2.rs` or its fixtures, and no downstream host-simulator migration are authorized in slice 2. | A separately typed, byte-canonical v3 commitment can bind the two-key wallet and corrected witness economics now while preserving prior-schema migration residue and deferring signing and wrapper integration to their ordered slices. |
| 2026-08-29 | QK-DEC-123 | Owner ratifies v2 migration slice 3 as the HOST-only, non-authorizing normal-A1+B continuation from QK-DEC-122 review schema v3 through signing, native P2WSH 2-of-2 finalization, exact raw-transaction extraction, and a separately invoked Bitcoin Core differential suite. A parallel `ReviewReadyV3Workflow` owns exact immutable S0, one caller-authenticated `DescriptorPairV2`, its `ReviewV3`, and its `ReviewV3Hash`; before any signing allocation, cryptographic call, or mutation, the continuation reparses only retained S0, rebuilds exact schema v3 and `QK-FEE-POLICY-V2`, and requires typed-fact and review-hash equality with no schema-v1/v2 translation or fallback. This continuation carries no physical approval, cycle-token, card-session, transport, export, or production authorization claim; those integrations remain assigned to later slices. Every pre-existing partial signature is verified without threshold skipping against its descriptor-derived A or B key and exact BIP143 `SIGHASH_ALL` digest; any foreign or third key, malformed DER, high-S value, wrong sighash byte, descriptor mismatch, or failed verification rejects under a stable named category, while an already-complete valid input receives no new partial-signature record. Terminal A signing uses only the unchanged opaque zeroizing `qk-secp::SecretKey` and pinned libsecp256k1 boundary with RFC6979, low-S normalization, strict DER serialization, and immediate self-verification. The sole mock-card input names input index and role B and supplies DER-only bytes; the B public key and digest are derived internally and the signature is syntax-, low-S-, role-, and digest-verified before insertion. No v2 role-C type, path, fixture, or branch exists. Work order is ascending input index then ascending compressed public-key bytes; each input contains exactly one A and one B signature at completion, at most two records may be inserted per input and 200 transaction-wide, and duplicate signature, duplicate role, occupied-role conflict, already-complete, over-threshold, incomplete, invalid-B, missing-terminal, unexpected-terminal, and terminal-key mismatch are distinct closed rejections. Each insertion is the sole type-02 byte delta, followed by complete parse, M5 fixed-point serialization, two-role cryptographic verification, and v3 semantic rebind. The approved pre-insertion `ReviewV3` and hash are never recomputed or replaced: a candidate's mechanically changed S0 SHA-256 is transition evidence only, while every other canonical-v3 field must remain byte-equal and the one-record delta must be exact. Failure consumes the continuation and releases no signature, intermediate PSBT, witness, or transaction. Finalization accepts only exactly two coherent A/B type-06 derivations at one branch/index, an absent type-05 or one byte-equal to the reconstructed script, no pre-existing type-07 or type-08 field, and exactly both verified A/B signatures. The witness is exactly four items: zero-length CHECKMULTISIG dummy, both DER-plus-`01` signatures ordered by their positions in the BIP67-sorted script, and the exact 71-byte script; order mismatch is named. The finalized PSBT replaces types 02 through 06 with exactly one empty-key-data type-08 record per input, preserves accepted UTXO, unknown, proprietary, global, and output records byte-for-byte under canonical ordering, reparses to an M5 fixed point, and proves the complete allowed delta. Raw extraction splices exact unsigned-transaction slices around BIP141 marker/flag `00 01` and exact witnesses, reparses through EOF, proves witness-stripped equality and type-08 equality, reconstructs every digest, verifies both signatures again, and then computes raw-order txid and wtxid. Four new const-derived QK-DEC-034 HOST candidates are exact and use no v1 limit identifier: maximum input witness `220 = 1+1+2*(1+72)+(1+71)`, maximum type-08 record `223 = 1+1+1+220`, maximum raw transaction `27,537 = 5,535+2+100*220`, and minimum finalized-PSBT shrink per input `121 = 2*108+2*64-223`; no registry or target allocation value is frozen. Slice-3 fixtures extend the registered QK-DEC-121 deterministic public v2 GOLDEN lineage rather than creating a new authority. Every fixture states `PERMANENTLY NEVER-FUND PUBLIC PRIVATE MATERIAL`, records the public Seed-A and Signer-B transcript construction and any deliberately exposed derived scalar required by the HOST test seam, uses reproducible RFC6979 signatures, and carries complete first-commit provenance from two separately written outside-Git constructors agreeing byte-for-byte on keys, derivations, scripts, digests, signatures, PSBT stages, witnesses, transactions, identifiers, review-v3 facts, and Core vectors. Route public keys and signature r values are screened against all committed KAT, NUMS, and fixture sets plus the standing encodings and small-multiple range; same-lineage GOLDEN matches are classified explicitly, and generators plus intermediates are destroyed before commit under QK-DEC-047. A new `differential/v2-slice3-bitcoin-core/` suite preserves every `differential/m16-bitcoin-core/` byte as frozen v1 history and reuses, without repinning, the SOURCE-REGISTER-pinned Bitcoin Core v28.0.0 archive, binaries, isolated-regtest procedure, and network-independent raw-transaction boundary. Its A/B vectors bind schema-v3 and policy-v2 facts, two derivations, two partial signatures, the 71-byte script, and the four-item witness. Unknown-free cases require `finalizepsbt` PSBT and raw-transaction byte equality; unknown-bearing cases require decoded-field and complete-record equivalence plus only the registered numeric-type-255/256 order delta. `testmempoolaccept` must accept the positives and reject exact swapped-witness, mutated-signature, and mutated-base controls; one normalized transcript records all commands, hashes, counts, results, and clean daemon exit. The migrated ring fence covers both `qk_host_sim_m23` review-ready and `qk_host_sim_m24` signing/finalization targets with separately executed 100,000-input AddressSanitizer campaigns, two-copy minimization agreement, registered corpus manifests, clean replay, named-error/no-panic/no-wrong-acceptance/no-partial-output oracles, and no v1 fallback. Exact slice scope is only this row plus fixture and transcript registration rows in `docs/SOURCE-REGISTER.md`; `host/qk-psbt/src/lib.rs` and `semantic.rs`, new `tests/signature_verification_v2.rs` and `tests/fixtures/signing_finalization_v2.txt`; `host/qk-host-sim/src/lib.rs`, a constructor-only addition in `src/finalization.rs`, new `src/review_ready_v3.rs`, `src/signing_v2.rs`, and `src/finalization_v2.rs`, and new `tests/review_ready_v3.rs`, `tests/signing_v2.rs`, and `tests/finalization_v2.rs`; `fuzz/README.md`, new `fuzz/CAMPAIGN-009.md`, `fuzz/fuzz_targets/qk_host_sim_m23.rs` and `qk_host_sim_m24.rs`, `fuzz/corpus/qk_host_sim_m23/` and `qk_host_sim_m24/`, `fuzz/CORPUS-MANIFEST-M23.tsv`, `fuzz/CORPUS-MANIFEST-M24.tsv`, and their registration logic in `tools/check-fuzz-corpora.sh`; and new `differential/v2-slice3-bitcoin-core/README.md`, `CAP-DERIVATIONS.md`, `run.py`, `fixtures/v2_core_vectors.txt`, `evidence/TRANSCRIPT-SCHEMA.txt`, and one `evidence/run-<UTC>-<HEAD>.txt`. Existing v1 review-ready, M24 signing, insertion behavior, M16 finalization behavior, M16 fixtures, M16 runner, M16 transcript, qk-secp sources/vendor tree, descriptor, provisioning, export, screen, card, camera, and hardware files remain byte-frozen. Kit-Spend, every Kit-Restore signing path, real card/APDU/session behavior, approval integration, export/SD/QR, camera, display, target cleanup, constant-time, resource, performance, production, test-status, registry-freeze, and Gate claims are excluded; QK-TST-REH-004 remains a later card-dependent rescue rehearsal and is not satisfied here. | A parallel v2 continuation can prove exact A/B signatures, canonical 71-byte witnesses, byte-rebound final output, and Core acceptance while leaving the v1 evidence chain intact and every unavailable approval, card, transport, target, and Kit door outside the claim. |
| 2026-08-29 | QK-DEC-124 | Owner supplements QK-DEC-123 solely to preserve and register exactly two Core differential transcripts: the first failed run and exactly one repaired rerun. The immutable first transcript is `differential/v2-slice3-bitcoin-core/evidence/run-20260829T010244Z-8f8106c.txt`, 15,716 bytes/141 LF, SHA-256 `0dd66791f408a90733000dcd9bfc0aaae92e94f27dd38505524449645e0cd110`, from repository commit `8f8106c0f0d1be5ca5a57617723d7181dc9041ef`; it reached Core v28.0.0 `finalizepsbt` and then failed its unknown-free byte-equality assertion. The exact cause was in `differential/v2-slice3-bitcoin-core/fixtures/v2_core_vectors.txt`: the initial fixture embedded a 179-byte witness-serialized height-one coinbase as PSBT `non_witness_utxo`, while Core returned the complete PSBT with that previous transaction canonically serialized in its 143-byte legacy form, so the finalized PSBT bytes diverged even though the spend data agreed. The initial `run.py` also stopped Core after its temporary directory had been removed, so the preserved failure records `daemon_stop_rpc_error_type=FileNotFoundError`; that cleanup sequencing was a differential-harness defect, separate from the PSBT comparison failure. Repair commit `99d6e39bb8fa60945bd1ed7e190be8705004f9e5` changed no product code and no environment: it changed only the slice-3 differential fixture, harness, transcript schema, harness README, and their SOURCE-REGISTER row. The repair replaced the Core-only seed with a 96-byte legacy coinbase carrying the same receive-zero P2WSH output and no witness marker/flag, witness stack, reserved value, or witness-commitment output; `run.py` now verifies that exact construction, stops the live Core process before temporary-directory cleanup, and records distinct no-process, already-exited, live-stop, and cleanup-error outcomes. The pinned Core v28.0.0 archive, binaries, Darwin x86_64 host, and isolated regtest procedure are unchanged. The failed transcript is retained as evidence of failure only and makes no acceptance claim. After this row lands, exactly one rerun may execute against the repaired tree; its transcript must be committed immutable and hash-registered together with the failed transcript under their exact paths. If that rerun fails, both transcripts are preserved and slice 3 stops; no third run or transcript is authorized. This row expands QK-DEC-123 only to this second evidence transcript, its two SOURCE-REGISTER registrations, and this supplemental row; every product, fixture-authority, harness, physical-lane, exclusion, and claim boundary otherwise remains unchanged. | Preserving the diagnosed failure beside one pre-authorized repaired rerun makes the Core evidence sequence complete without hiding the first result or widening the product boundary. |
| 2026-08-29 | QK-DEC-125 | Owner ratifies v2 migration slice 4 as the HOST-only provisioning and Kit-R secret-ownership continuation of QK-DEC-121. A new parallel `HostProvisioningRunV2::from_manual_dice` is the sole v2 entry and accepts exactly four separately supplied ManualKeypad transcripts in fixed order Seed-A, Signer-B, Kit-R, A2; each transcript is exactly 100 literal ASCII bytes `1` through `6`, every other byte is `InvalidDiceSymbol`, any shorter or longer transcript is `DiceCount`, and exact equality across any of the six purpose pairs is `TranscriptReuse` after all four transcripts pass shape validation. No filtering, normalization, correction, guessing, retry, search, cross-session reuse claim, general transcript API, or 125-byte DiceGrid acceptance exists in this slice: DiceGrid remains unavailable until its QK-REQ-ENT-004 physical gates close and a later Owner row authorizes that product entry. The v2 path reuses the byte-unchanged M26 literal-hash validator, BIP39 English checksum mapping, NFKD fixed-point handling, PBKDF2-HMAC-SHA512 with exactly 2,048 rounds and fixed empty passphrase, private BIP32 derivation at `m/48'/0'/0'/2'`, BIP173 mainnet P2WSH encoding, volatile-wiping fixed secret owner, and the frozen M17 A1 capsule; it samples no entropy and generates no nonce. `SHA256(Seed-A transcript)` and `SHA256(Signer-B transcript)` are the distinct 32-byte BIP39 entropies for account authorities A and B, while `SHA256(A2 transcript)` is A2; only those two private account chains are derived, and no Signer-C, Card-C, third account, third origin, third xpub, or three-key descriptor is created on the v2 path. The two account xpubs construct and strictly reparse through `qk-descriptor`'s v2 surface the exact 306-byte receive and change descriptors, 613-byte wallet-ID transcript, wallet_id, branch-zero/index-zero and branch-one/index-zero 71-byte-witnessScript P2WSH scripts, and their 62-byte mainnet addresses. For the Kit-R transcript, `T=SHA256(literal transcript)`, `salt=SHA256(ASCII "QuietKey/QKEC-1" || ASCII "Kit-R" || wallet_id[0..16])`, `PRK=HKDF-SHA256-Extract(salt,T)`, and `R=HKDF-SHA256-Expand(PRK, ASCII "QuietKey/Kit-R/pad/v1" || 00 || wallet_id,96)` exactly; the first 16 wallet-id bytes are the only ceremony identifier and no separate state is introduced. The setup payload is exactly `Seed-A entropy[32] || Seed-B entropy[32] || A2[32]`, where Seed-B entropy is the literal Signer-B transcript hash. `HostProvisioningRunV2` owns the exact setup payload and R as two fixed `Secret<96>` values. `HostProvisioningRunV2::encrypt_a1` takes one caller-supplied 96-bit nonce by mutable borrow and emits the exact public `ProvisioningArtifactsV2` facts—two 111-byte account xpubs, two 306-byte descriptors, wallet_id, two 34-byte scripts, two 62-byte addresses, and one 67-byte authenticated A1 capsule—without consuming or exposing either secret owner. Nonce state commits only after capsule construction; a repeated equal nonce is `NonceReuse` and any later different nonce is `AlreadyEncrypted`. The run has no public secret field, byte accessor, serializer, formatter, logger, clone, copy, debug, display, equality, snapshot, setup-output, or regeneration operation, and both secret owners are optimization-resistant wiped on drop; transcript buffers, mnemonic, BIP39 seeds, account private material, T, salt, PRK, derivation scratch, and every unsuccessful partial owner are wiped on every return path. A1 plaintext remains only a private input to the unchanged capsule operation, and failure returns no public artifact. This original-setup owner is retained only for a later consuming slice-8 operation; this slice computes no XOR share, frame, checksum, copy, QR, fallback string, page, or print artifact and exposes no post-setup regeneration API. Share secrecy remains computational and capped by Kit-R transcript unpredictability; HOST zeroization evidence supplies no target-remanence claim. The unsuffixed M26 `HostProvisioningRun`, `ProvisioningArtifacts`, QKEC-1 TLV entry, Signer-C behavior, three-account geometry, fixture, tests, two registered M26 fuzz targets, corpora, manifest, and hashes remain byte-frozen migration residue solely for downstream v1 HOST code not yet migrated; they are not v2 product entry points, receive no new behavior, and are neither overwritten nor re-registered. Slice-4 fixtures extend only the QK-DEC-121 v2 GOLDEN lineage using its four exact public 100-byte transcripts and one fixed public test nonce, state `PERMANENTLY NEVER-FUND PUBLIC PRIVATE MATERIAL`, and lock literal transcript hashes, BIP39/BIP32 A/B facts, both descriptors, wallet_id, first scripts and addresses, A1 bytes, T, salt, PRK, R, and payload through private in-crate checks while the public surface exposes only the listed public artifacts and opaque run type. Complete provenance lands before the fixture's byte-verbatim import: two separately written outside-Git constructors agree byte-for-byte on all 66 semantic fields; every key-bearing fact is an exact already-screened QK-DEC-121 same-lineage value, a fresh tracked-tree scan finds only registered same-lineage occurrences, and the two constructor roots, duplicate outputs, screening scratch, and transient private intermediates are destroyed before the row-first commit. Two new ring-fenced targets, `qk_provisioning_v2_inputs` and `qk_provisioning_v2_chain`, leave the registered M26 targets and corpora untouched; they cover exact count/symbol/reuse precedence, every reuse pair, deterministic A/B/D/A1/Kit-R construction, strict v2 reparse, opaque-secret surface, drop/failure cleanup, no partial output, named errors, repeat consistency, and forbidden third-role or regeneration behavior. Each receives a separately persisted, minimized, hash-registered corpus and a 100,000-input AddressSanitizer campaign under the pinned nightly with two-copy minimization agreement and zero panic, timeout, sanitizer finding, wrong acceptance, wrong named rejection, partial artifact, or residual fuzz artifact. This slice adds no external dependency or source authority and reuses the existing pinned BIP39, BIP173, SHA-2, HMAC, HKDF, RIPEMD-160, libsecp256k1, A1, and v2 descriptor authorities. Exact slice scope is only this row in `docs/DECISION-LOG.md`; the v2 fixture/provenance row in `docs/SOURCE-REGISTER.md`; `host/qk-provisioning/Cargo.toml`, `src/lib.rs`, new `src/descriptor_build_v2.rs`, new `src/kit_r.rs`, new `src/fixture_tests_v2.rs`, `tests/surface.rs`, new `tests/provisioning_v2.rs`, and new `tests/fixtures/provisioning_v2.txt`; `fuzz/Cargo.toml`, `README.md`, `run-bounded.sh`, `replay-corpus.sh`, `reproduce.sh`, `minimize-corpus.sh`, `minimize-finding.sh`, new `fuzz_targets/qk_provisioning_v2_inputs.rs`, new `fuzz_targets/qk_provisioning_v2_chain.rs`, new `corpus/qk_provisioning_v2_inputs/`, new `corpus/qk_provisioning_v2_chain/`, new `CORPUS-MANIFEST-V2-S4.tsv`, and new `CAMPAIGN-010.md`; and the matching new-target registration logic in `tools/check-fuzz-corpora.sh`. Every other file is excluded, including the existing M26 source modules, fixture, targets, corpora and manifest; qk-a1, qk-descriptor, qk-secp, qk-bip32 and qk-host-sim; DiceGrid and camera work; QKEC product entry, entropy or nonce generation; card I/O or provisioning; screen, ceremony, coordinator-export or transport integration; Kit share/frame/combine/QR/fallback/print generation; scanner, Kit-Restore or Kit-Spend; persistence, real storage, power-cut, constant-time, allocation-registry, target-resource, performance, production, or Gate claims. | A separate v2 run can derive the two-key public wallet and retain exactly one fixed-memory Kit payload and pad across A1 creation without exposing plaintext, while physical DiceGrid release, share encoding, printing, scanning, restore and spend remain in their dependency-ordered slices and all registered v1 evidence stays immutable. |
| 2026-08-29 | QK-DEC-126 | Owner ratifies v2 migration slice 5 as a parallel HOST-only deterministic screen-flow topology over the already-ratified v2 fact seams. The active route families are original `Setup`, normal `A1B`, and one `Kit` root that requires the user to select exactly one typed `KitSpend` or `KitRestore` door and confirm it before either share screen; the selected door is then immutable for the session. No v2 type, event, screen, factor, action, fixture, or transition names Card C, Signer C, `RecoveryA1C`, `RecoveryBC`, `ProvisionC`, `VerifyC`, a third account, or a general Kit signing route. The exact closed screen kinds are `SetupStart`, `TierSelection`, `EntropyModeSelection`, `CeremonyInput`, `CeremonyEcho`, `CeremonyConfirm`, `CeremonyCommitment`, `DerivationExplanation`, `ProvisioningResult`, `ProvisionB`, `VerifyB`, `SpareBSelection`, `ProvisionSpareB`, `VerifySpareB`, `CreateA1`, `ScanBackA1`, `CoordinatorMaterial`, `CreateTwoKits`, `VerifyTwoKits`, `Rehearsal`, `SetupReady`, `NormalStart`, `Transport`, `Intake`, `FactorB`, `FactorA1`, `Validation`, `ReviewOverview`, `ReviewArithmetic`, `ReviewRecipient`, `ReviewChange`, `ReviewOpReturn`, `ReviewLocktime`, `ReviewSequence`, `ReviewFeePolicy`, `FinalApproval`, `AwaitingSigning`, `Export`, `TransactionResult`, `KitStart`, `KitDoorSelection`, `KitDoorConfirmation`, `ScanKitShareOne`, `ScanKitShareTwo`, `CombineKitShares`, `KitSpendTransaction`, `KitSpendValidation`, `KitSpendCompleteness`, `KitSpendDeferred`, `KitRestoreActionSelection`, `CardRemainsConfirmation`, and `KitRestoreDeferred`. The Setup purpose order is exactly Seed-A, Signer-B, Kit-R, A2. The parallel `ManualKeypadSessionV2` carries QK-DEC-119's exact P0.1 mapping, four named state-preserving entry rejections, 100-byte fixed secret buffer per purpose, simultaneous four-buffer retention, six-pair reuse rejection, exact echo then explicit confirmation then caller-supplied commitment order, and wipe-on-completion/cancel/interruption/failure/drop behavior, but changes the third purpose only from Signer-C to Kit-R and submits all four exact slices only to unchanged `HostProvisioningRunV2::from_manual_dice`; it creates no alternate validation, hash, normalization, filter, retry, owned transcript output, or public secret surface. `DiceGrid` remains visible only as the architecturally named unavailable choice: attempting to advance it returns the distinct state-preserving `DiceGridUnavailable`, retains the selection screen, and accepts no 125-byte unit or camera event; only `ManualKeypad` enters ceremony until the four QK-REQ-ENT-004 physical gates close and a later Owner row authorizes that entry. Setup proceeds through the exact public `ProvisioningArtifactsV2` result, required B provision and verification, an explicit optional-spare selection permitting either zero or one byte-equivalent spare B provision and verification only in this original setup path, A1 creation and scan-back, coordinator material, abstract creation and verification of exactly two complete Kits, rehearsal, `SetupReady`, and `CompletedWiped`. Card exchanges, coordinator export, Kit construction, printing, and verification are topology-only successful-operation assertions here; they produce no card, share, frame, file, page, QR, fallback, or transport capability and remain owned by physical card work and slices 6 and 8. Normal A1+B order is start, hostile transport and immutable intake, abstract B then A1 factor binding, schema-v3 validation, the complete fixed review visitation ledger, final approval hold, matching signing outcome, export, exact transaction-result facts, and `CompletedWiped`. Review order is overview, checked arithmetic, every non-change non-OP_RETURN recipient or self-transfer in transaction order, every change in order, every OP_RETURN in order, locktime, every input sequence/direct-RBF fact in order, exact QK-FEE-POLICY-V2 facts and warnings in canonical order, then final approval. Review and result screens borrow and select only existing `ReviewReadyV3`/`ReviewV3`/`ReviewV3Hash` and `ExportArtifacts`/`SdArtifactMetadata` facts; the topology derives, hashes, sums, classifies, truncates, converts, normalizes, or reformats no displayed fact. Each normal flow has opaque process-unique provenance and a monotonic hold token; completed approval retains one inseparable token plus exact `ReviewV3Hash`, accepts a signing outcome only with that identity, and rejects every transport, camera, or intake yield after the hold as `PostApprovalYield`. It calls no signer, card, finalizer, exporter, or approval authority. The Kit common prefix is start, door selection and confirmation, first Kit-share assertion, second Kit-share assertion, and abstract combination. The two share screens accept only the typed `KitShareAccepted` topology assertion; PSBT, A1, BBQr transaction, coordinator, generic camera, transport, and intake events are `KitScannerModeMismatch`. No bytes are parsed, authenticated, combined, or released and no scanner claim is made before slices 7 and 9. After combination, `KitSpend` orders an abstract sweep-transaction presentation, abstract validation, then the factual external coordinator-UTXO-completeness confirmation before `KitSpendDeferred`; that deferred screen wipes into a typed slice-11 terminal; it exposes no review, approval, signing, finalization, export, or sweep capability until slice 11 supplies a bound object proving every input owned, exactly one new-descriptor output, and no change, OP_RETURN, second output, or unowned input. `KitRestore` chooses exactly replacement B or A1 reprint after abstract combination. Replacement B requires a typed human statement that the old B remains are physically in hand; a false or missing-card statement wipes with `MissingCardRequiresKitSpend` and requires a fresh Kit-Spend session without changing the locked door. Either allowed choice ends at `KitRestoreDeferred`, then wipes into a typed slice-10 deferred terminal. It accepts no transaction intake, review, hold, signing, finalization, export, second-live-card, Kit-regeneration, or general wallet-output event; attempts to enter a signing-class event fail as `RestoreSigningProhibited`. The later restore slice alone supplies fresh-nonce A1 or permitted replacement-card facts and the mandatory fresh-wallet-migration result; this slice does not return an opened-Kit wallet to normal operation. The terminal set is exactly `CompletedWiped`, `DeferredWiped` carrying `KitSpendSlice11` or `KitRestoreSlice10`, and `FailedWiped` carrying one closed non-secret reason. Cancellation, operation failure, media removal, card removal, timeout, shutdown, restart, and power loss wipe and terminate from every nonterminal state. Invalid transition, door-switch attempt, scanner-mode mismatch, incomplete review, review-identity mismatch, post-approval yield, prohibited restore signing, or missing-card restore wipes and terminates from every state in which that route-specific condition can occur; later events return only `Finished`. Logical wiping clears every retained fact reference, visit marker, transient echo borrow, pending commitment, door/action/human-statement selection, provenance/token, and approval identity. It makes no physical-memory or target-remanence claim. Exhaustive headless tests cover Setup with and without the spare, all ManualKeypad keys and boundaries, all six reuse pairs, normal schema-v3 review order and fact ties, early/stale/foreign holds, no-yield, both Kit doors, both restore choices, door immutability, mode lock, false-remains routing, every closed interruption from every reachable state, deferred boundaries, stable wiped termination, and absence of every C semantic from the v2 public surface. Two new ring-fenced targets, `qk_host_sim_v2_s5_screen` and `qk_host_sim_v2_s5_manual_keypad`, receive separately persisted, minimized, hash-registered corpora and 100,000-input AddressSanitizer campaigns with deterministic transition, named outcome, no panic, no wrong acceptance, no skip, no secret output outside the exact transient echo, no post-hold yield, no Kit capability release, and wipe/deferred-terminal oracles. They reuse registered v2 slice-2/3/4 and M25 public facts and add no source authority, fixture lineage, registry dependency, or product behavior. Owner's supplemental QK-DEC-125 authorization is recorded: repair commit `e4d49d43f220e16cc142290f7aad0da75c550209` changed exactly `fuzz/Cargo.toml` to place `qk-provisioning/fuzzing` in the root default feature while restoring the dependency guard's canonical path declaration, and `fuzz/DEPENDENCY-ALLOWLIST.tsv` only in its applicable header and the `qk-provisioning` path checksum to `2f8d69079a30e9484eecd0110127019be438ee492034ecd65ce6ee8e7cdc8dc1`; it added no registry dependency and is the complete slice-4 scope amendment. Exact slice-5 scope is only this row; `host/qk-host-sim/src/lib.rs`; new `src/screen_flow_v2.rs` and `src/manual_keypad_v2.rs`; new `tests/screen_flow_v2.rs`, `tests/manual_keypad_v2.rs`, and `tests/surface_v2.rs`; `fuzz/Cargo.toml`, `README.md`, `run-bounded.sh`, `replay-corpus.sh`, `reproduce.sh`, `minimize-corpus.sh`, and `minimize-finding.sh`; new `fuzz_targets/qk_host_sim_v2_s5_screen.rs` and `qk_host_sim_v2_s5_manual_keypad.rs`; new `corpus/qk_host_sim_v2_s5_screen/` and `corpus/qk_host_sim_v2_s5_manual_keypad/`; new `CORPUS-MANIFEST-V2-S5.tsv` and `CAMPAIGN-011.md`; and the matching new-target registration logic in `tools/check-fuzz-corpora.sh`. Every existing file not named is excluded, including `host/qk-host-sim/Cargo.toml`, v1 `screen_flow.rs`, `manual_keypad.rs`, every M27/M29 test/target/corpus/manifest, all provisioning, descriptor, PSBT, signing, finalization, export and card production modules, and all existing fixtures. Pixel rendering, strings, fonts, layout, pagination, display/keypad drivers, real camera/card/media/transport, actual Kit frame/combine/print/scanner/restore/spend operations, approval integration, persistence, power-cut, constant-time, target resources, performance, production and every Gate claim remain excluded; OD-05 and D-3 stay open. | A parallel fact-selecting v2 topology can remove every third-card route, bind normal review to schema v3, make setup-only spare and pre-scan Kit-door discipline explicit, and stop honestly at later-slice Kit boundaries without reviving v1 semantics or manufacturing unavailable capabilities. |
| 2026-08-29 | QK-DEC-127 | Owner ratifies v2 migration slice 6 as the parallel HOST-only two-key watch-only coordinator-export successor to QK-DEC-118. The new `WatchOnlyCoordinatorTierV2` vocabulary is exactly `SimpleRecovery`, `Inheritance`, and `QuantumShelter`: the first two receive one SD artifact and `QuantumShelter` rejects as the distinct `QuantumShelterDescriptorExport`; the universal Recovery Kit, wallet threshold and packaging facts are unchanged. The normative wire authority remains the already-pinned bitcoin/bips commit `7fe0b034ec967b52a5a28276419117326df93263`, BIP-129 blob `b1d054662b9d6f86d408d9f26505f13256e015ab` and BIP-389 blob `43497061ddaf80f0260d4c7c32e33d653473017b`, with the existing local BIP-380 checksum authority. Named compatibility is bounded only to Sparrow 2.5.4 commit `8871f4f1af528a4673fee6129373c884e3267860` with its pinned drongo gitlink `080cf3f7cf74133ba68b369065d0f2e7ea4337da`, and libnunchuk commit `01778037708d8e97a2c0b7ea95259a4b4323520d`; no Nunchuk application version, later source behavior, or other coordinator support is claimed. The sole construction entry consumes a borrowed `ProvisioningArtifactsV2`, strictly reparses its exact 306-byte receive and change D strings through `parse_descriptor_pair_v2`, requires wallet-ID equality, rederives branch-zero/index-zero and branch-one/index-zero scripts through the four-derivation v2 path, and requires byte equality with both supplied 34-byte scripts and both supplied 62-byte mainnet addresses before any artifact exists. The private retained binding contains only the two D strings, wallet ID, scripts and addresses; it excludes the A1 capsule and account-xpub copies. Construction preserves registered role A then B expression order and every origin and account xpub, replaces exactly the two branch digits at descriptor positions `[153,292]` in lockstep with BIP-389 `<0;1>` at multipath positions `[153,296]`, and recomputes the BIP-380 checksum. The exact geometry is 305-byte multipath body, 314-byte full descriptor and 408-byte record; the registered GOLDEN descriptor checksum is `vnpen3f9` and its record SHA-256 is `b5493ced45cff457010d5f0b44982576b192c8524d102471ba9a57a1ab14b158`. Before construction and again from reopened bytes, expansion to `/0/*` and `/1/*`, checksum recomputation and strict v2 reparse must reproduce the two authoritative 306-byte D strings byte-for-byte or `DescriptorRoundTripMismatch` rejects. The canonical file is exactly four nonempty 7-bit-ASCII lines encoded as UTF-8, each LF-terminated including the last: literal `BSMS 1.0`; the exact checksummed two-role `wsh(sortedmulti(2,.../<0;1>/*...))` descriptor; literal `No path restrictions`; and the exact receive branch-zero/index-zero address. No BOM, CR, NUL, blank, comment, label, wallet name, wallet ID, birthday, rescan hint, duplicated xpub, instruction or fifth line exists. Reopened-byte rejection precedence is exact length, 7-bit encoding and four-line framing, version, descriptor encoding/checksum, restrictions, address shape, then semantic D/fact rebinding and exact whole-record equality; the closed `BsmsErrorV2` categories are `InvalidDescriptorPair`, `WalletIdMismatch`, `FirstScriptMismatch`, `FirstAddressMismatch`, `DescriptorRoundTripMismatch`, `InvalidRecordLength`, `InvalidRecordEncoding`, `InvalidVersionLine`, `InvalidDescriptorLine`, `InvalidRestrictionsLine`, and `InvalidAddressLine`. V1 records receive no translation or fallback. Immutable metadata binds exact serialized length, SHA-256, wallet ID and both existing receive/change index-zero coordinator-comparison address facts; the exporter derives, normalizes or reformats no additional comparison fact. The exact final name remains `qk-<32-lowercase-hex-caller-nonce>-watch.bsms`, where the caller supplies the opaque 128-bit nonce and HOST creates no randomness; the temporary name appends `.tmp`. Publication reuses the unchanged QK-DEC-112 mock-SD failure categories and exact create-new temporary, complete write, sync, close, reopen, exact length/hash/full syntax and semantic verification, then atomic no-replace rename order. A final name exists only after complete verification; no input or existing destination is overwritten or deleted, and any remaining temporary file is invalid to every consumer. The v2 fixture extends only the QK-DEC-121 public GOLDEN lineage, states `PERMANENTLY NEVER-FUND PUBLIC PRIVATE MATERIAL`, adds no key or authority, and lands byte-verbatim only after complete first-commit SOURCE-REGISTER provenance from two separately written destroyed outside-Git constructors agreeing on every semantic byte. One new ring-fenced target, `qk_host_sim_v2_s6_watch_only`, receives a separately persisted, minimized, hash-registered corpus and one 100,000-input AddressSanitizer campaign under the pinned nightly with two-copy minimization agreement; its oracles lock exact construction, both D round trips, fact and tier fences, every line and named precedence, nonce/name behavior, all mock-SD faults and collisions, namespace preservation, deterministic repeat consistency, no panic, timeout, sanitizer finding, wrong acceptance, wrong named rejection, partial valid artifact, or residual fuzz artifact. Existing `bsms.rs`, `watch_only_export.rs`, every M28 source, test, fixture, target, corpus, manifest and campaign, M25 transaction export, slice-4 provisioning, slice-5 screen topology and all crate manifests remain byte-frozen. Exact slice-6 scope is only this row and the two v2 slice-6 entries in `docs/SOURCE-REGISTER.md`; `host/qk-host-sim/src/lib.rs`; new `src/bsms_v2.rs` and `src/watch_only_export_v2.rs`; new `tests/support/v2_s6.rs`, `tests/watch_only_v2_fixture.rs`, `tests/watch_only_v2_rejections.rs`, `tests/watch_only_v2_lifecycle.rs`, `tests/watch_only_v2_surface.rs`, and `tests/fixtures/watch_only_v2.txt`; `fuzz/Cargo.toml`, `README.md`, `run-bounded.sh`, `replay-corpus.sh`, `reproduce.sh`, `minimize-corpus.sh`, and `minimize-finding.sh`; new `fuzz_targets/qk_host_sim_v2_s6_watch_only.rs`, new `corpus/qk_host_sim_v2_s6_watch_only/`, new `CORPUS-MANIFEST-V2-S6.tsv`, new `CAMPAIGN-012.md`, and the matching registration logic in `tools/check-fuzz-corpora.sh`. Every file not named is excluded. QR coordinator export, address packs, coordinator API/database/wallet automation, screen integration, real storage and power-cut evidence, approval, signing, card sessions, Kit frame/combine/generation/print/scanner/restore/spend, camera, display, target cleanup/resources, performance, production and every Gate claim remain excluded; transaction QR remains only the frozen P/T path and all physical-trigger lanes are unchanged. | One type-separated two-key successor can emit the exact standards record from registered v2 facts while strict D round trips, tier capability separation and the existing atomic SD model prevent v1 fallback, descriptor drift and transport widening. |
| 2026-08-29 | QK-DEC-128 | Owner re-adopts five topology-neutral requirements in Core Architecture v2: reproducible release builds remain Gate-C evidence; writable flash may neither simulate nor be described as an immutable root of trust; outside an active authorized session the terminal contains no wallet secret; the exact card protocol and open commodity-reader rescue tooling are published; and power-on begins in calculator decoy mode, which is camouflage rather than authorization. Authority A is session-held, reconstructed only during an active authorized session by authenticating and decrypting A1 with A2, and never terminal-resident at rest. If one Kit envelope is lost while A1 and B remain intact, normal A1+B remains available to sweep into a fresh wallet at convenience; the missing envelope or Kit is not recreated. Firmware direction is signed offline update packages verified only against compiled-in trust anchors, with no separately supplied anchor field and no downgrade bypass in any form; version-floor advancement and key-set changes commit only after a confirmed successful first boot; no Key Card is present during update; printed material carries the release-key fingerprints; and release hashes are Bitcoin-anchored. Firmware implementation is deferred until after slice 11 or a separately ratified lane. The Owner-supplied release-key ceremony design is ratified at decision level while execution is deferred; holder identities remain an open Owner item, the release-key holders are not established as independent, and no holder-independence claim is made. The exact human-assertion gesture is assigned to the slice-10 and slice-11 decision rows. The CM0 OTP secure-boot question is a stage-02 physical blocker with the recorded expectation that the answer is likely no; no such capability may be assumed without exact-target evidence. | Restores topology-neutral release, lifecycle, recovery, publication and decoy invariants without claiming unavailable hardware properties or starting firmware work. |
| 2026-08-29 | QK-DEC-129 | Owner ratifies v2 migration slice 7 as a dependency-free, zero-direct-allocation HOST-only `qk-kit` reference for the exact Kit share frame, M18 fallback, deterministic logical QR symbol, and opaque two-share combination. The canonical frame is exactly 142 bytes: ASCII `QKKS[4]` || version `01` || share index `01` or `02` || full `wallet_id[32]` || share `[96]` || the first eight bytes of `SHA256(ASCII "QuietKey/KitShare/v1" || 00 || preceding 134 frame bytes)`. Parsing precedence is exact length, checksum over the still-uninterpreted prefix, magic, version, then index; each stage has its distinct `KitError`, checksum comparison releases no field, and every rejection leaves every caller output byte unchanged. Frame construction takes fixed-size caller-owned facts and generates no randomness. The fallback is the exact QK-DEC-090 alphabet `23456789abcdefghijkmnpqrstuvwxyz`, packs the complete frame MSB-first into exactly 228 symbols, requires the four low pad bits to be zero, accepts no separator, line byte, alias, case fold, whitespace, erasure, correction, normalization, or other symbol, and rejects exact length, malformed symbol, nonzero padding, then decoded-frame failure as distinct named outcomes; four lines of 57 remain presentation only and the terminal input method remains slice 9. The logical QR encoder accepts only a valid canonical frame and fixes version 10, ECC Q, byte mode, no ECI, 57x57 core, four-module quiet zone and therefore a 65x65 logical output; geometry is const-derived as 154 data codewords, 192 parity codewords, eight blocks `6x(19+24)+2x(20+24)`, 346 interleaved codewords, 2,768 placed bits and zero remainder bits. For deterministic interpretation of QK-DEC-121's lowest-ISO-penalty rule, each candidate mask is scored on the complete 57x57 final symbol after both format and version copies are written, with the quiet zone excluded: N1 is `3+(run-5)`, N2 is three per same-color 2x2, N3 is forty for each qualifying side of every `n:n:3n:n:n` dark/light run ratio whose neighboring light run is at least `4n`, treating the outside border as light and counting both qualifying sides, and N4 gives the inclusive 45 through 55 percent dark band zero using `((abs(dark*20-total*10)+total-1)/total floor)-1` ten-point steps. The strictly lower score wins and ties retain the lowest-number mask. This fully enumerated local profile under QK-DEC-121/QK-DEC-129 is normative for QuietKey; no unpinned QR edition or third-party generator is made authoritative. QR decoding, image recognition and geometry rejection remain slice 9. Combination first parses the left frame fully, then the right, rejects byte-identical frames as `DuplicateShare`, other equal-index pairs as `SameShareIndex`, then unequal wallet IDs as `WalletMismatch`, normalizes the valid pair by index, and XORs its 96 share bytes into one opaque owner; caller order cannot affect the result. Parsed share owners and the combined owner have optimization-resistant wipe-on-drop behavior and no public secret field, byte accessor, serializer, formatter, logger, clone, copy, debug, display, equality or snapshot surface. A combined payload is not released by this slice; descriptor reconstruction, wallet-ID rebinding and any permitted consuming operation remain slices 10 and 11. Tests lock the registered v2 GOLDEN frame/checksum, both 228-symbol fallbacks, all QR data/interleaved codewords, every mask score, selected masks and exact 57x57/65x65 packed matrices; all frame and fallback round trips; output immutability and rejection precedence; both caller orders; duplicate, same-index and two-wallet substitutions; opaque surface; direct allocation count zero; and wipe behavior. The QK-DEC-121 public lineage remains `PERMANENTLY NEVER-FUND PUBLIC PRIVATE MATERIAL`; two separately written outside-Git constructors using two separately pinned QR generators agree byte-for-byte on the complete fixture, and all generator, dependency and duplicate-output material is destroyed before commit under QK-DEC-047. The external tools are behavioral cross-checks only and no external source byte or dependency enters the workspace. Two M21-style targets, `qk_kit_v2_s7_codec` and `qk_kit_v2_s7_combine`, each execute 100,000 inputs under the pinned nightly with AddressSanitizer, overflow checks and debug assertions; their separately persisted, two-copy-minimized, hash-registered corpora lock deterministic repeat consistency, named errors, no panic, timeout, wrong acceptance, output mutation or premature payload release. Exact slice-7 scope is only this row and the three slice-7 entries in `docs/SOURCE-REGISTER.md`; `host/Cargo.toml`, `host/Cargo.lock`; new `host/qk-kit/Cargo.toml`, `src/lib.rs`, `src/frame.rs`, `src/fallback.rs`, `src/qr.rs`, `src/sha256.rs`, `src/secret.rs`, `tests/codec.rs`, `tests/combination.rs`, `tests/allocation.rs`, `tests/surface.rs`, and `tests/fixtures/kit_share_v2.txt`; `fuzz/Cargo.toml`, `fuzz/Cargo.lock`, `fuzz/DEPENDENCY-ALLOWLIST.tsv`, `fuzz/README.md`, `fuzz/run-bounded.sh`, `fuzz/replay-corpus.sh`, `fuzz/reproduce.sh`, `fuzz/minimize-corpus.sh`, `fuzz/minimize-finding.sh`, new `fuzz/fuzz_targets/qk_kit_v2_s7_codec.rs`, `fuzz/fuzz_targets/qk_kit_v2_s7_combine.rs`, new `fuzz/corpus/qk_kit_v2_s7_codec/`, `fuzz/corpus/qk_kit_v2_s7_combine/`, new `fuzz/CORPUS-MANIFEST-V2-S7.tsv`, new `fuzz/CAMPAIGN-013.md`, and matching registration logic in `tools/check-fuzz-corpora.sh`. Every existing file not named remains byte-frozen, especially `qk-a1-codec`, `qk-provisioning`, `qk-host-sim`, every earlier fixture, target, corpus, manifest and campaign, and `tools/check.sh`. Setup generation or post-setup regeneration, copy/page rendering, print export, scanner or fallback entry, camera, display, card, persistence, Kit-Restore, Kit-Spend, target cleanup/resources, constant-time, performance, production and every Gate claim are excluded; physical-trigger lanes remain unchanged. | One fixed-memory owner can validate and encode each envelope independently, bind and combine only the one valid opposite-index same-wallet pair, and carry the candidate payload opaquely into later rebinding while exact QR/fallback bytes become reproducible without creating a scanner, generator, or recovery capability early. |
| 2026-08-29 | QK-DEC-130 | Owner ratifies v2 migration slice 8 as the HOST-only, setup-only Kit generator and logical two-copy print-export boundary. The sole operation is `HostProvisioningRunV2::emit_two_kit_copies`, which consumes the exact slice-4 secret owner, requires the run's one A1 capsule to have been created, and otherwise returns named `A1NotReady`; success and every failure consume and wipe the run. It constructs share 1 as the retained Kit-R pad and share 2 as the retained setup payload XOR that pad, then calls only the byte-frozen slice-7 `qk-kit` encoder. There is no clone, second call, retry token, configurable copy count, retained payload, pad, share or frame state, post-setup regeneration/reissue API, or path that reaches this operation from a later wallet session. The operation emits exactly four immutable scoped page views to one caller callback in this fixed order: copy 1/index 1, copy 1/index 2, copy 2/index 1, copy 2/index 2. Same-index fallback and QR bytes are byte-identical across copies, every opposite-index cross-copy pairing reconstructs the exact registered payload under the same full wallet ID, and each view contains only one share's 228-symbol fallback in four typed 57-symbol presentation lines and its exact 529-byte packed 65x65 QR plus typed public copy, index, wallet and QR metadata. The callback sees no payload, pad, raw share, mnemonic, seed, descriptor, A2, both-share view, owned share-bearing value, formatter or logger surface; its short-lived borrow cannot be retained by type, while any physical or malicious copy beyond that scoped boundary is explicitly outside this HOST claim. A closed accepted/rejected callback result is required. Rejection is named `PrintRejected`, stops before every later page and returns no completion receipt; any page already observed by the external sink is a tombstone under the existing physical procedure and cannot be retracted or erased by this API. Only four accepted callbacks return a privately constructed, non-secret `KitSetupReceiptV2` binding wallet ID and the fixed two-copy/four-page completion facts. No automatic retry exists, and open `QK-LIM-KIT-009` and `QK-LIM-KIT-010` remain unselected. The slice-7 frame, fallback and QR output buffers created by this generation path are all share-equivalent. One fixed 899-byte frame/fallback/QR buffer set is reused serially; an optimization-resistant guard wipes every byte before first use, after each callback on acceptance or rejection, on every named error, and on unwind/drop. A callback runs only after all three representations for that page are complete and coherent. The payload-XOR result, frame, codec outputs and all other caller scratch follow the same wipe-on-completion discipline; the retained payload and pad are also wiped when the consuming run exits. Volatile HOST writes establish only the tested software cleanup path and make no power-loss, register, allocator, print-spool, physical-page, malicious-caller, target-remanence or Gate claim. Tests lock the two registered QK-DEC-121 GOLDEN fixtures without adding a fixture or SOURCE-REGISTER entry: `provisioning_v2.txt` SHA-256 `04161895860df1b672e91e3249471dde9564cef13dd72d5b3a45b240e9d79741` and `kit_share_v2.txt` SHA-256 `0ab559508e65c7758aa183975db620b3506cebd9ab63cf50a27e14ca6daf80f7`. They assert the exact four-page order, copy identity, all four cross-pair combinations, frame/fallback/QR facts, A1 gate, first-rejection stop, no partial callback, receipt release only after page four, caller-buffer and secret-scratch wipe on success, each named failure and caught callback unwind, and absence of any regeneration or public secret-bearing trait. One ring-fenced `qk_provisioning_v2_s8_kit_setup` target receives a separately persisted, two-copy-minimized, hash-registered corpus and executes 100,000 inputs under the pinned nightly with AddressSanitizer, overflow checks and debug assertions; it covers deterministic success, missing A1 and rejection at each callback while requiring stable named outcomes, exact order/copy identity, no panic, wrong acceptance, partial callback, later callback after rejection, premature receipt or nonzero caller buffer on ordinary exit. This slice creates only logical renderer inputs; exact page prose, fingerprint representation, pixels, fonts, placement, PDF/file bytes, printer/spool/cache behavior, physical attempts and verification, envelopes, seals, custody, scan-back, fallback entry, camera, display, card, media, power-cut behavior, target resources, performance, constant-time, production and every Gate claim remain excluded, and slices 9 through 11 remain unopened. Exact slice-8 scope is only this row in `docs/DECISION-LOG.md`; `host/Cargo.lock`; `host/qk-provisioning/Cargo.toml`, `src/lib.rs`, `src/secret.rs`, new `src/kit_setup_v2.rs`, `tests/surface.rs`, and new `tests/kit_setup_v2.rs`; `fuzz/Cargo.toml`, `fuzz/Cargo.lock`, `fuzz/DEPENDENCY-ALLOWLIST.tsv`, `fuzz/README.md`, `fuzz/run-bounded.sh`, `fuzz/replay-corpus.sh`, `fuzz/reproduce.sh`, `fuzz/minimize-corpus.sh`, `fuzz/minimize-finding.sh`, new `fuzz/fuzz_targets/qk_provisioning_v2_s8_kit_setup.rs`, new `fuzz/corpus/qk_provisioning_v2_s8_kit_setup/`, new `fuzz/CORPUS-MANIFEST-V2-S8.tsv`, new `fuzz/CAMPAIGN-014.md`, and matching v2-slice-8 registration logic in `tools/check-fuzz-corpora.sh`. Every file not named remains byte-frozen, especially every `qk-kit` file, `qk-host-sim`, qk2 document, existing fixture, target, corpus, manifest and campaign, and `tools/check.sh`; all physical-trigger lanes remain unchanged. | A consuming secret owner and one guarded reusable page buffer can expose the fixed four print-boundary observations without durable share storage or a later generation capability, while deferring every physical print and human-factor choice to its assigned evidence gate. |
| 2026-08-30 | QK-DEC-131 | Owner ratifies v2 migration slice 9 as the HOST-only, mode-locked Kit-share intake continuation from QK-DEC-126 and the byte-frozen QK-DEC-129 codec. `KitIntakeSessionV2::begin` consumes a `ScreenFlowV2` only at `ScanKitShareOne`, after one typed `KitSpend` or `KitRestore` door has been selected and confirmed; it also receives exactly one `KitInputModeV2`, `Scanner` or `Fallback`, which is immutable across both share screens. A new session is required to change either door or input mode. Scanner mode accepts only one caller-owned mutable 142-byte hostile byte-mode payload per screen, already decoded from the printed Kit QR by the future `qk-io` boundary, and revalidates it through `qk_kit::frame_metadata`; it accepts no image, generic camera event, A1, PSBT, BBQr transaction, coordinator, transport, generic intake, QR wrapper, alternate version, or other payload type. This HOST seam makes no image-recognition, error-correction, QR-candidate, camera, capture-attempt, or geometry-performance claim: QK-REQ-PLT-009 leaves those transport facts with `qk-io` and their exact-target evidence remains Gate A. Fallback mode is now fixed as a two-key coordinate transcription over the exact M18 alphabet arranged semantically, not as pixel layout, in four rows of eight: `23456789`, `abcdefgh`, `ijkmnpqr`, `stuvwxyz`. With no row pending, numeric key 1 through 4 selects the row; with a row pending, numeric key 1 through 8 selects the column and appends that one exact lowercase symbol. `CE` clears a pending row, otherwise deletes the most recent committed symbol; red `C` cancels and wipes. `Equals` submits only with exactly 228 committed symbols and no pending row. There is no alias, case fold, separator, line byte, whitespace, erasure, correction, normalization, guess, paste, bulk string, A1-reader reuse, timing/multitap interpretation, or alternate alphabet. The four printed lines remain presentation only; the typed screen exposes only the fixed table, selected door and mode, page ordinal, committed-symbol count, pending row, and const-derived line/column progress, never an owned transcript or frame. At the exact applicable stage, every defined navigation/edit/submit action is accepted; invalid row key, invalid column key, empty delete, incomplete submit, submit with a pending coordinate, and append after 228 symbols are distinct `KitIntakeErrorV2` rejections. Every such rejection terminates and wipes rather than selecting an unratified retry count. Supplying scanner bytes in fallback mode, any fallback key in scanner mode, any generic or foreign input at either share screen, or any in-session mode change is the exact QK-DEC-126 `KitScannerModeMismatch` wiping outcome; door reselection remains the separate `DoorSwitchAttempt`. Cancellation, operation failure, media or card removal, timeout, shutdown, restart, power loss, invalid start/state, codec rejection, duplicate share, same share index, wallet mismatch, or drop likewise wipes and terminates. The first valid canonical frame is retained only in a fixed secret owner while screen two is active. The second candidate is validated before `qk_kit::combine_frames`; only one valid different-index same-wallet pair advances the underlying topology through both typed share acceptances and combination. Success returns one non-clonable `KitIntakeReadyV2` that inseparably owns the exact opaque `RecoveredKitPayload`, locked door, input mode, full wallet ID, and both canonical non-secret frame identities in presentation order. It exposes no payload, frame, share, seed, A2, descriptor, serializer, formatter, logger, generic flow escape, signing, restore, or spend operation; slices 10 and 11 alone may consume it. The public QK-DEC-126 assertion-only events remain topology tests and are not accepted by any later semantic slice as an intake capability. Every caller or session buffer holding scanned or keyed share-equivalent bytes receives the QK-DEC-129/QK-DEC-130 cleanup discipline: the 142-byte scanner candidate, 228-byte fallback transcript, 142-byte decoded fallback frame, retained first frame, second-frame scratch, and every temporary copy are cleared by volatile writes plus a compiler fence on acceptance, every named rejection, cancellation/interruption, ordinary completion, and unwind/drop. The sole new unsafe code is that private fixed-buffer wipe boundary; the rest of `qk-host-sim` remains unsafe-denied. The returned qk-kit payload owner retains its established wipe-on-drop behavior. These HOST writes prove only the exercised software path and make no power-loss, register, allocator, camera, framebuffer, swap, crash-data, target-remanence, constant-time, production, or Gate claim. The scanner core accepts at most the one expected candidate at each screen because any rejection ends the session; this is not a product capture retry value. `QK-LIM-KIT-003` through `QK-LIM-KIT-008`, `QK-LIM-KIT-011`, and `QK-LIM-KIT-012` remain open and unselected. Tests reuse the registered `kit_share_v2.txt` fixture byte-verbatim and add no fixture or SOURCE-REGISTER entry. They lock both doors, both modes, both frame orders, every cross-copy pairing, exact coordinate mapping and all 228 positions, CE boundaries, every named precedence, foreign-input mode mismatch at both screens, door/mode immutability, no payload before valid combine, exact same-wallet/different-index facts, zero direct allocation, opaque surface, stable terminal behavior, all closed interruptions at each stage, caller-buffer clearing, session drop, and caught unwind. One ring-fenced `qk_host_sim_v2_s9_kit_intake` target receives a separately persisted, two-copy-minimized, hash-registered corpus and executes 100,000 inputs under the pinned nightly with AddressSanitizer, overflow checks and debug assertions; its oracles require deterministic named outcomes, exact mode and door binding, no panic, timeout, sanitizer finding, wrong acceptance, scanner-mode escape, post-rejection work, premature payload, input over-consumption, nonzero caller buffer on ordinary exit, or residual artifact. Exact slice-9 scope is only this row in `docs/DECISION-LOG.md`; `host/Cargo.lock`; `host/qk-host-sim/Cargo.toml`, `src/lib.rs`, `src/screen_flow_v2.rs`, new `src/kit_intake_v2.rs`, `tests/surface_v2.rs`, new `tests/kit_intake_v2.rs`, and new `tests/kit_intake_v2_allocation.rs`; `fuzz/Cargo.toml`, `fuzz/Cargo.lock`, `fuzz/DEPENDENCY-ALLOWLIST.tsv`, `fuzz/README.md`, `fuzz/run-bounded.sh`, `fuzz/replay-corpus.sh`, `fuzz/reproduce.sh`, `fuzz/minimize-corpus.sh`, `fuzz/minimize-finding.sh`, new `fuzz/fuzz_targets/qk_host_sim_v2_s9_kit_intake.rs`, new `fuzz/corpus/qk_host_sim_v2_s9_kit_intake/`, new `fuzz/CORPUS-MANIFEST-V2-S9.tsv`, new `fuzz/CAMPAIGN-015.md`, and matching v2-slice-9 registration logic in `tools/check-fuzz-corpora.sh`. Every file not named remains byte-frozen, especially all `qk-kit` files, provisioning, PSBT, descriptor, signing, finalization, export, card, qk2 document, existing fixture, target, corpus, manifest and campaign files, and `tools/check.sh`; Kit-Restore, Kit-Spend, actual qk-io/camera/display, physical fallback usability, resource selection, target integration and all physical-trigger lanes remain unchanged. | One fixed-memory owner can bind the preselected Kit door and one exact input representation to both hostile share submissions, erase every share-equivalent scratch boundary, and release only an opaque same-wallet opposite-index capability for the next migration slice. |
