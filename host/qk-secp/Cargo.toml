[package]
name = "qk-secp"
version = "0.0.1"
edition = "2021"
publish = false
description = "Verification-only libsecp256k1 v0.8.0 FFI boundary over the vendored pinned tree. HOST evidence only. Not product code. Not a wallet. No QuietKey signing wrapper, callable Rust signing API, or PSBT-integrated signing path; no secret-key inputs; the vendored tree and native archive retain dormant upstream signing symbols that qk-secp never declares or calls. No PSBT integration, no conformance or target claim."
