[package]
name = "qk-ipc"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"
publish = false
description = "Dependency-free HOST reference for bounded QuietKey process IPC with a non-default Unix receive boundary."

[features]
fuzzing = []
host-runtime = []

[dependencies]
