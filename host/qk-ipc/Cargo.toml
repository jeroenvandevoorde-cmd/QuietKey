[package]
name = "qk-ipc"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"
publish = false
description = "Dependency-free HOST reference for the bounded QuietKey process IPC envelope. Not a socket adapter or wallet."

[features]
fuzzing = []

[dependencies]
