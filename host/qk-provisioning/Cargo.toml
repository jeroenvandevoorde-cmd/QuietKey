[package]
name = "qk-provisioning"
version = "0.0.1"
edition = "2021"
publish = false
description = "HOST-only deterministic reference for the ratified v1 and v2 provisioning mathematics. No entropy, card, ceremony, target, or product claim."

[features]
fuzzing = []

[dependencies]
qk-a1 = { path = "../qk-a1" }
qk-descriptor = { path = "../qk-descriptor" }
qk-kit = { path = "../qk-kit" }
qk-secp = { path = "../qk-secp" }
qk-wallet-v2 = { path = "../qk-wallet-v2" }
