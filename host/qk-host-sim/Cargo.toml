[package]
name = "qk-host-sim"
version = "0.0.1"
edition = "2021"
publish = false
description = "Library-only HOST seams: a legacy symbolic runner plus D-09 review binding and final-only insertion of externally produced signatures. No signature generation, finalization, extraction, media output, UI, or target claim."

[dependencies]
qk-host-model = { path = "../qk-host-model" }
qk-psbt = { path = "../qk-psbt" }
qk-descriptor = { path = "../qk-descriptor" }
