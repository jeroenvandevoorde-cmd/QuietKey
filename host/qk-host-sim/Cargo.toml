[package]
name = "qk-host-sim"
version = "0.0.1"
edition = "2021"
publish = false
description = "Library-only HOST scaffolds: a legacy payload-free symbolic runner and a distinct D-09 review-binding seam. Not product code; no target, UI, wallet, signing, finalization, or export."

[dependencies]
qk-host-model = { path = "../qk-host-model" }
qk-psbt = { path = "../qk-psbt" }
qk-descriptor = { path = "../qk-descriptor" }
