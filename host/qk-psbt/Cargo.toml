[package]
name = "qk-psbt"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"
publish = false
description = "Bounded PSBT v0 parsing, schema-v3 proof, and feature-gated purpose-bound normal finalization (no signing or media I/O). HOST evidence only."

[features]
default = []
normal-v3 = []

[dependencies]
qk-descriptor = { path = "../qk-descriptor" }
qk-secp = { path = "../qk-secp" }
