[package]
name = "qk-device-wire"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"
publish = false
description = "Dependency-free HOST reference for the bounded QuietKey mock-device wire."

[features]
fuzzing = []

[dependencies]
