[package]
name = "qk-card-trace"
version = "0.0.1"
edition = "2021"
license = "Apache-2.0"
publish = false
description = "Bounded offline HOST checker for registered QuietKey card-bench traces. No card, PC/SC, applet, secret, or production protocol surface."

[dependencies]
